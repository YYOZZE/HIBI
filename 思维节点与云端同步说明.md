# 思维节点与云端同步说明

## 一、数据存储位置

- **本地**：思维节点（项目 + 白板 `canvasItems`）保存在当前账号目录的 `hibi_mind_nodes.json`。  
  路径由 `AccountStoragePaths.mindNodesFile()` 统一管理，多账号目录为 `hibi_accounts/<账号key>/`。
- **云端**：按用户存储在后端 `user_data` 中的 `mind` 字段，通过 `GET /api/sync/pull` 与 `POST /api/sync/push` 同步。

---

## 二、同步触发时机（当前实现）

| 时机 | 动作 |
|------|------|
| **登录 / 注册成功** | 先 `pull`（拉取云端）并与本地合并，写回本地后再 `push` 一次，确保云端与本地都变为合并后的最新状态。 |
| **退出登录前** | 先 `push` 当前本地数据到云端，再清理登录态。 |
| **本地写盘后** | `MindRepository._save()` 会触发 `UserSyncScheduler.requestPush()`，采用约 **300ms 防抖**上传整包。 |
| **应用回前台** | 执行 `pullAndNotify()`：先取消待推送，再拉取并合并，最后统一 `reloadFromDisk()` + `syncEpoch` 通知页面刷新。 |
| **白板页打开期间** | 周期执行 `pullForCanvasAndNotify()`，实现跨端近实时可见；拉取频率已做交互态自适应（见下文）。 |

---

## 三、核心同步原理（防旧覆盖新）

### 1) 先拉后推（云端优先保护）

- 在 `pull` 前会取消未执行的本地 `push`（`cancelPendingPush()`），避免“本地旧快照先上传覆盖云端新数据”。
- 回前台和白板实时拉取都走这条路径，降低覆盖风险。

### 2) 按 `id + updatedAt` 合并

- 思维节点合并在 `SyncMerge.mergeMindLists()` 完成：
  - 按节点 `id` 去重；
  - 同 `id` 仅保留 `updatedAt` 更新的一条（ISO 时间字符串比较）。
- 这保证了“新版本节点”不会被旧版本节点回写覆盖。

### 3) 仓库重载与单例一致性

- `pull` 后统一调用 `MindRepository.instance.reloadFromDisk()`，页面通过 `syncEpoch` 刷新，避免内存旧对象继续参与保存。
- `MindRepository.update()` 使用 upsert（按 id 替换/插入），确保修改能落到当前仓库内真实节点。

---

## 四、白板近实时同步与体验优化

### 已实现功能

- **跨端近实时可见**：白板页拉取周期可到 500ms 级别，另一端修改可快速出现。
- **拖拽/编辑防抖动保护**：用户正在拖拽、连线、平移、编辑时，不应用远端覆盖刷新。
- **交互后短缓冲**：交互结束后的短时间窗口内暂不应用远端覆盖，避免元素“跳来跳去”。
- **自适应拉取频率**：
  - 最近有交互：保持快频率（约 500ms）；
  - 长时间空闲：自动降频（约 2s），减少无效拉取和干扰。

### 设计目标

- 在“实时可见”与“本端操作稳定”之间平衡：  
  **你在操作时本端优先稳定，停止操作后快速追云端。**

---

## 五、当前已落地的能力清单

- 思维节点多端同步（登录态）。
- 白板改动自动写盘 + 防抖推送（300ms）。
- 回前台自动拉取并刷新。
- 白板页近实时拉取刷新（含并发保护）。
- 交互中远端覆盖抑制（防拖拽跳动）。
- 思维合并规则：`id + updatedAt` 防旧覆盖新。
- 仓库重载与 upsert，防旧引用导致的保存失效。

---

## 六、文件与代码位置（便于排查）

- 同步调度：`lib/features/auth/services/user_sync_scheduler.dart`
- 合并策略：`lib/features/auth/services/sync_merge.dart`
- 思维仓库：`lib/features/mind/services/mind_repository.dart`
- 白板实时拉取与交互保护：`lib/features/mind/mind_canvas_page.dart`

---

## 七、补充说明

- 当前为客户端轮询 + 合并方案，已兼顾“近实时”与“防覆盖”。
- 若后续需要进一步降低流量并实现“仅有变更才拉取”，可在后端增加版本号/增量接口或 WebSocket 推送，再切换为事件驱动同步。
