# 服务订阅后端笔记

## 1. 目标与范围

本文档记录「服务增值/订阅」后端实现与运维要点，覆盖：

- 套餐与金额在哪里配置
- 订单创建、支付回调、权益计算的后端链路
- ECS 环境变量与部署自检
- 常见故障与排查步骤

---

## 2. 后端核心文件

- `backend_jideshi_hibi_app/hibi_payment.py`
  - 订阅套餐配置（价格/时长）
  - 订单表初始化
  - 创建订单（可调用数捷下单）
  - 支付回调验签与订单更新
  - 用户权益计算
- `backend_jideshi_hibi_app/api_only_app.py`
  - 暴露支付相关 API 路由
  - 调用 `hibi_payment.py` 的业务方法

---

## 3. 套餐金额与时长配置位置（最关键）

### 3.1 真实扣款金额（后端生效）

在 `backend_jideshi_hibi_app/hibi_payment.py` 的 `PLANS`：

- 金额单位是**分**（`amount_cents`）
- 示例：`10000` 代表 `100.00 元`

当前结构（示意）：

- `plan_id: (金额分, 展示名, 展示文案)`

### 3.2 订阅有效期（后端生效）

在同文件的 `VALID_DAYS`：

- 季度：90 天
- 年：365 天
- 月：30 天

支付成功后，后端按 `VALID_DAYS` 计算 `valid_until`（到期时间戳）。

### 3.3 前端价格文案（仅展示）

在 `lib/features/profile/value_added_page.dart` 的 `_plans.priceLabel`。

注意：前端文案不决定实际收费。  
若修改价格，必须同步修改后端 `PLANS`，再更新前端展示文案，避免前后不一致。

---

## 4. 数据表与状态流转

### 4.1 订单表

`payment_orders`（SQLite，默认与用户库同库）关键字段：

- `id`: 本地订单号（如 `hibi_xxx`）
- `user_id`
- `plan_id`
- `amount_cents`
- `status`: `pending` / `paid`
- `external_order_id`: 三方流水号
- `created_at` / `paid_at`
- `valid_until`: 权益到期时间
- `notify_raw`: 回调原文（截断保存）

### 4.2 流程

1. 客户端调用 `POST /api/payment/create_order`
2. 后端写入 `pending` 订单
3. 若数捷参数齐全：向数捷发起下单，返回 `pay_url`
4. 用户支付后，数捷回调 `POST /api/payment/notify`
5. 后端验签通过后，更新订单为 `paid`，写入 `valid_until`
6. 客户端调用 `GET /api/payment/my_entitlements` 刷新权益

---

## 5. API 清单（后端）

- `GET /api/payment/config_status`
  - 返回数捷配置是否就绪（布尔，不返回密钥）
- `POST /api/payment/create_order`（需登录）
  - body: `{"plan_id":"xxx"}`
  - return: `order_id`, `pay_url`, `amount`, `subject`
- `GET /api/payment/order/{order_id}`（需登录）
  - 查询订单状态
- `GET /api/payment/my_entitlements`（需登录）
  - 返回当前权益：`pro`、`basic_plans`、`pro_valid_until`
- `POST /api/payment/notify`
  - 数捷异步回调入口（form-urlencoded）
- `GET /api/payment/pay_page`
  - 未接好数捷时的兜底说明页

---

## 6. 权益规则

- 基础服务：`data_service`、`assistant_service`、`theme_service`
  - 可单独购买，互不依赖
- PRO 档：`pro_max`、`pro_max_1tb`
  - 购买后默认包含全部基础服务
  - 等级优先：`pro_max_1tb` > `pro_max`
- 仅 `status=paid` 且 `valid_until >= 当前时间` 的订单计入有效权益

---

## 7. ECS 环境变量

至少应配置：

- `SHUJIEPAY_SUBMIT_URL`（数捷完整下单 URL）
- `SHUJIEPAY_NOTIFY_URL`（公网可访问回调地址）
- `SHUJIEPAY_MCH_ID`（商户号）
- `SHUJIEPAY_MCH_PRIVATE_KEY`（商户私钥，已配置）
- `SHUJIEPAY_PLATFORM_PUBLIC_KEY`（平台公钥，已配置）

可选：

- `SHUJIEPAY_RETURN_URL`
- `HIBI_BASE_URL`

---

## 8. 部署与自检

### 8.1 同步后端到 ECS

在项目根目录执行：

```powershell
.\backend_jideshi_hibi_app\sync_to_server.ps1
```

### 8.2 重建并重启容器（ECS）

```bash
cd /root/jideshi_hibi_backend
docker build -t jideshi_hibi_api .
docker restart jideshi_hibi_api
```

### 8.3 配置健康检查

访问：

```text
http://121.41.6.21:7861/api/payment/config_status
```

当返回 `ready: true`，说明数捷关键配置齐全。

---

## 9. 常见问题排查

### 9.1 下单后跳到说明页，不是数捷收银台

原因通常是 `SHUJIEPAY_SUBMIT_URL` / `SHUJIEPAY_MCH_ID` / `SHUJIEPAY_NOTIFY_URL` 未正确配置，或下单异常。  
先看 `config_status` 是否 `ready=true`，再看后端日志是否有“数捷下单请求失败”。

### 9.2 支付成功但权益没生效

优先排查：

1. 数捷是否真正回调到 `/api/payment/notify`
2. 回调验签是否通过（平台公钥是否正确）
3. `trade_status` 是否为成功值
4. 订单是否仍为 `pending`

### 9.3 前端显示价格与实际扣款不一致

后端 `PLANS` 与前端 `priceLabel` 不一致导致。  
以后端 `PLANS` 为准，前端仅展示。

---

## 10. 全功能试用（`trial_all`）

- **套餐 id**：`trial_all`，在 `hibi_payment.py` 的 `PLANS` 中配置（默认 **0 元 / 182 天**，不参与自动续费）。
- **每账号一次**：若 `payment_orders` 中已存在该用户的 `plan_id=trial_all` 且 `status=paid`，则不可再次领取（含已过期）。
- **与付费订阅**：**不互斥**——已订阅 PRO/基础服务的用户仍可领取唯一一次试用；试用有效期与订阅有效期在时间上可**并行**（各自 `valid_until` 独立），权益展示上 PRO 仍优先覆盖基础能力。
- **开通方式**：`POST /api/payment/create_order` 与付费相同；后端对 0 元试用 **直接入账**（`immediate: true`、`status: paid`），**不跳转数捷**。
- **权益**：试用有效期内，`get_user_entitlements` 将 `basic_plans` 视为包含数据/助理/主题三项（与 PRO 覆盖的基础能力一致，但不写入 `pro` 字段）。
- **展示用剩余时长**：当 `trial_all` 为 `active` 时，对 `data_service` / `assistant_service` / `theme_service` 三条在 `plans[]` 中增加 **`display_remaining_seconds`** = `remaining_seconds`（该项真实剩余）**+** 试用剩余秒数；**不改变**各订单 `valid_until`，仅用于订阅页、画布同步条等 UI。`trial_all` 自身 `display_remaining_seconds` = 自身 `remaining_seconds`。
- **到期后**：套餐状态为 `trial_expired`，**无宽限期、无续订**；前端按钮置灰「已试用」。
- **部署自检**：若客户端点击「试用」仍提示 **「您已开通付费权益，无需再领取试用」**，说明线上容器/进程仍在运行 **旧版** `hibi_payment.py`（仍包含「付费不可试用」逻辑）。请将当前仓库中的 `hibi_payment.py` 同步到 ECS 后 **重建镜像或重启** FastAPI 服务，直至该文案不再出现。

## 11. 建议的运营改进（后续可做）

- 增加「订单状态轮询」接口调用策略（支付后 3~10 秒自动刷新一次）
- 增加「对账脚本」：按日比对已支付订单与权益有效期
- 增加后台管理页：在线修改套餐价格与时长（避免改代码发布）

