# 后台管理页说明

## 1. 功能目标

为「服务增值订阅」提供后台管理能力，支持运营侧查看和调整客户订阅数据：

- 客户列表与检索（账号/昵称/user_id）
- 客户总消费金额
- 每个套餐的订阅状态与剩余时间
- 客户创建时间、创建 IP/定位
- 最近登录时间、登录 IP/定位
- 管理员手动覆盖单套餐状态（active/grace/interrupted/inactive）
- 隐私条款在线编辑（App 内「设置-隐私条款」实时读取）
- **项目助理 System Prompt 在线编辑**（智能体工具调用 / 日程与思维节点；保存后写入服务端 SQLite，与 `hibi_abp_tools.DEFAULT_SYSTEM_PROMPT` 同源默认，首次进入后台已预填）
- **图形认证开关**（登录 / 注册是否必须完成阿里云图形验证；关闭后 `GET /api/auth/captcha_config` 的 `configured` 为 false，服务端不再校验图形参数）
- **升级推送地址**（配置最新版本号、更新说明；**Windows/Android 支持直接上传安装包并自动生成下载链接**，iOS 仍填 App Store/TestFlight 链接；App 启动后拉取 `GET /api/app/update_manifest` 对比版本，在「我的」页展示「版本更新」）

---

## 2. 登录页面与密码

### 2.1 登录入口

- 登录页：`/admin/login`
- 管理页：`/admin/customers`

未登录访问管理页时会自动跳转到登录页。

### 2.2 当前后台密码

- 默认后台密码：`tsinghibi`

> 已按需求写入后端默认值。

### 2.3 改后台密码方式

后台密码读取规则：

- 若 ECS `.env` 有 `ADMIN_DASH_PASSWORD`，优先使用该值
- 若未配置，则使用默认密码 `tsinghibi`

修改步骤（ECS）：

1. 登录服务器并编辑后端 `.env`（路径按你现有部署）：

```bash
cd /root/jideshi_hibi_backend
vi .env
```

2. 新增或修改：

```bash
ADMIN_DASH_PASSWORD=你的新后台密码
```

3. 重启后端服务：

```bash
systemctl restart jideshi-hibi-api
systemctl is-active jideshi-hibi-api
```

4. 用新密码访问 `http://121.41.6.21:7861/admin/login` 验证登录。

---

## 3. 技术原理

## 3.1 鉴权机制（后台）

- 后端在 `api_only_app.py` 内实现后台登录态。
- 登录成功后生成随机 session token（内存态），写入 HttpOnly Cookie：`hibi_admin_session`。
- Cookie 有效期：12 小时。
- 后台接口会校验此 Cookie；不通过则返回 401。

说明：

- 该机制为轻量方案，适合当前单实例部署。
- 若后续做多实例/负载均衡，建议迁移到 Redis/数据库会话存储。

## 3.2 客户数据聚合

- 用户基础信息来自 `users` 表（`hibi_auth_sync.py`）
- 订阅与订单来自 `payment_orders` 表（`hibi_payment.py`）
- 后台列表查询时聚合：
  - `SUM(已支付订单 amount_cents)` -> 总消费
  - `get_user_entitlements(user_id)` -> 各套餐状态、剩余时间

## 3.3 套餐状态覆盖（管理员操作）

- 新增表：`admin_plan_overrides`
- 字段：`user_id + plan_id + forced_status + forced_valid_until + forced_grace_until + note`
- 覆盖优先级：管理员覆盖 > 自动计算状态
- 可通过“清除覆盖”恢复系统自动计算状态

---

## 4. 接口与页面

### 4.1 页面路由

- `GET /admin/login`：后台登录页
- `POST /admin/login`：提交密码并写入会话 Cookie
- `POST /admin/logout`：退出后台
- `GET /admin/customers`：管理后台（单页内 **选项卡**：**系统配置** — 图形认证、隐私条款、项目助理 Prompt；**用户管理** — 客户列表与套餐操作；进入「用户管理」时才请求并展示客户数据。地址栏可加 `#users` 直达用户管理）

### 4.2 后台 API

- `GET /api/admin/customers`
  - 查询客户列表及订阅聚合信息
- `POST /api/admin/customers/{user_id}/plans/{plan_id}/set_status`
  - 设置单套餐状态和剩余天数
- `POST /api/admin/customers/{user_id}/plans/{plan_id}/clear_override`
  - 清除单套餐管理员覆盖
- `GET /api/admin/legal/privacy_terms`
  - 读取后台当前隐私条款（用于后台编辑器回填）
- `POST /api/admin/legal/privacy_terms`
  - 更新隐私条款文本（保存后 App 端即时可读）
- `GET /api/admin/settings/abp_system_prompt`
  - 读取当前项目助理 System Prompt（后台编辑区回填；无库内记录时返回代码默认全文）
- `POST /api/admin/settings/abp_system_prompt`
  - 保存项目助理 System Prompt（`body: { "content": "..." }`）；传空字符串 `content` 可清除库内记录，运行时回退到代码默认（见下「优先级」）
- `GET /api/admin/settings/graph_captcha_enabled`
  - 读取是否开启登录/注册图形认证（`{ "enabled": true|false }`）
- `POST /api/admin/settings/graph_captcha_enabled`
  - 保存开关（`body: { "enabled": true|false }`），写入 SQLite `legal_documents` 中 `graph_captcha_enabled`（`true`/`false` 字符串）
- `GET /api/admin/settings/app_update_manifest`
  - 读取升级推送 JSON（后台「升级推送地址」表单回填）
- `POST /api/admin/settings/app_update_manifest`
  - 保存升级推送（`body`: `{ "latest_version", "release_notes", "urls": { "windows","ios","android","linux" } }`），写入 `legal_documents` 的 `app_update_manifest`
- `POST /api/admin/uploads/app_package`
  - 后台上传安装包文件（`multipart/form-data`：`platform` + `file`），返回 `download_url` 并可回填到升级推送 URL

**公开接口（无需登录）**

- `GET /api/app/update_manifest` — App 启动后检查更新，返回 `latest_version`、`release_notes`、`urls`、`updated_at`。
- `GET /api/app/packages/{filename}` — 后台上传的安装包公开下载地址（由后台自动生成并写入升级推送 URL）

**图形认证与 App 行为**

- 部署、环境变量、自检 URL 与排障清单见：[阿里云图形认证部署与排障.md](./阿里云图形认证部署与排障.md)（当前示例基址 `http://121.41.6.21:7861`）；控制台方案名称 / 方案编码 / 各端 appId 见：[阿里图片认证.md](./阿里图片认证.md)。
- 默认**开启**（新库首次启动会写入 `true`）。
- **关闭**时：`/api/auth/captcha_config` 返回的 `configured` 为 **false**（即使服务端已配置阿里云 appId），登录、注册接口不再校验 `captcha_challenge_id` / 图形参数；创建挑战、校验接口会返回 403。
- **开启**时：与原先一致，需完成图形验证后方可登录/注册。

**项目助理 Prompt 运行时优先级（与后台的关系）**

1. 若 ECS `.env` 配置了 **`HIBI_ABP_SYSTEM_PROMPT_FILE`** 且该**文件存在且内容非空**：**始终优先使用该文件**，后台里保存的内容不生效。
2. 否则使用 **SQLite `legal_documents` 表** 中 `abp_system_prompt` 一条（即后台保存的内容）。
3. 若库中无记录或已清除：使用 **`hibi_abp_tools.py` 内置 `DEFAULT_SYSTEM_PROMPT`**。

首次部署或新库会在初始化时自动插入与代码一致的默认全文，因此打开 `/admin/customers` 时编辑区**已有默认内容**，可直接修改后保存。

### 4.3 App 查阅接口

- `GET /api/legal/privacy_terms`
  - 公开读取隐私条款（无需登录）
  - App 设置页会通过该接口展示隐私条款内容

---

## 5. IP 定位数据来源

- 注册/登录时，后端记录客户端 IP（优先 `X-Forwarded-For` / `X-Real-IP`）。
- 定位调用公共 IP 地理接口进行解析并缓存。
- 历史老用户可能没有创建 IP/定位，属于正常现象，后续登录会补齐最近登录信息。

---

## 6. 风险与后续建议

- 默认密码仅用于初始阶段，必须尽快替换。
- 当前后台会话是内存态，服务重启后需重新登录。
- 建议后续新增：
  - 操作审计日志（谁改了哪个用户、何时改）
  - 角色权限（只读运营 / 超级管理员）
  - 管理后台独立域名与 HTTPS 强制

