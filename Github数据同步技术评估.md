# GitHub 仓库作为用户数据存储 · 技术评估报告

| 项 | 内容 |
|----|------|
| 状态 | **仅评估，暂不实施** |
| 日期 | 2026-08-11 |
| 结论摘要 | **可行，定位为「用户自持云盘 + 准实时多端同步」；不适合作为真·实时 Git 协作引擎** |
| 相关现状 | 自建云同步已停用（GitHub 身份硬禁用）；局域网 LWW 仍可用；GitHub 仅 `read:user` + Star 门禁 |

> 本文档供产品/技术决策归档。不包含实现计划排期；落地前需另开设计与风险验收。

---

## 1. 背景与动机

### 1.1 业务诉求

1. App 已具备 GitHub 登录 / 授权 / Star 门禁能力。
2. 希望用 **用户自己的 GitHub 仓库**（私有或公开，由用户选择）存放用户数据。
3. 用户可先在 GitHub 建仓，再以 **token 权限** 交给 App，并与 App 用户绑定；支持 **一个 GitHub 账户绑定多个 token/仓库**。
4. 通过 git / GitHub API 在 Windows / Android / iOS 间同步：日程、思维导图、助理、设置等；一端更新后另一端拉取或准实时同步。
5. 必须避免 **旧数据覆盖新数据**；希望借鉴成熟冲突处理经验。
6. 项目曾跑通自建服务器同步，现已停用，可作为合并策略与调度节奏的参考。

### 1.2 评估范围

| 纳入 | 不纳入（本期评估不承诺） |
|------|-------------------------|
| 技术可行性、架构选型、风险、与现有代码契合度 | 具体开发排期、UI 原型、OAuth 应用审核材料定稿 |
| 权限模型、同步节奏、冲突策略建议 | 写代码、改 scope、上线开关 |
| 与旧云同步 / 局域网同步的关系 | 运营与客服话术全文 |

---

## 2. 现状基线（与方案直接相关）

### 2.1 GitHub 身份层

| 项 | 现状 |
|----|------|
| 流程 | OAuth App + Device Flow（无 Client Secret） |
| Scope | **仅 `read:user`**（读资料 + 查 starred） |
| Token 存储 | `flutter_secure_storage` + SharedPreferences 备份 |
| 用户目录键 | `gh_<numericId>`；V4.0.1 常绕过门禁走 `local/` |
| 数据仓读写 | **无**；Star 仓 `YYOZZE/HIBI` 仅门禁/发版，不是用户数据仓 |

### 2.2 本地数据形态（适合进仓）

根路径：`Documents/hibi_accounts/<账号key>/`（见 [`本地账户数据结构说明.md`](本地账户数据结构说明.md)）。

| 数据 | 本地形态 | 是否适合进 GitHub 仓 |
|------|----------|----------------------|
| 思维 / 白板 | `hibi_mind_nodes.json` | 适合（结构化 JSON） |
| 日程 | `hibi_schedule_events.json` | 适合 |
| 助理 agents / 消息 | `hibi_assistant/*.json` | 适合（注意体积与隐私） |
| 聊天附件 `chat_files/` | 文件树 | **默认不宜**频繁进仓 |
| 主题 / 传输设置等 | SharedPreferences | 可选；非核心 |
| 智能体 API Key / ASR 等密钥 | SharedPreferences | **禁止**写入仓库 |

### 2.3 已有同步与合并资产

| 能力 | 状态 | 可复用点 |
|------|------|----------|
| 自建 `UserSyncService` pull/push | 对 GitHub 身份 **硬禁用** | 调度节奏、先拉后推、防空包 |
| `SyncMerge` | 仍在 | 思维 `id+updatedAt` LWW；日程/助理合并规则 |
| 局域网同步 | 可用 | 同网高速 LWW；可与云盘方案并存 |
| 白板近实时（约 300ms–2s） | 依赖自建后端 | **不可原样套到 GitHub API** |

关键说明文档：

- [`思维节点与云端同步说明.md`](思维节点与云端同步说明.md)
- [`backend_jideshi_hibi_app/AUTH_SYNC_DEPLOY.md`](backend_jideshi_hibi_app/AUTH_SYNC_DEPLOY.md)
- [`lib/features/auth/AUTH_GITHUB.md`](lib/features/auth/AUTH_GITHUB.md)

---

## 3. 方案概述（评估对象）

### 3.1 目标架构（概念）

```text
┌─────────────┐     防抖写盘      ┌──────────────────────┐
│ Windows App │ ───────────────► │ 本地 hibi_accounts/  │
└──────┬──────┘                  └──────────┬───────────┘
       │ pull / merge / push                │
       ▼                                    │ 同结构 JSON
┌─────────────────┐                         │
│ 用户 GitHub 仓  │ ◄───────────────────────┘
│ (私有优先)      │
└────────┬────────┘
         │ pull / merge
         ▼
┌─────────────┐
│ 手机 / iPad │
└─────────────┘
```

**原则：GitHub 只做版本化对象存储；冲突语义在 App 内用 `SyncMerge`/LWW 解决，不依赖 git 文本合并。**

### 3.2 推荐技术路径（若未来实施）

| 优先级 | 路径 | 说明 |
|--------|------|------|
| **首选** | GitHub Contents API / Git Data API | 跨端稳定；无需在移动端嵌入完整 git |
| 可选 MVP | 用户自建仓 + Fine-grained PAT（仅该仓 Contents） | 权限面可控；UX 偏重 |
| 正式版 | OAuth 扩权后自动创建私有仓 | 体验好；权限与合规成本更高 |
| **不推荐作主路径** | App 内嵌 libgit2 / git 二进制 | 体积、凭证、半提交、移动端维护成本高 |
| 轻量备选 | Gist | 仅适合小备份；多仓/权限/产品叙事弱 |

### 3.3 绑定模型（产品概念）

1. App 用户（`local` 或 `gh_*`）绑定 0..N 个「同步目标」。
2. 每个目标：`owner/repo` + 凭证（PAT 或未来 OAuth token）+ 可见性（private/public，用户自选）。
3. 同一时刻一个 **active** 目标用于自动同步；其余可作切换/归档。
4. Star 用的官方仓与用户数据仓 **必须分离**。

---

## 4. 可行性评估

### 4.1 总体结论

| 维度 | 评级 | 说明 |
|------|------|------|
| 技术可行性 | **高** | 数据已是整文件 JSON；合并策略已有；GitHub API 成熟 |
| 产品契合度 | **高** | 已有 GitHub 登录叙事；数据落在用户名下，运维负担低 |
| 「真·实时」 | **低** | 限速、无字段级 CRDT、不适合白板 500ms 级互刷 |
| 「准实时 / 多端一致」 | **中高** | 启动/回前台 pull + 变更防抖 push 可达到可用体验 |
| 实施成本 | **中** | 新适配层 + 权限 UX + 冲突/错误态；非开关级改造 |
| 风险可控性 | **中** | 关键风险见第 6 节；多数可用策略与默认策略压降 |

**一句话：适合替代停用的自建云盘，做个人多端同步；不适合承诺协同编辑级实时。**

### 4.2 与旧服务器同步对比

| | 自建服务器（已停用） | GitHub 仓方案 |
|--|--|--|
| 运维 | 需养 API / DB / 机器 | 几乎零自建后端 |
| 鉴权 | 自建 Bearer + 订阅逻辑 | 用户 GitHub 权限 / PAT |
| 延迟与配额 | 可控 | 受 GitHub 限速与可用性约束 |
| 冲突 | 服务端曾「后写覆盖」+ 客户端 merge | **必须以客户端 merge 为主** |
| GitHub 登录用户 | 当前硬禁用云同步 | 反而最适合接此链路 |
| 附件 | 可单独策略 | 大文件勿频繁 commit |
| 故障域 | 你们的服务挂则全员受影响 | GitHub 挂则同步暂停；本地仍可用 |

### 4.3 防旧盖新（成熟做法，可复用）

不必发明新理论；与现有云同步/局域网经验一致：

1. **实体级时间戳**：每条记录 `updatedAt`（或设备 revision）。
2. **按 id LWW**：思维节点、日程事件等同 id 取较新者（已有 `SyncMerge`）。
3. **禁止整包盲目覆盖**：永远 pull → merge → 写本地 → push。
4. **乐观并发**：Contents API 写文件携带已知 `sha`；冲突则重新 pull → merge → 再写。
5. **先拉后推 / 取消待推送**：回前台时取消未执行的旧 push，避免旧快照盖住远端新数据（旧调度器已有此思路）。
6. **禁止空包 / 登出空状态 push**。
7. **删除用 tombstone**（可选增强）：避免离线旧副本「复活」已删条目。
8. **Git merge 不做业务语义**：仓内文件冲突交给 App 合并，而不是依赖自动文本 merge。

---

## 5. 「实时」边界（必须写进产品预期）

| 场景 | 建议承诺 | 不建议承诺 |
|------|----------|------------|
| 日程 / 设置 | 变更后数十秒内可同步；启动必拉 | 毫秒级双端同屏编辑 |
| 思维白板 | 防抖数秒～数十秒上传；打开页/回前台拉取 | 500ms 级对端光标/拖拽镜像 |
| 助理消息 | 会话结束后或防抖同步 | 流式生成过程中每 token 上仓 |
| 弱网 / 限速 | 本地优先，队列重试，展示「上次成功同步时间」 | 「始终实时一致」 |

建议对内对外口径：**「多端准实时同步 + 用户自持备份」**，而非「实时 Git 协作」。

---

## 6. 风险登记

> 评级：影响 × 可能性的综合判断。缓解列为「若未来实施」的默认约束，**当前不实施则风险不触发**。

### 6.1 技术与数据完整性

| ID | 风险 | 等级 | 说明 | 缓解方向 |
|----|------|------|------|----------|
| R1 | 旧快照覆盖新数据 | **高** | 多端离线编辑、待推送队列乱序 | 强制 merge；sha 乐观锁；取消过期 push；单测覆盖冲突用例 |
| R2 | 高频 push 触发限速 / 失败风暴 | **高** | 白板若沿用 300ms 防抖会打爆 API | 分类防抖（白板更长）；批量写；指数退避；熔断 |
| R3 | 附件与大 JSON 导致仓膨胀 | **中** | `chat_files/`、超长助理历史 | 默认不同步附件；历史可截断或分包；单文件体积告警 |
| R4 | 移动端嵌 git 不稳定 | **中** | 若选 libgit2 路径 | **评估结论：主路径用 API，不嵌完整 git** |
| R5 | 部分合并规则不一致 | **中** | 云端日程曾「本地优先」、LAN 用 mtime | 统一一份「GitHub 同步合并规范」并文档化 |
| R6 | tombstone 缺失导致删除复活 | **中** | 双端离线一方删除 | Phase 2 引入删除标记或版本向量 |

### 6.2 安全与隐私

| ID | 风险 | 等级 | 说明 | 缓解方向 |
|----|------|------|------|----------|
| R7 | 密钥进仓 | **高** | API Key、token、ASR 等进公开/私有仓被扫 | **硬排除** prefs 密钥；提交前校验扫描 |
| R8 | 用户选公开仓泄露日程/对话 | **高** | 产品允许 public 时 | 默认强制 private；公开仓二次确认 + 敏感字段警告 |
| R9 | PAT 权限过大 / 泄露 | **高** | classic PAT 可动全部仓 | 仅推荐 fine-grained；最小 Contents；secure storage；支持吊销指引 |
| R10 | OAuth 扩 `repo` 后权限面过大 | **中** | 正式版自动建仓需要 | 优先 fine-grained；隐私政策说明；可只授单一仓 |
| R11 | Token 双写在 prefs 的历史包袱 | **低～中** | 现有会话快照含 accessToken | 数据仓凭证与登录 token 隔离；逐步收紧 prefs |

### 6.3 产品与体验

| ID | 风险 | 等级 | 说明 | 缓解方向 |
|----|------|------|------|----------|
| R12 | 绑仓/填 PAT 门槛高 | **中** | 非开发用户难完成 | MVP 可接受；正式版自动建仓；提供图文引导 |
| R13 | 「实时」预期落差 | **中** | 用户拿来对比网盘/协同白板 | 文案降级为准实时；展示同步状态 |
| R14 | `local/` 与 `gh_*` 目录割裂 | **中** | V4 默认 local，仓绑在谁身上易混乱 | 产品明确：local 可绑仓，或强制 GitHub 登录后同步 |
| R15 | 多仓绑定认知负担 | **低～中** | 一账户多目标 | 默认单 active；高级设置才多仓 |
| R16 | 与局域网同步同时开冲突 | **低～中** | 两套源同时写 | 文档约定优先级；或互斥自动云同步 |

### 6.4 平台与合规依赖

| ID | 风险 | 等级 | 说明 | 缓解方向 |
|----|------|------|------|----------|
| R17 | GitHub 宕机 / 区域网络不可达 | **中** | 同步中断 | 本地永远可用；队列重试；不阻塞主功能 |
| R18 | API / ToS / 限速策略变更 | **中** | 长期依赖第三方 | 抽象 `RemoteSyncStore` 接口，便于换后端 |
| R19 | 企业/教育网络禁用 GitHub | **低～中** | 部分用户无法用 | 保留本地 + LAN；可选自建后端作为企业通道 |
| R20 | 审计与取证：数据在用户仓 | **低**（偏正面） | 你们看不到用户明文 | 隐私叙事优势；客服需改为「查本地/用户仓」 |

### 6.5 工程与维护

| ID | 风险 | 等级 | 说明 | 缓解方向 |
|----|------|------|------|----------|
| R21 | 与停用云同步代码纠缠 | **中** | 误复用 `UserSyncScheduler` 高频逻辑或 GitHub token 当后端 Bearer | **新适配层**；禁止把 `gho_` 当自建 API token |
| R22 | 测试矩阵大 | **中** | Win/Android/iOS × 冲突 × 限速 × 换绑 | 先做合并单测 + 契约测试；真机抽检主路径 |
| R23 | 提交历史噪音 | **低** | 每次小改一个 commit | 可 squash 式单文件更新；或定期 GC 说明（GitHub 侧能力有限） |

---

## 7. 明确不建议的做法（评估红线）

1. 把现有白板 **300ms / 500ms** 级 pull-push 原样接到 GitHub。
2. 默认同步 **公开仓**，或不经确认同步助理全文到 public。
3. 将 **API Key / OAuth token / PAT** 写入仓库文件。
4. 以 **git 文本 merge** 替代业务 LWW。
5. 在移动端 **强依赖完整 git 可执行文件** 作为唯一同步引擎。
6. 把用户数据写进 **`YYOZZE/HIBI` Star 仓库**。
7. 未抽象存储接口就写死 GitHub，导致日后无法切回自建或其它对象存储。

---

## 8. 若未来实施的建议阶段（仅规划参考，非开工令）

| 阶段 | 内容 | 目标 |
|------|------|------|
| Phase 0 | 本评估评审；统一合并规范；产品口径（准实时） | 决策 Go / No-Go |
| Phase 1 | 手动绑私有仓 + PAT；同步 mind/schedule/assistant JSON；启动与回前台 pull；变更防抖 push；复用 `SyncMerge` | 可演示的多端一致 |
| Phase 2 | 同步状态 UI、冲突极少见提示、多仓切换、排除附件与密钥的校验 | 可给内测用户 |
| Phase 3 | OAuth/自动建仓；与 LAN 优先级策略；可选附件方案；`RemoteSyncStore` 可替换 | 正式产品化 |

**当前决议倾向：停在 Phase 0，先不做。**

---

## 9. 决策建议

| 问题 | 建议答案 |
|------|----------|
| 要不要做？ | **技术上值得保留为候选方案**；在自建同步停用、又已有 GitHub 登录的前提下，性价比高。 |
| 现在做不做？ | **先不做**。先固化风险接受度、产品口径与合并规范，再开设计。 |
| 定位是什么？ | 用户自持的 **云端备份 + 准实时多端同步**，不是实时协作引擎。 |
| 与 LAN 关系？ | 可并存：LAN 负责同网高速；GitHub 负责跨网备份。 |
| 最小可行技术？ | Contents API + 现有 `SyncMerge` + 防抖调度；不要先嵌 git。 |

---

## 10. 参考与关联文档

- [`本地账户数据结构说明.md`](本地账户数据结构说明.md)
- [`思维节点与云端同步说明.md`](思维节点与云端同步说明.md)
- [`backend_jideshi_hibi_app/AUTH_SYNC_DEPLOY.md`](backend_jideshi_hibi_app/AUTH_SYNC_DEPLOY.md)
- [`lib/features/auth/AUTH_GITHUB.md`](lib/features/auth/AUTH_GITHUB.md)
- [`lib/features/auth/AUTH_GITHUB_SEALED.md`](lib/features/auth/AUTH_GITHUB_SEALED.md)

### 外部可借鉴方向（概念层）

- 乐观并发控制（写前校验资源版本 / `sha`）
- Last-Write-Wins 与 tombstone（分布式笔记/日程类 App 常用）
- 「本地优先 + 后台同步队列」（offline-first）
- GitHub Contents API 作为应用数据后端的既有社区实践（需自建合并层）

---

## 11. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-11 | 初稿：可行性、风险登记、红线与分阶段建议；明确 **暂不实施** |
