# 服务增值支付功能说明

## 一、功能概述

- **前端**：个人中心 → 服务增值页，五个套餐卡片，点击「订阅/续订」先选择支付方式（支付宝/微信），再创建订单并跳转支付链接（未配置数捷时跳转本服说明页）。
- **后端**：订单表、创建订单、支付回调验签更新、订单查询；可选对接数捷支付（需配置环境变量）。
- **数捷文档**：<https://www.shujiepay.com/doc/index.html>（请求 form-urlencoded，返回 JSON，UTF-8，签名 SHA256WithRSA；V2 使用商户私钥 + 平台公钥、timestamp）。

---

## 二、套餐与金额（后端 PLANS）

| plan_id       | 展示名             | 金额说明   | 金额（分） |
|---------------|--------------------|------------|------------|
| data_service  | 数据服务           | 1元/季     | 100        |
| assistant_service | 助理服务        | 2元/季     | 200        |
| theme_service | 主题服务           | 1元/季     | 100        |
| pro_max       | HIBI-PRO-MAX       | 12元/年    | 1200       |
| pro_max_1tb   | HIBI-PRO-MAX-1TB   | 100元/月   | 10000      |

---

## 三、会员逻辑

- **前三个（数据/助理/主题）**：可**单独**订阅一个或多个，互不依赖。
- **后两个（HIBI-PRO-MAX / HIBI-PRO-MAX-1TB）**：订阅后**包含**前三个服务；同时支持与基础服务、彼此之间**单独续订并独立累计时长**。
- **有效期**：支付成功后按套餐写入 `valid_until`。季=90 天、年=365 天、月=30 天；仅对 `valid_until >= 当前时间` 的订单计为生效权益。
- **权益接口**：`GET /api/payment/my_entitlements` 返回 `pro`（当前最高 PRO 档）、`basic_plans`（生效的基础服务 ID 列表）、`pro_valid_until`（PRO 到期时间戳，可选）。

---

## 跑通清单与还需提供的信息

### 您已完成的

- 已在服务器 `.env` 中配置：**平台公钥**（`SHUJIEPAY_PLATFORM_PUBLIC_KEY`）、**商户私钥**（`SHUJIEPAY_MCH_PRIVATE_KEY`）。

### 代码侧（已就绪）

- 下单：用商户私钥对参数签名，POST 到数捷；回调：用平台公钥验签后更新订单并写入 `valid_until`。
- 已修复回调验签中 try 块缩进问题。
- 新增 **配置自检**：`GET /api/payment/config_status` 返回各环境变量是否已配置（仅布尔，不返回密钥），`ready: true` 表示可发起数捷下单。

### 关键配置项（在服务器 .env 中）

| 变量 | 说明 | 从哪里获取 |
|------|------|-------------|
| **SHUJIEPAY_MCH_ID** | 商户号 | 数捷商户后台或对接文档 |
| **SHUJIEPAY_SUBMIT_URL** | 数捷下单接口 URL | SDK2 推荐 `https://www.shujiepay.com/api/pay/submit`（或你的数捷站点域名 + `/api/pay/submit`） |
| **SHUJIEPAY_NOTIFY_URL** | 异步通知地址 | 您自己的公网地址，且需在数捷商户后台填写一致，如 `http://121.41.6.21:7861/api/payment/notify`；若数捷要求 HTTPS，需域名 + HTTPS 或反向代理 |

#### 这三个在哪里找、怎么填

**1. 数捷商户号（SHUJIEPAY_MCH_ID）**

- **在哪里找**：商户后台 → **API 信息** 页，字段名 **商户ID**。
- **怎么填**：截图里为 **1575**。在服务器 `.env` 填：`SHUJIEPAY_MCH_ID=1575`（若您后台显示不同则以实际为准）。

**2. 数捷下单接口的完整 URL（SHUJIEPAY_SUBMIT_URL）**

- **在哪里找**：商户后台 → **API 信息** 页，字段名 **接口地址**（通常是站点根地址）。
- **怎么填**：当前项目已兼容 SDK2 与 V2 两种下单方式，优先推荐 SDK2 页面跳转地址：  
  `SHUJIEPAY_SUBMIT_URL=https://你的数捷域名/api/pay/submit`  
  例如官方站点：`https://www.shujiepay.com/api/pay/submit`。  
  若你的服务商只提供 V2 create 接口，也可填其完整 V2 下单 URL。

**3. 异步通知地址（SHUJIEPAY_NOTIFY_URL）**

- **含义**：用户支付成功后，数捷会向这个地址 POST 请求，我们后端验签后把订单改为已支付。我们**每次下单请求**里都会把 `notify_url` 传给数捷，数捷按该地址回调。
- **后台没有单独配置项时**：若 API 信息页没有「异步通知地址」输入框，说明数捷以**请求里传的 notify_url 为准**，无需在后台再填。您只需在**咱们服务器 `.env`** 里配置即可。
- **怎么填**：用您后端对外的访问地址 + `/api/payment/notify`。例如：  
  `SHUJIEPAY_NOTIFY_URL=http://121.41.6.21:7861/api/payment/notify`  
  若用域名且为 HTTPS：`SHUJIEPAY_NOTIFY_URL=https://您的域名/api/payment/notify`  
  确保该 URL 能从公网访问（数捷服务器能 POST 到）。

配置后执行 `docker restart jideshi_hibi_api`（或 `bash server_redeploy.sh`）使生效。

### 可选

- `SHUJIEPAY_RETURN_URL`：支付完成同步跳转。
- `SHUJIEPAY_DEFAULT_TYPE`：默认支付渠道（如 `alipay` / `wxpay` / `qqpay` / `bank`）。
- `HIBI_BASE_URL`：本服 base URL（默认 `http://121.41.6.21:7861`）。

### 自检方式

浏览器或 curl 访问：**http://121.41.6.21:7861/api/payment/config_status**  
返回示例：`{"SHUJIEPAY_SUBMIT_URL": true, "SHUJIEPAY_NOTIFY_URL": true, "SHUJIEPAY_MCH_ID": true, "SHUJIEPAY_MCH_PRIVATE_KEY": true, "SHUJIEPAY_PLATFORM_PUBLIC_KEY": true, "HIBI_BASE_URL": true, "ready": true}`  
当 `ready` 为 `true` 时，应用内点击「订阅/续订」会跳转数捷支付页；否则跳转本服说明页。

---

## 四、后端

### 4.1 文件与库

- **hibi_payment.py**：订单表、创建订单、数捷下单/回调验签、订单查询。
- **api_only_app.py**：注册 `init_payment_db()`，提供支付相关路由。
- **requirements.txt**：含 `cryptography`（RSA 签名/验签）。

### 4.2 订单表 `payment_orders`

- `id`（主键）、`user_id`、`plan_id`、`amount_cents`、`subject`、`status`（pending/paid）、`external_order_id`、`created_at`、`paid_at`、`notify_raw`、**`valid_until`**（付费后有效期截止时间戳；支付成功时按套餐写入：季 90 天、年 365 天、月 30 天）。
- 与用户库共用 `hibi_users.db`（路径由 `HIBI_DATA_DIR` 或同目录决定）。

### 4.3 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET  | `/api/payment/config_status` | 数捷配置自检，返回各环境变量是否已配置及 `ready`。 |
| POST | `/api/payment/create_order` | 需 Bearer；body `{"plan_id":"xxx","pay_type":"alipay|wxpay"}`（`pay_type` 可选）；返回 `order_id`、`pay_url`、`amount`、`subject`。 |
| GET  | `/api/payment/order/{order_id}` | 需 Bearer；查询订单详情。 |
| GET  | `/api/payment/my_entitlements` | 需 Bearer；当前会员权益：`pro`、`basic_plans`、`pro_valid_until`。 |
| POST | `/api/payment/notify` | 数捷异步回调；form body，验签后更新订单为已支付并写入 `valid_until`。 |
| GET  | `/api/payment/pay_page?order_id=` | 未配置数捷时的说明页。 |

### 4.4 数捷支付密钥与环境变量

数捷文档：<https://www.shujiepay.com/doc/index.html>。请求用 **商户私钥** 签名，回调用 **平台公钥** 验签。

#### 密钥角色

| 密钥 | 用途 | 配置位置 |
|------|------|----------|
| **商户公钥** | 在数捷商户后台上传，供平台验证商户请求 | 数捷商户后台（不上传到我方服务器） |
| **商户私钥** | 本服请求数捷下单时用于签名 | **仅**配置在服务器 `.env`，见下表 |
| **平台公钥** | 数捷异步回调时验签 | 服务器 `.env`，从数捷商户后台获取 |

#### 商户私钥配置在哪里

- **配置位置**：部署后端的服务器上的 **环境变量**，不要写在代码里、不要提交到 Git。
- **ECS 示例**：在 `/root/jideshi_hibi_backend/.env` 中增加或编辑一行：
  ```bash
  SHUJIEPAY_MCH_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----
  （你的私钥 PEM 全文，可多行）
  -----END PRIVATE KEY-----"
  ```
- **变量名**：`SHUJIEPAY_MCH_PRIVATE_KEY`。值为 PEM 格式私钥全文（含 `-----BEGIN PRIVATE KEY-----` 与 `-----END PRIVATE KEY-----`）；若用双引号包裹，可保留换行。
- **安全**：私钥仅存在于服务器 `.env`，同步脚本不会覆盖该文件；切勿把 `.env` 或私钥内容提交到仓库。

#### 当前使用的商户公钥（供在数捷后台配置时对照）

在数捷商户后台上传的 **商户公钥** 对应如下（与下方私钥为一对，私钥只放在本服 `.env`）：

```

```

若数捷要求 PEM 格式，可写成（同一公钥）：

```
-----BEGIN PUBLIC KEY-----

ZsbswIDAQAB
-----END PUBLIC KEY-----
```

#### 当前使用的平台公钥（配置到服务器 SHUJIEPAY_PLATFORM_PUBLIC_KEY）

用于回调验签的 **平台公钥**，配置在服务器 `.env` 的 `SHUJIEPAY_PLATFORM_PUBLIC_KEY`：

**Base64（一行）：**
```

```

**PEM 格式（写入 .env 时可用，注意换行）：**
```
-----BEGIN PUBLIC KEY-----

YQzwIDAQAB
-----END PUBLIC KEY-----
```

#### 环境变量一览（.env）

| 变量 | 说明 |
|------|------|
| `SHUJIEPAY_SUBMIT_URL` | 数捷下单接口 URL（推荐 SDK2：`/api/pay/submit`） |
| `SHUJIEPAY_NOTIFY_URL` | 异步通知地址，需公网可访问（如 `https://你的域名/api/payment/notify`） |
| `SHUJIEPAY_RETURN_URL` | 支付完成同步跳转（可选） |
| `SHUJIEPAY_DEFAULT_TYPE` | 默认支付渠道（可选，默认 `alipay`） |
| `SHUJIEPAY_MCH_ID` | 商户号 |
| `SHUJIEPAY_MCH_PRIVATE_KEY` | **商户私钥** PEM 全文，配置在服务器 .env，见上文 |
| `SHUJIEPAY_PLATFORM_PUBLIC_KEY` | **平台公钥** PEM 全文，用于回调验签，见上文「当前使用的平台公钥」 |
| `HIBI_BASE_URL` | 本服 base URL（如 `http://121.41.6.21:7861`），用于拼 `pay_page` 等 |

未配置 `SHUJIEPAY_SUBMIT_URL` 或 `SHUJIEPAY_MCH_PRIVATE_KEY` 时，创建订单后 `pay_url` 为本服 `pay_page`，不发起数捷请求。

---

## 五、Flutter 端

### 5.1 相关文件

- **lib/features/profile/value_added_page.dart**：套餐列表（StatefulWidget）；进入页时拉取 `GET /api/payment/my_entitlements`，展示「当前权益」摘要；点击「订阅/续订」会先弹出支付方式选择，再调用下单。
- **lib/features/profile/payment_service.dart**：`PaymentService.createOrderAndGetPayUrl(planId, token, payType: ...)`、`PaymentService.fetchMyEntitlements(token)`；模型 `UserEntitlements`（pro、basicPlans、proValidUntil）、`CreateOrderResult`。

### 5.2 套餐卡按钮逻辑（当前）

- **全部套餐都支持单独续费**：已订阅时显示「续订」，未订阅时显示「订阅」。
- **基础服务被 PRO 包含时**：状态显示「已包含于 PRO（可单独续订并独立计时）」；按钮仍可点击「续订」。
- **PRO-MAX 与 PRO-MAX-1TB**：两档都可独立续订；不会因已开更高档而禁用按钮。

### 5.3 流程（onValueAdd）

1. 未登录 → SnackBar「请先登录后再购买」。
2. 未配置 API →「未配置支付服务，请稍后再试」。
3. 弹窗让用户选择支付方式（支付宝 / 微信），并调用 `PaymentService.createOrderAndGetPayUrl(plan.planId, user.token, payType: ...)`；失败 →「创建订单失败，请稍后再试」。
4. 成功后客户端会按平台自动尝试拉起支付页（`externalApplication` → 移动端 `inAppBrowserView` → `platformDefault`）。  
   - Windows：优先调用系统默认浏览器；  
   - Android/iOS：优先外部浏览器，失败时回退到应用内浏览器。  
5. 支付完成后回到应用，点击「我已支付，查看结果」查询订单状态并刷新订阅权益。

---

## 六、部署与同步

### 6.1 同步到 ECS

在项目根目录执行：

```powershell
.\backend_jideshi_hibi_app\sync_to_server.ps1
```

脚本会同步 `api_only_app.py`、`hibi_auth_sync.py`、**hibi_payment.py**、`requirements.txt`、Dockerfile、.env.example、说明文档等（不覆盖服务器已有 .env）。

### 6.2 服务器重建与重启

```bash
ssh root@121.41.6.21
cd /root/jideshi_hibi_backend && docker build -t jideshi_hibi_api . && docker restart jideshi_hibi_api
```

或使用仓库内的 `server_redeploy.sh`（若已上传）。

---

## 七、付款订阅与数捷对接要点

- **下单**：本服用 `SHUJIEPAY_MCH_PRIVATE_KEY`（商户私钥）对请求参数做 SHA256WithRSA 签名，POST form-urlencoded 到数捷下单接口；数捷用商户在后台上传的**商户公钥**验签。
- **回调**：数捷异步通知 `GET/POST /api/payment/notify`（SDK2 示例为 `GET`），本服用 `SHUJIEPAY_PLATFORM_PUBLIC_KEY`（平台公钥）验签后更新订单为已支付并写入 `valid_until`。
- **商户公钥**：在数捷商户后台上传（本文档已记录当前使用的公钥，便于对照）。**商户私钥**仅配置在服务器 `.env`，变量名 `SHUJIEPAY_MCH_PRIVATE_KEY`，详见 4.4 节。

## 八、注意事项

- 数捷文档：<https://www.shujiepay.com/doc/index.html>。若文档有具体下单 URL、参数名、回调参数字段，请按最新文档核对并可能调整 `hibi_payment` 中参数名与签名顺序。
- 回调验签已排除空值；若数捷要求「按字典序拼接参与签名参数」等，需按文档再调整。
- 支付回调地址必须为公网可访问（建议 HTTPS），且需在数捷商户后台配置为 `SHUJIEPAY_NOTIFY_URL` 一致。
- 若浏览器打开的是 `api/payment/pay_page?order_id=...` 说明当前订单未拿到数捷真实收银台链接。优先检查：
  1) `SHUJIEPAY_SUBMIT_URL` 是否为 SDK2 地址（如 `https://www.shujiepay.com/api/pay/submit`）；  
  2) `SHUJIEPAY_NOTIFY_URL` 是否指向当前在线后端（例如 `http://121.41.6.21:7861/api/payment/notify`）；  
  3) 修改 `.env` 后是否已重启 `jideshi-hibi-api`。

### 8.1 收银台「渠道受理失败 690001 · appid 配置」（易宝 / 一码付）

- **现象**：用户在微信内或浏览器打开数捷跳转后的收银台（常见域名如 `jspay.plutuspay.com`，页面标题「一码付」）时，弹窗提示：  
  **`渠道受理失败:690001:请进行appid配置，如已配置请联系易宝运营处理`**。
- **与本项目代码的关系（结论：不是 Flutter / 本仓库里少配 appid）**：
  - **Flutter 端**未集成易宝、微信、支付宝原生 SDK，**没有**在应用内配置微信/支付宝 `appid` 的环节；只做：登录 → 调后端下单 → 用系统浏览器 / 应用内浏览器打开返回的 **`pay_url`**（见 `value_added_page.dart`、`payment_service.dart`）。
  - **后端 `hibi_payment.py`** 向数捷 SDK2 `submit` 提交的参数为：`pid`（商户号）、`out_trade_no`、`name`、`money`、`type`（`alipay` / `wxpay` 等）、`notify_url`、`return_url`、`timestamp` 及 **`sign` / `sign_type`**。**不包含**微信公众账号、小程序或支付宝应用的 **`appid` 字段**；该层由 **易宝（Yeepay）及上游支付渠道**在商户入网、后台绑定应用时维护。
- **处理方向**：
  1. 在 **数捷商户后台** 或 **易宝商户平台** 按实际开通的支付方式，完成 **微信支付** 所需的 **公众号 / 小程序 / 移动应用 appid** 绑定或授权；**支付宝** 则绑定对应 **支付宝应用 appid**（具体以数捷/易宝对接文档为准）。
  2. 若已自行配置仍报 690001，按文案联系 **易宝运营**，核对子商户、渠道开通、appid 授权是否生效。
  3. **`GET /api/payment/config_status` 中 `ready: true`** 仅表示 **本机数捷密钥与下单 URL 等已齐、能向数捷发起下单**；**不能**替代易宝侧渠道 appid 配置，二者是不同阶段。
- **备注**：错误码与文案以易宝/数捷当时版本为准；若文档更新，以官方说明为准。

### 8.2 「补单」触发 `Method Not Allowed` 的原因与修复

- **SDK2 实测结论**（`SDK_2.0/SDK/notify_url.php`）：示例代码在回调页使用 `$_GET` 取参数并验签，说明回调/补单链路可能走 **GET**，而不只是 POST。
- **SDK2 期望返回**：示例在验签与业务处理成功后直接返回纯文本 **`success`**，失败返回 **`fail`**，不是 JSON。
- **现象解释**：若后端只实现 `POST /api/payment/notify`，在数捷后台点补单时浏览器会打开 `.../notify?...`（GET），就会出现 `{"detail":"Method Not Allowed"}`。
- **本项目修复**：
  1. `/api/payment/notify` 改为同时支持 `GET + POST`；  
  2. 兼容 `x-www-form-urlencoded`、`application/json` 与 query 参数；  
  3. 回调响应改为纯文本 `success/fail`（HTTP 200）；  
  4. 回调原文写入 `notify_raw`，便于排障。
- **上线验证**：部署后访问 `http://121.41.6.21:7861/api/payment/notify?out_trade_no=test` 不应再返回 `Method Not Allowed`；真实补单应回 `success` 或 `fail`。

### 8.3 支付成功但客户端未立即显示到期时间更新（2026-03-23 追加）

- **已定位问题**：回跳入账链路中，`hibi_payment.py` 曾误用 `sqlite3.Connection.rowcount`，在处理 `update_order_paid()` 时抛异常，导致 `pay_page` 出现 500，订单未能转 `paid`。
- **已修复并上线**：改为使用 `cursor.rowcount`；回放真实回调参数后，`/api/payment/pay_page` 返回 200 且显示 `回跳校验：success`。
- **客户端侧表现说明**：支付完成后订阅剩余时长存在短暂缓存（权益接口/页面刷新时机），通常点击「我已支付，查看结果」后会立即刷新；若仍未刷新，等待 5~20 秒后重进订阅页再看。
- **后端兜底机制（当前）**：
  1. `notify` 回调（GET/POST）验签并入账；  
  2. `return_url(pay_page)` 回跳带签名参数时再做一次入账；  
  3. 订单查询接口对 `pending` 订单触发主动查单（query）补齐。
