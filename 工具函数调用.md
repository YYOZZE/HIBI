# 工具函数调用（Function Calling / Tool Use）

本文档说明希比（HIBI）项目中**智能体对话**如何通过大模型的 **工具调用**能力，在服务端读写用户的**日程**与**思维节点（白板）**，使模型在明确指令下能够：查询/阅读日程、新建日程、读取思维节点图、为白板方块设置提醒。

更偏产品与设计背景的说明见：`lib/features/assistant/智能体工具调用_ABP功能设计.md`。本文聚焦**当前代码中的实现与接口**。

---

## 一、背景：什么是工具调用

主流 OpenAI 兼容接口在 `POST /v1/chat/completions` 中支持在请求体里传入 `tools`（函数名、描述、JSON Schema 参数），模型可在回复中返回 `tool_calls`（要调用的函数名与参数）。应用侧**执行**这些函数，把结果以 `role: "tool"` 的消息追加回对话，再请求模型生成最终自然语言回复。该模式常被称为 **Function Calling** 或 **Tool Use**。

本项目后端采用同一模式：**工具在服务端执行**，直接读写与该用户 Token 绑定的云端 `user_data`（与同步服务中的 mind、schedule 一致），不依赖前端再实现一套业务逻辑。

---

## 二、工作流程（端到端）

### 2.1 客户端：用户每发「一轮」话

1. 用户在 `AgentChatPage` 输入内容并发送（或语音转写后等价发送）。
2. Flutter 组装一次 `POST /api/chat` 请求体，主要包括：
   - `message` / `userMessage`：本轮**新**用户输入；
   - `history`：此前多轮的 `user` / `assistant` **纯文本**对话（**不含** `system`，由后端每次重新拼）；
   - `use_backend_system_prompt`、`agent_name`、`agent_role`、`current_mind_node_id`（可选）；
   - 请求头 `Authorization: Bearer <token>`（工具链路必需）。
3. **一次 HTTP 请求 = 用户在本轮对话里的一次发送**；本地仓库仍会追加消息，供下一轮把更长 `history` 带上。

### 2.2 服务端：组 `messages` 并调用模型

4. 若 `use_backend_system_prompt: true` 且 Token 有效：解析出 `user_id`；拼接 `system_content`（可选人设前缀 + ABP 全文，见第四节）。
5. 核心结构（见 `api_only_app.py`）：

   `messages = [ {"role":"system","content": system_content}, …history…, {"role":"user","content": 本轮用户输入} ]`

6. 走工具链路时调用 `_chat_completions_with_tools`：**非流式** `POST /v1/chat/completions`，携带 `tools` 与 `tool_choice: "auto"`。
7. 若模型返回 `tool_calls`：服务端执行 `execute_tool`（读写该用户 `user_data`），把 **assistant 消息**（含 `tool_calls`）和 **`role: tool`** 结果追加到**同一条 `messages` 列表**，再向模型发起下一轮请求；**最多 3 轮**（防止死循环）。
8. 得到最终 assistant 文本后，响应体只返回 `reply` 等字段给客户端（当前不把 tool 明细暴露给 Flutter）。

### 2.3 「是不是每句话都带一个 System Prompt？」——怎么理解

| 说法 | 正误 | 说明 |
|------|------|------|
| 用户每打一个字，就单独绑一条 system | **否** | 产品语义上不是这样设计的。 |
| 用户**每发送一轮**新消息，都会再打一次 `/api/chat` | **是** | 每一轮 HTTP 请求里，后端组出来的 `messages` **数组开头都会有一条** `role: "system"`。 |
| 为什么要重复带 system？ | 常见做法 | 大模型 API 多为**无状态**：服务端不替你长期保存对话，需要客户端用 `history` + 本轮输入一起上传；**system 约定与角色也每次放在数组首条**，保证模型始终看到同一份规则。 |
| 同一次用户发送里，工具多轮（第 7 步）会再塞多条 system 吗？ | **否** | 工具循环只在**服务端内存**里扩展同一份 `messages`：在已有 system、user、assistant、tool 之后追加，**不会**为每个 tool 再复制一份 system。 |

简要对照：

```
【一轮用户发言】
  客户端 POST /api/chat 一次
    → messages = [ system 一条, …历史…, user 本轮 ]
    → 若需工具：内部可能 1～3 次调模型，messages 变长，但 system 仍通常只在索引 0
【下一轮用户再发言】
  再 POST 一次，history 变长，system 再次出现在新请求的 messages[0]（内容可与上次相同）
```

---

## 三、在本项目中的位置与数据流

| 环节 | 说明 |
|------|------|
| 入口 | `POST /api/chat`（`backend_jideshi_hibi_app/api_only_app.py`） |
| 启用条件 | 请求体 `use_backend_system_prompt: true`，且请求头 `Authorization: Bearer <token>` 能解析出 `user_id` |
| 工具定义与执行 | `backend_jideshi_hibi_app/hibi_abp_tools.py`：`get_tools_definitions()`、`execute_tool()` |
| 多轮工具 | `_chat_completions_with_tools()`：非流式请求模型，若有 `tool_calls` 则执行工具并追加结果，**最多 3 轮** |
| 前端发参 | `lib/features/assistant/services/http_assistant_api.dart`：`use_backend_system_prompt`、`agent_name` / `agent_role`、`current_mind_node_id`（可选） |
| 对话页 | `lib/features/assistant/agent_chat_page.dart`：**用户已登录**（有 token）时对**任意智能体**开启 `useBackendSystemPrompt`，并传入 `currentMindNodeId`（若从白板进入或项目助理绑定节点） |

**为何以前「手动创建的智能体」不会调工具：**  
旧逻辑仅在 `agent.isAutoCreated == true`（白板自动创建的「项目助理」）时置 `use_backend_system_prompt: true`，自定义名称的智能体一直走「仅 agent 名称/职能」的普通 system prompt，**请求里不带 `tools`**，模型自然表现为「没法帮你建日程」。现已改为：**只要已登录，所有智能体对话都走后端 ABP + 工具**；后端会把 `agent_name`、`agent_role` **拼在 ABP 全文之前**，保留人设（如「肥呆」的说话风格）。

**简要数据流：**

```
用户消息 → tool_choice=auto（项目助理）→ 带 tools 请求模型
    → 若有 tool_calls → execute_tool（读写 user_data）→ 将 tool 结果写回 messages → 再次请求模型
    → 返回最终 reply
```

---

## 四、何时允许模型使用工具

在 **`use_backend_system_prompt: true` 且已登录** 下，后端对模型请求固定 **`tool_choice: "auto"`**（见 `api_only_app.py`），是否真正发起工具调用由 **System Prompt** 与模型判断；不再用关键词开关 `tool_choice`。

**未登录**：客户端应传 `use_backend_system_prompt: false`（或不传），后端使用 `agent_name` / `agent_role` 组普通 system prompt，**无 tools**（与旧版一致）。

`intent_allows_tools` 与 `INTENT_KEYWORDS` 仍保留在 `hibi_abp_tools.py` 中供兼容或测试，**不再参与**上述路由。

System Prompt 优先级：**`HIBI_ABP_SYSTEM_PROMPT_FILE`（文件非空）** → **后台「项目助理 Prompt」写入的 SQLite** → 代码 `DEFAULT_SYSTEM_PROMPT`。  
当请求同时带有 `agent_name` 或 `agent_role` 时，后端会在 ABP 正文**前**追加两段人设说明（名字、角色/风格），再与 `history`、`user` 拼进**同一次**请求的 `messages`（见第二节）。

代码默认 `DEFAULT_SYSTEM_PROMPT` 已强调：**具备 tools、用户要求排期/提醒时应调用工具，禁止谎称无法创建日程**（若你方在后台改过旧版 Prompt，请自行合并该段或从本仓库默认全文再保存一次）。

要点包括：可读全量项目列表与白板、可读日程；写入仅限「无提醒方块上设提醒」「新建/更新普通日程」；不删日程、不改白板布局；用户随口提到日程时可直接 `create_schedule`；新建日程默认提前 **15 分钟**提醒；多项目时先 `list_mind_projects` 再选相关项；`update_schedule` 勿用于 `mind_block_` id。

---

## 五、工具一览（实现与参数）

以下工具在 `get_tools_definitions()` 中以 OpenAI 兼容 `tools` 数组注册，由 `execute_tool()` 分发。

### 5.1 `list_mind_projects` — 列出全部思维节点项目

| 项目 | 说明 |
|------|------|
| **用途** | 返回当前用户全部项目的 `id` 与 `title`，便于模型自动挑选与话题相关的项目。 |
| **参数** | 无。 |
| **返回** | `projects`、`count`。 |

### 5.2 `get_mind_canvas` — 读取某一项目的白板

| 项目 | 说明 |
|------|------|
| **用途** | 获取指定项目下的方块与连线摘要（**不含**坐标等布局字段，避免误改布局）。 |
| **参数** | `project_name`：与思维节点 `title` **完全一致**；若配合请求体 `current_mind_node_id` 表示「当前项目」，可传空字符串。 |
| **方块字段** | 含 `id`、`text`、`completed`、`dotColor`、`reminderStartTimeMs`、`reminderEndTimeMs`、`hasReminder` 等。 |
| **返回** | `project_id`、`project_title`、`blocks[]`、`lines[]`；未找到项目时含 `error`。 |

### 5.3 `get_schedule` — 查询日程

| 项目 | 说明 |
|------|------|
| **用途** | 列出与日期范围**有交集**的日程（读 `user_data` 中 `schedule`）。 |
| **参数** | `start_date`、`end_date`（可选，仅日期 `YYYY-MM-DD`）；若均省略，则默认从**服务器本地日期「今天」**起连续 `days` 天（默认 **7**）。 |
| **返回** | `meta`（实际查询区间与条数）+ `events[]`（含 `reminderMinutes`、`location` 等）。 |

### 5.4 `create_schedule` — 新建日程

| 项目 | 说明 |
|------|------|
| **用途** | 新增一条日程（`id` 为 `evt_...`）。 |
| **参数** | 必填：`title`、`start_time`、`end_time`（ISO8601）；可选：`is_all_day`、`location`、`reminder_minutes`、`essence`。 |
| **默认提醒** | 未传 `reminder_minutes` 时写入 **15** 分钟。 |
| **返回** | `ok`、`id`、`title`。 |

### 5.5 `update_schedule` — 更新已有日程

| 项目 | 说明 |
|------|------|
| **用途** | 在用户已明确同意时更新一条**普通**日程。 |
| **参数** | 必填：`event_id`；可选字段按需覆盖：`title`、`start_time`、`end_time`、`is_all_day`、`location`、`reminder_minutes`、`essence`。 |
| **限制** | **`event_id` 以 `mind_block_` 开头时拒绝**，避免与方块提醒脱节；方块关联日程仍由 `set_block_reminder` 在无提醒方块上处理。 |

### 5.6 `set_block_reminder` — 为白板方块设提醒（并同步日程）

| 项目 | 说明 |
|------|------|
| **用途** | 在**尚无提醒**的方块上写入提醒，并同步 **id 为 `mind_block_<blockId>`** 的日程。 |
| **参数** | 必填：`block_id`、`start_time`、`end_time`；`project_name` 与请求体 `current_mind_node_id` 二选一用于定位项目。 |
| **限制** | 若方块**已有**起止提醒时间，**拒绝覆盖**，返回说明；可参考该时间在其它方块或新建 `create_schedule`。 |
| **返回** | 成功时 `ok`、`block_id`、`project_title` 等。 |

---

## 六、前端请求字段与响应

### 6.1 请求（与工具相关）

| 字段 | 说明 |
|------|------|
| `use_backend_system_prompt` | `true` 时走 ABP system prompt + 工具链路（须登录）。**客户端：已登录则应对所有智能体置 `true`。** |
| `agent_name` / `agent_role` | 在 ABP 路径下仍会传给后端，用于人设前缀（见第四节）。 |
| `current_mind_node_id` | 可选；当前打开的思维节点 id，供 `get_mind_canvas` 在「当前项目」语义下使用（见上）。 |
| `Authorization` | `Bearer` Token，后端解析 `user_id` 后读写该用户的 `mind` / `schedule`。 |

### 6.2 响应

| 字段 | 说明 |
|------|------|
| `reply` | 模型最终自然语言回复（前端 `HttpAssistantApi` 当前只解析并展示此字段）。 |
| `tools_used` | 在项目助理路径下通常为 `true`（固定 `tool_choice=auto`）。**并不保证**一定执行了某个工具；客户端已在对话成功后 **pull**（见第七节）。 |

---

## 七、与云端同步的关系

思维节点与日程的持久化格式与 `/api/sync/push`、`/api/sync/pull` 使用的 `user_data` 一致。工具在后端通过 `save_user_data` 改的是**服务端**数据。

**日历 / 白板界面读的是本地文件**（合并后的 `user_data` 镜像）。因此助理对话里若执行了 `create_schedule` 等工具，必须再调 **`GET /api/sync/pull`** 把云端合并到本地，日程页才会出现新条目。

**实现（当前客户端）：**

- `AgentChatPage` 在 `use_backend_system_prompt` 成功返回后调用 `UserSyncScheduler.pullAfterAssistantToolUse()`：执行 `pull` 并 `ScheduleEventStore.reloadFromDisk()` 等。
- 历史上 `UserSyncService.pull` 曾要求 **「数据服务」订阅**，导致**只订阅助理、未订阅数据服务**的用户 **`pull` 被跳过**，工具写入云端后本地永远为空——表现为「助理说建好了，日历没有」。现已支持在「助理工具后拉取」路径 **`bypassSubscriptionCheck: true`**，只要已登录即可拉取合并（与是否购买数据服务包解耦）。

**仍看不到日程时：** 若模型未真正发起 `tool_calls`（仅口头编造「已创建」），云端也无数据，拉取无效——见第十节与后端日志。

---

## 八、相关源文件索引

| 路径 | 作用 |
|------|------|
| `backend_jideshi_hibi_app/api_only_app.py` | `/api/chat`、`_chat_completions_with_tools` |
| `backend_jideshi_hibi_app/hibi_abp_tools.py` | 工具定义、意图关键词、execute_tool |
| `backend_jideshi_hibi_app/hibi_auth_sync.py`（由 api 引用） | `get_user_data` / `save_user_data`、Token → `user_id` |
| `lib/features/assistant/services/http_assistant_api.dart` | 组装请求 |
| `lib/features/assistant/agent_chat_page.dart` | 已登录则 `useBackendSystemPrompt`；成功后 `pullAfterAssistantToolUse` |
| `lib/features/auth/services/user_sync_scheduler.dart` | `pullAfterAssistantToolUse`、`pullAndNotify` |
| `lib/features/auth/services/user_sync_service.dart` | `pull(..., bypassSubscriptionCheck)` |
| `lib/features/assistant/智能体工具调用_ABP功能设计.md` | 设计稿与待确认项 |
| `lib/features/assistant/智能体对话原理.md` | 通用对话链路（非工具专用） |

---

## 九、小结

- **已登录用户**与**任意智能体**对话时，应开启 `use_backend_system_prompt`，使模型收到 **tools**；否则自定义智能体不会出现「可调工具」的行为。
- 可读取**全部项目列表**与**白板摘要**、按区间查询**日程**；写入主要为 **方块提醒**（不覆盖已有提醒）与 **新建/更新普通日程**（默认提前 15 分钟提醒）。
- 实现上遵循 OpenAI 兼容 **`tools` + `tool_calls` + `role: tool`** 多轮补全；业务逻辑集中在 **`hibi_abp_tools.py`**。
- `get_mind_canvas` 以项目**标题**匹配，并可选结合 **`current_mind_node_id`** 表示当前项目；`list_mind_projects` 用于项目多时先枚举再筛选。

## 十、排查：模型仍不调用工具 / 日历没有新日程

**A. 模型不调用工具**

1. **是否登录**：无 token 时不会走工具链路。  
2. **请求体**：`use_backend_system_prompt` 是否为 `true`（见 `HttpAssistantApi` 组包）。  
3. **后台 Prompt**：若 SQLite / 文件中写入了「禁止调用工具」等冲突指令，会压制行为；对照 `hibi_abp_tools.DEFAULT_SYSTEM_PROMPT` 检查。  
4. **模型能力**：所用 `MODEL_ID` 需支持 OpenAI 兼容的 `tools`（豆包等需按厂商文档确认）；若 API 忽略 `tools`，需换模型或检查 `_chat_completions_with_tools` 请求格式。

**B. 助理口头说「已创建」但日历仍无**

1. **是否仅编造**：若本轮未产生真实 `tool_calls`，服务端无新日程，拉取也不会出现——需对照服务端日志或给 `/api/chat` 增加 `tools_used` 明细（未来可扩展）。  
2. **同步是否执行**：确认客户端已包含「对话成功后 `pullAfterAssistantToolUse`」逻辑；旧版本无此步骤时必现「云端有、本地无」。  
3. **日期/时区**：`create_schedule` 使用 ISO8601 时间；若模型把「明天 20:00」写错成 UTC 或日期偏移，可能在月视图上落在别的格子。
