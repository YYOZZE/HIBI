# 阿里图形认证接入说明（登录 / 注册）

登录与注册**仅**走阿里云 **图形认证**（号码认证 PNVS 子能力）：控制台 **[图形认证方案管理](https://dypns.console.aliyun.com/graphSolution)**，`ct4.js` + `initAlicom4`，服务端二次校验 `https://captcha.alicaptcha.com/validate`。

## 官方文档（必读顺序）

1. [在 H5 页面集成网页端 SDK 实现图形认证](https://help.aliyun.com/zh/pnvs/developer-reference/integrate-the-sdk-with-h5-pages) — `captchaId`（appId）、`product: "bind"`、`onNextReady` 后再 `showCaptcha`。
2. [服务端调用二次校验接口实现图形认证](https://help.aliyun.com/zh/pnvs/developer-reference/graphical-authentication-server-integration) — `sign_token = HMAC-SHA256(app_key, lot_number)`，`POST` `application/x-www-form-urlencoded`。

## 控制台方案与密钥（与代码默认一致）

| 端 | 方案名称 | 方案编码 | appId | appKey（仅服务端 .env，勿提交仓库） |
|----|----------|----------|-------|--------------------------------------|
| win 端 H5 / 桌面 WebView | hibi23h52 | FG000000009822094004 | `7cbe038048b4be54f004373f63ca3a86` | `ec5e2503f96f2c791313194ca095839f` |
| 鸿蒙 | hibi23hm2 | FG000000009772444004 | `38b49eb185433cc60cf9fae64eba26b0` | `f5f52cc329c3fddbb4cd46934d809f88` |
| iOS | hibi23pg2 | FG000000009702314004 | `ae43dbbddb3eb925f81a47da7cf460b7` | `ee90f83c78598ac152ee8ab80efaf238` |
| 安卓 | hibi23az2 | FG000000009851764004 | `114c902dbd26b325b8a5359cac5d13b1` | `19aa380e6185999e209c1c4aa9df8eb2` |

- 请求 **`/api/auth/*` 时 `platform` 取值**：`web`（含 Flutter Web、Windows/macOS/Linux 桌面）、`android`、`ios`、`harmony`（独立鸿蒙 Flutter 需在 `auth_captcha_platform.dart` 中启用 `TargetPlatform.ohos` 分支）。
- **方案编码 FG*** 仅供运维对照日志；**SDK 与二次校验只认 appId / appKey**。

### `captcha_id paused`（-50105）与控制台是否有「开关」

官方 [图形认证接入流程](https://help.aliyun.com/zh/pnvs/user-guide/graphical-authentication-service-usage-process) 写的是：**新增方案 → 选接入端 → 取密钥**，以及验证形式、流量图等，**并未描述**类似「暂停 / 启用方案」的独立按钮。因此你在 **图形认证方案管理**里找不到明显「开关」是正常现象。

服务端错误表里 **`-50105` / `captcha_id paused`** 的语义仍是 **「该验证 id 当前不可用于拉取/校验」**（[服务端二次校验文档 - 错误码表](https://help.aliyun.com/zh/pnvs/developer-reference/graphical-authentication-server-integration)）。这通常由**阿里云侧策略或账号状态**决定，而不一定对应你能点到的某一个 UI。实务上可按下面排查（仍无解则 **工单 + 4008013260**，报 appId、FG 方案编码、完整报错文案）：

1. **产品入口一致**：必须在 **[号码认证 DYPNS](https://dypns.console.aliyun.com/overview)** 下 **图形认证方案**，不要和 **「验证码 2.0」** 等产品线混用（appId 来源不同）。
2. **账号与计费**：号码认证/图形认证是否已开通、是否欠费、是否仅有试用额度已耗尽（部分状态下接口会直接拒绝）。
3. **密钥与方案是否仍有效**：方案是否被删除或重建过（旧 appId 即会失效）；`.env` / 后端默认里的 appId 是否与当前列表里 **密钥参数** 完全一致、无多空格。
4. **接入端是否匹配**：H5/Web 控制台方案应选 **H5**；若方案实际建在 Android 却用于 H5 初始化，也可能表现为配置类错误（以阿里云返回为准）。

此前提示「到控制台启用方案」是一种**通俗说法**；更准确是：**在控制台与工单侧确认该 appId 在号码认证图形认证产品线中处于可用状态**，而不是单纯找「启用开关」。

## 本项目实现要点

| 环节 | 说明 |
|------|------|
| 后端 | `hibi_graph_captcha.py` 按 `platform` 选 appId/appKey；`api_only_app.py` 提供 `captcha_config`、`embed`（内联 `ct4.js`）、`challenge`、`verify`；登录/注册校验四参数或已验证 challenge。 |
| embed 通信 | **Windows WebView2**：`chrome.webview.postMessage`。**Android/iOS**：`webview_flutter` 注册 `JavaScriptChannel` 名 **`HibiCaptcha`**；embed 内 `send()` 优先走 `HibiCaptcha.postMessage(JSON.stringify(obj))`。 |
| Windows | `webview_windows` + `GET .../embed?app_id=...&platform=web`（与 **hibi23h52** 同一 appId）。 |
| Android / iOS | `webview_flutter` 加载同一 embed URL，`platform` 传 `android` / `ios`。 |
| Flutter Web | `dart:js` 调 `initAlicom4`，已加 **`https: true`** 与官方一致。 |
| `ct4.js` | 控制台 **API&SDK → 图形验证码 → 网页端 → H5** 下载；仓库 `static/ct4.js` 应与之一致。 |

### iOS 使用 HTTP 基址时

若 `ApiConfig.authApiBaseUrl` 为 **`http://` 明文 IP**，必须在 iOS `Info.plist` 中为该域配置** ATS 例外**（或改为 HTTPS），否则 WebView 无法加载 embed。

## 接口与自检

- `GET /api/auth/captcha_config?platform=...`
- `POST /api/auth/captcha/challenge`，再完成验证后 `POST /api/auth/captcha/verify`，登录/注册带 `captcha_challenge_id` 与四参数（或按现有 challenge 流程）。

详细部署与排障：[阿里云图形认证部署与排障.md](./阿里云图形认证部署与排障.md)。

## 历史长文与 WebView 排障

原先较长的「根因 A～I、WebView2 挂死」等说明已收敛到上文与部署排障文档；若需逐条对照旧版，可在 Git 历史中查看 `阿里图片认证.md` 过往版本。
