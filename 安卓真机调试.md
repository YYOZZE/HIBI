# 安卓真机调试说明

本文记录 USB 与无线两种方式，以及**无线调试时在电脑输入手机显示的配对码**的步骤，便于后续直接按文档操作。

---

## 一、方式一：USB 连接

1. **手机**：设置 → 开发者选项 → 打开「USB 调试」。
2. **数据线**连接电脑，手机弹出「允许 USB 调试？」点**允许**。
3. **电脑**在项目根目录执行：
   ```bash
   flutter devices
   ```
   确认列表里出现你的安卓设备后执行：
   ```bash
   flutter run -d android
   ```
   或只连了一台设备时直接：
   ```bash
   flutter run
   ```

---

## 二、方式二：无线调试（含配对码）

不插线，通过 WiFi 连接。需要手机和电脑在同一局域网。

### 2.1 手机端

1. 设置 → 开发者选项 → 打开 **「无线调试」**。
2. 进入「无线调试」 → 点 **「使用配对码配对设备」**（或「Pair device with pairing code」）。
3. 界面会显示：
   - **IP 和端口**（如 `192.168.1.100:37123`，这是**配对端口**）；
   - **6 位配对码**（如 `123456`）。  
   记下这两项，下一步在电脑上要用。

### 2.2 电脑端：输入配对码

在**项目根目录**打开终端，执行（把 `IP` 和 `配对端口` 换成手机显示的）：

```bash
adb pair IP:配对端口
```

例如手机显示 `192.168.1.100:37123`，则：

```bash
adb pair 192.168.1.100:37123
```

回车后终端会提示 `Enter pairing code:`，**输入手机上的 6 位配对码**并回车。配对成功会提示 `Successfully paired to ...`。

### 2.3 电脑端：连接调试

无线调试界面还会有一个**连接用端口**（通常与配对端口不同，如 `192.168.1.100:5555`）。执行：

```bash
adb connect IP:连接端口
```

例如：

```bash
adb connect 192.168.1.100:5555
```

若无线调试页只给了一组「IP:端口」，可先用该端口做 `adb pair`，配对成功后一般会多出一个设备，再用 `adb devices` 查看设备 id，或直接：

```bash
flutter run -d android
```

Flutter 会优先选已连接的安卓设备。

### 2.4 小结（无线）

| 步骤 | 命令 / 操作 |
|------|-------------|
| 1. 手机 | 无线调试 → 使用配对码配对设备 → 记下 **IP:配对端口** 和 **6 位码** |
| 2. 电脑 | `adb pair IP:配对端口` → 提示后**输入 6 位配对码** |
| 3. 电脑 | `adb connect IP:连接端口`（或根据 `adb devices` 选设备） |
| 4. 运行 | `flutter run -d android` 或 `flutter run` |

---

## 三、常见问题

- **`adb` 找不到**：安装 Android SDK Platform-Tools，并把其目录加入系统 PATH；或使用完整路径运行 `adb`。
- **`flutter devices` 没有安卓设备**：先执行 `adb devices` 看是否列出设备；若没有，检查 USB 线/无线配对与连接。
- **依赖解析卡住**：见项目根目录 `README.md` 中「依赖解析报错 / 手机调试启动不了」，配置国内镜像后重试。
- **Gradle 构建很慢**：见 `README.md` 中「Android 真机运行卡在 assembleDebug」，项目已配 Maven 镜像，首次稍等即可。

---

*配对码每次在手机点「使用配对码配对设备」都会变化，只需在电脑执行一次 `adb pair` 并输入当次的 6 位码即可。*
