# 希比助手技术框架

> 产品模型很简单：**AI 判断 → 调用 tools → 端侧写日程 / 思维导图**。  
> 不依赖后端执行工具；打开对话即可聊，无需启动进度条。

---

## 1. 一句话架构

**希比助手**用「智能体配置」里的火山/OpenAI Key **直连模型 API**；每轮带上固定一小套 tools；工具在 App 内执行，写本机 `ScheduleEventStore` / `MindRepository`。

```
AgentChatPage（打开即聊）
  → ChatSessionService.send
      ├─ 希比助手 → ClientAbpRuntime
      │     静默 ensureLoaded → OpenAiCompatibleDirectApi.chatWithTools
      │     → ClientAbpExecutor（本地写日程/思维）
      └─ 其他智能体 → 后端或纯直连聊天
```

内置人设：`lib/features/assistant/models/hibi_assistant.dart`（`agent_hibi_builtin`）。

---

## 2. 关键文件

| 层级 | 路径 | 作用 |
|------|------|------|
| 对话页 | `lib/features/assistant/agent_chat_page.dart` | UI、语音、附件 |
| 会话编排 | `lib/features/assistant/services/chat_session_service.dart` | 队列/打断；希比助手走端侧 |
| 直连 + tools | `lib/features/assistant/services/openai_compatible_direct_api.dart` | `chatWithTools` |
| 端侧编排 | `lib/features/assistant/services/client_abp/client_abp_runtime.dart` | tools 定义、system prompt、静默 `ensureLoaded` |
| 端侧执行 | `lib/features/assistant/services/client_abp/client_abp_executor.dart` | 写日程/思维 |
| 日程存储 | `lib/features/schedule/schedule_event_store.dart` | 本地 `hibi_schedule_events.json` |
| 思维存储 | `lib/features/mind/services/mind_repository.dart` | 本地思维节点 |

---

## 3. 运行要点

1. **凭据**：必须在「设置 → 智能体配置」填写并选中 Key；缺失时会话明确报错提示。
2. **预热**：首次调用前 `ClientAbpRuntime.ensureLoaded()` 静默加载本地 store，**无全屏进度 UI**。
3. **tools**：固定 7 个（日程 + 思维），每轮注入；无庞大 Intent / Skill Index / Bootstrap。
4. **写盘后刷新**：端侧写完后 bump `UserSyncScheduler.syncEpoch`，日程页监听后刷新。
5. **严禁幻觉**：未收到工具 `ok` 前，模型不得说「已经写好了」。

---

## 4. Tools（固定小套）

| 工具名 | 作用 |
|--------|------|
| `list_mind_projects` | 列出思维节点项目 |
| `get_mind_canvas` | 读某项目白板摘要 |
| `get_schedule` | 按日期范围查日程 |
| `create_schedule` | 新建普通日程（`evt_` 前缀） |
| `update_schedule` | 更新普通日程（需用户明确同意） |
| `delete_schedule` | 软删除普通日程 |
| `set_block_reminder` | 给尚无提醒的白板方块设提醒 |

默认提前提醒 **15** 分钟（`kClientAbpDefaultReminderMinutes`）。

---

## 5. 自定义模型与其它智能体

- 希比助手：**始终**端侧 `chatWithTools`，用选中的火山/OpenAI 兼容配置。
- 其他智能体：仍可走后端 `/api/chat`，或无 Key 时回退；与希比助手路径独立。

---

## 6. 附件与 ASR

- 附件：发送时拷贝到账号目录，气泡可预览/打开。
- ASR：App 直连火山 OpenSpeech（`AsrStreamService`），凭据在智能体配置。

---

## 7. 想改什么改哪里

| 目标 | 改哪里 |
|------|--------|
| 助手人设 | `hibi_assistant.dart` 的 `role` |
| tools 列表 / system prompt | `client_abp_runtime.dart` |
| 工具执行逻辑 | `client_abp_executor.dart` |
| 日历展示刷新 | `schedule_page.dart`（`syncEpoch`） |
| 直连协议 | `openai_compatible_direct_api.dart` |
