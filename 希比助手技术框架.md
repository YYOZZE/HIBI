# 希比助手技术框架

> 面向二次开发：以当前仓库实现为准。  
> **产品链路（希比助手主路径）**：模型判断 → function calling → App 本地改日程 / 思维。  
> **直连模型 API**（火山/OpenAI 兼容 Key）；**不走后端 ABP 执行工具**；无过重预加载进度条、无复杂意图路由。

---

## 1. 一句话架构

内置「希比助手」读取「设置 → 智能体配置」中的 Key，**直连** `/chat/completions`；每轮注入固定 7 个 tools；模型决定是否调用；`ClientAbpExecutor` 写本机 `ScheduleEventStore` / `MindRepository`。

```
用户发消息
  → AgentChatPage（打开即聊，无启动进度条）
  → ChatSessionService
      ├─ 希比助手（agent_hibi_builtin）→ ClientAbpRuntime   ← 主路径：端侧
      │     静默 ensureLoaded（无 UI）
      │     → OpenAiCompatibleDirectApi.chatWithTools（直连 + function calling）
      │     → ClientAbpExecutor（本地写日程 / 思维）
      │     → syncEpoch++（日程页刷新）
      └─ 其他智能体 → 仍可走后端 /api/chat 或纯直连聊天（与希比助手无关）
```

人设写死：`lib/features/assistant/models/hibi_assistant.dart`。

**已废弃叙事（勿再按此理解希比助手）**：

- 「永远走后端 ABP / 必须登录才有 Skills」——希比助手主路径已是端侧。
- 「打开对话要预加载进度条 / 按意图裁剪 tools」——已去掉。

---

## 2. 关键文件

| 层级 | 路径 | 作用 |
|------|------|------|
| 对话页 | `lib/features/assistant/agent_chat_page.dart` | UI、语音、附件 |
| 会话编排 | `lib/features/assistant/services/chat_session_service.dart` | 队列/打断；内置助手走 `ClientAbpRuntime` |
| 端侧编排 | `lib/features/assistant/services/client_abp/client_abp_runtime.dart` | tools schema、system prompt、静默 `ensureLoaded`、发起 `chatWithTools` |
| 端侧执行 | `lib/features/assistant/services/client_abp/client_abp_executor.dart` | 本地执行 7 个 tools |
| 直连 HTTP | `lib/features/assistant/services/openai_compatible_direct_api.dart` | OpenAI 兼容直连 + function calling 循环 |
| 人设 | `lib/features/assistant/models/hibi_assistant.dart` | 内置 id / name / role |
| 日程存储 | `lib/features/schedule/schedule_event_store.dart` | 本地日程 JSON |
| 思维存储 | `lib/features/mind/services/mind_repository.dart` | 本地思维项目 |
| 写盘后刷新 | `lib/features/auth/services/user_sync_scheduler.dart`（`syncEpoch`）+ `schedule_page.dart` | 工具写完后月历刷新 |
| 后端（其他智能体/同步） | `backend_jideshi_hibi_app/api_only_app.py` | `/api/chat`、`/api/sync/*`；**不是**希比助手 tools 执行面 |
| 后端工具定义（遗留对照） | `backend_jideshi_hibi_app/hibi_abp_tools.py` | 服务端 ABP；希比助手主路径不依赖 |

---

## 3. 运行要点

1. **凭据**：必须在「智能体配置」填写并选中火山/OpenAI 兼容 Key；缺失则会话报错提示配置。
2. **直连**：请求发往用户配置的 `base_url` + `/chat/completions`，不经 HIBI 后端转发工具调用。
3. **tools**：固定 7 个（日程 + 思维），每轮全量注入；无 IntentRouter / Skill Index / Bootstrap 进度条。
4. **预热**：首次调用前静默 `ensureLoaded()` 加载本地 store，不挡 UI。
5. **防幻觉**：未收到工具 `ok` 前，禁止说「已经写好了」。默认提醒 15 分钟（`kClientAbpDefaultReminderMinutes`）。

---

## 4. Tools（固定小套）

| 工具名 | 作用 |
|--------|------|
| `list_mind_projects` | 列出思维节点项目 |
| `get_mind_canvas` | 读某项目白板摘要 |
| `get_schedule` | 按日期范围查日程 |
| `create_schedule` | 新建普通日程（`evt_` 前缀） |
| `update_schedule` | 更新普通日程（需用户明确同意） |
| `delete_schedule` | 软删除普通日程 |
| `set_block_reminder` | 给尚无提醒的白板方块设提醒 |

---

## 5. 其它智能体 / 附件 / ASR

- **其它智能体**：可走后端 `/api/chat` 或无后端时的纯直连；与希比助手端侧路径独立。
- **附件**：发送时拷贝到账号目录，气泡可预览/打开。
- **ASR**：App 直连火山 OpenSpeech（`AsrStreamService`），凭据在智能体配置。

---

## 6. 想改什么改哪里

| 目标 | 改哪里 |
|------|--------|
| 助手人设 / 何时该调工具（文字约束） | `lib/features/assistant/models/hibi_assistant.dart` 的 `role` |
| tools 列表 / system prompt | `lib/features/assistant/services/client_abp/client_abp_runtime.dart` |
| 工具执行（写日程/思维） | `lib/features/assistant/services/client_abp/client_abp_executor.dart` |
| 直连与 function calling 协议 | `lib/features/assistant/services/openai_compatible_direct_api.dart` |
| 希比助手是否走端侧 | `lib/features/assistant/services/chat_session_service.dart`（`HibiAssistant.isBuiltInId` 分支） |
| 日历展示刷新 | `lib/features/schedule/schedule_page.dart`（监听 `syncEpoch`） |
