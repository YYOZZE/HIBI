# 主题功能 · 技术说明

本文档记录希比 HIBI 当前主题系统的技术原理、文件结构与扩展方式（含新增 `2027SS` 主题）。

---

## 一、主题概览

用户可在「设置 → 主题设置」切换以下主题：

| 主题 ID | 展示名称 | 视觉风格 |
|--------|----------|----------|
| `hibi` | hibi主题 | 图三背景 + 毛玻璃 + 深紫色系 |
| `dark` | 暗色主题 | 纯色深色背景 + 蓝色点缀 |
| `light` | 亮色主题 | 纯色浅色背景 + 蓝色点缀 |
| `2027ss` | 2027SS | 高级深色分层 + 柔光蓝 + 更大圆角（类苹果风格） |

---

## 二、技术原理

### 2.1 主题状态与持久化

- 入口文件：`lib/app/theme_notifier.dart`
- 核心对象：`ThemeNotifier extends ChangeNotifier`
- 工作流：
  - 启动时 `load()` 从 `SharedPreferences` 读取 `app_theme_id`
  - `setThemeId()` 写回本地并 `notifyListeners()`
  - 根节点监听后重建 `MaterialApp(theme: ...)`

### 2.2 根节点注入与热切换

- 根入口：`lib/main.dart`
  - 启动时创建 `ThemeNotifier` 并先 `await load()`
  - 通过 `ThemeNotifierScope` 注入全局
  - `ListenableBuilder` 监听主题变化并重建 `MaterialApp`

### 2.3 ThemeData 与 ThemeExtension 双层设计

- 主题主配置：`lib/app/app_theme.dart`
  - `AppTheme.getTheme(AppThemeId id)` 统一返回对应 `ThemeData`
  - 每个主题定义自己的 `ColorScheme`、`textTheme`、`card/dialog/input/button/navigationBar` 等

- 主题扩展：`lib/app/app_theme_extension.dart`
  - `themeId`：当前主题枚举
  - `useImageBackground`：是否使用图像毛玻璃背景
  - `solidBackgroundColor`：纯色主题主背景色

此设计让页面层既可读标准 Material 颜色，也能读主题“语义字段”（比如是否要图像背景）。

### 2.4 背景渲染策略（重点）

- 文件：`lib/app/frosted_background.dart`
- 渲染规则：
  - `hibi`：图像背景 `xhb-image/3.png` + 高斯模糊 + 半透明蒙层
  - `dark/light`：纯色 `ColoredBox`
  - `2027SS`：深色线性渐变 + 冷蓝/暖灰柔光圆形光晕 + 轻模糊遮罩

> `2027SS` 的背景是独立分支，不依赖图片资源，可稳定跨端一致显示。

### 2.5 主壳与底部导航适配

- 文件：`lib/app/main_shell.dart`
- 逻辑：
  - 当 `useImageBackground == true`（hibi）时，底栏使用毛玻璃层
  - 其它主题（含 2027SS）使用不透明 `NavigationBar`，避免页面内容与底栏视觉重叠

### 2.6 页面级主题接入

- 主题选择页：`lib/features/profile/settings_page.dart`
  - 通过 `AppThemeId.values` 动态渲染主题列表
  - 图标/副标题由 `_iconFor()`、`_subtitleFor()` 映射
  - 点击后调用 `ThemeNotifierScope.of(context).setThemeId(id)`

- 画布页：`lib/features/mind/mind_canvas_page.dart`
  - 读取 `HibiThemeExtension` 决定背景策略（图像/纯色）

---

## 三、2027SS 主题实现说明

### 3.1 目标

- 高级感、克制、偏“类苹果”风格
- 深色基底但不死黑，强调层次与光感
- 交互控件圆角更柔和，信息区块边界更细腻

### 3.2 关键样式点

- 主色：偏 Luminous Blue 风格（用于按钮、选中态）
- 背景：深色渐变 + 冷暖双光晕平衡
- 卡片/弹窗：更大圆角（20+）
- 按钮与输入框：圆角统一、描边弱化、焦点态更柔
- 底部导航：胶囊选中态 + 更稳的对比度

---

## 四、文件结构与职责

| 文件 | 职责 |
|------|------|
| `lib/main.dart` | 创建并加载 `ThemeNotifier`，挂载 `MaterialApp(theme)` |
| `lib/app/theme_notifier.dart` | 主题枚举 `AppThemeId`、主题持久化、切换通知 |
| `lib/app/theme_notifier_scope.dart` | 将 `ThemeNotifier` 注入 widget 树 |
| `lib/app/app_theme.dart` | 所有主题的 ThemeData 定义与分发 |
| `lib/app/app_theme_extension.dart` | 扩展主题语义字段（背景策略等） |
| `lib/app/frosted_background.dart` | 全局背景渲染（hibi / dark / light / 2027SS） |
| `lib/app/main_shell.dart` | 主壳、底部栏与主题背景适配 |
| `lib/features/profile/settings_page.dart` | 主题设置入口与切换 UI |
| `lib/features/mind/mind_canvas_page.dart` | 画布背景读取主题扩展并适配显示 |

---

## 五、新增主题标准步骤

1. 在 `AppThemeId` 新增枚举值与 `fromValue` 映射  
2. 在 `AppTheme.getTheme()` 新增分支并实现对应 ThemeData  
3. 为该主题附加 `HibiThemeExtension`  
4. 如需特殊背景，在 `FrostedBackground` 增加主题分支  
5. 在 `ThemeSettingsPage` 补齐图标和副标题映射  
6. 手动验证关键页面（主壳、设置页、白板、弹窗、输入框、按钮）  

---

## 六、维护建议

- 优先在 `AppTheme` 层统一收敛颜色与圆角，页面尽量少写硬编码色值
- 新主题先确定“语义色”（背景、表面、文字、强调、描边）再落控件
- 页面如果需要主题差异化，优先读取 `HibiThemeExtension.themeId` 判断

---

*文档对应当前代码实现：`lib/app/` 与 `lib/features/profile/settings_page.dart`、`lib/features/mind/mind_canvas_page.dart`。*
