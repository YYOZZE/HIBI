# 日程提醒功能说明（现状 · 系统能力 · 改进方向）

本文记录 **希比 HIBI** 客户端在「设置好的提醒时间到达后」实际调用的系统能力与数据流，并归纳主流技术方案，便于后续把「到点提醒」做得更可靠、体验更接近系统日历/闹钟类应用。

---

## 1. 业务含义（提醒在什么时候响）

- 每条日程有 **开始时间** `startTime` 与可选 **提前提醒分钟数** `reminderMinutes`（如 15 表示开始前 15 分钟提醒）。
- **提醒触发时刻** = `startTime - reminderMinutes`（非全天日程；全天日程在展示文案上有单独处理）。
- 支持 **重复日程**（日/周/月）时，会对每个周期内未来的多个「触发时刻」分别登记（有上限，见下文章节 3）。

---

## 2. 现有实现：到点后 App 用了什么「系统能力」

实现集中在 **`lib/features/schedule/schedule_reminder_service.dart`**，依赖 **`flutter_local_notifications`**（`pubspec.yaml` 中声明），并配合 **`timezone` / `flutter_timezone`** 做本地时区下的 `zonedSchedule`。

### 2.1 核心机制（按优先级）

| 机制 | 作用 |
|------|------|
| **定时本地通知 `zonedSchedule`** | 在提醒时刻由系统 **AlarmManager（Android）** / **UserNotifications（iOS/macOS）** / **Windows 通知（WinRT）** 触发一条 **本地通知**，带标题、正文、铃声、振动（各平台能力不同）。 |
| **Android：`AndroidScheduleMode.exactAllowWhileIdle`** | 尽量使用 **精确闹钟**，在 Doze/空闲时仍有机会触发；失败时降级为 **`inexactAllowWhileIdle`**。 |
| **Android：通知渠道 `AndroidNotificationChannel`** | 渠道 id：`hibi_schedule_alarm_v1`；**高重要性**、`AudioAttributesUsage.alarm`、振动模式，使表现接近「闹钟/提醒」类而非普通静默通知。 |
| **Android：权限** | 见 `android/app/src/main/AndroidManifest.xml`：`POST_NOTIFICATIONS`、`SCHEDULE_EXACT_ALARM`、`RECEIVE_BOOT_COMPLETED`、`VIBRATE`、`WAKE_LOCK`；启动时还会请求 **`requestExactAlarmsPermission()`**（与精确闹钟相关）。 |
| **前台兜底 `Timer.periodic`（约每 6 秒）** | 进程在 **前台运行** 时，在时间窗口内再 **主动 `show()` 一次通知**，并驱动 **`alertNotifier`** 弹出应用内 **AlertDialog**（「我知道了 / 5 分钟后再提醒」），减轻「仅依赖系统调度时偶发不准」的问题。 |
| **Windows：`WindowsNotificationScenario.alarm` + 预设铃声** | 使用 **闹钟场景** 的长时通知；并可选调用 **`user32.dll` 的 `MessageBeep`** 作为额外提示音。 |
| **数据变更后重新登记** | `ScheduleEventStore` 在加载/增删改日程后调用 **`rescheduleAll` / `upsertEvent` / `removeEvent`**，先 **`cancelAll` 再按当前列表** 重新 `zonedSchedule`，保证与本地 JSON 一致。 |
| **回到前台** | `MainShell` 在 **`AppLifecycleState.resumed`** 时再执行一次 **`rescheduleAll`**，缓解杀后台、省电策略导致的漏登记。 |

### 2.2 各平台「用户看到/听到」的是什么（概括）

- **Android**：系统通知栏/ heads-up；渠道为「日程提醒」类；振动 + 铃声（受系统音量与勿扰策略影响）。
- **iOS / macOS**：系统本地通知（初始化时请求 alert/sound/badge）；**非**系统「时钟」应用，仍是 App 的本地通知。
- **Windows**：操作中心中的 **闹钟场景** 通知 + 可选系统 beep；**不是**单独的 Win32 闹钟程序。

### 2.3 应用内弹窗（与系统通知的关系）

- 当 **App 在前台** 且命中兜底时间窗时，除系统通知外，还会通过 **`ScheduleReminderAlert` + `showDialog`** 弹出 **「日程提醒」** 对话框（见 **`lib/app/main_shell.dart`**）。
- **进程不在前台** 时，主要依赖 **`zonedSchedule` 触发的系统通知**；若用户未授权通知或系统杀进程策略极端，体验会弱于前台。

---

## 3. 实现细节与约束（与「不完善」相关的点）

1. **单条日程最多登记约 96 个未来时间点**（`_maxSchedulesPerEvent`），重复日程在 90 天 horizon 内展开；超长序列需考虑分批或滚动扩展。
2. **精确闹钟**：依赖用户是否授予 **精确闹钟**（Android 12+ 部分机型需进系统设置）；否则走 **非精确** 窗口，可能偏差数分钟。
3. **重启手机**：清单已声明 `RECEIVE_BOOT_COMPLETED`；**具体是否依赖插件在开机后自动恢复全部 Scheduled Notification**，应以当前 `flutter_local_notifications` 版本行为为准。若线上发现「重启后全丢」，需要在 **`Application` 或 `BroadcastReceiver` 收到开机广播后执行 Dart 侧 `rescheduleAll`**（见下文改进项）。
4. **点击通知**：当前代码对 `payload`（事件 id）有传递，但 **`initialize` 未统一接入「点击通知打开日程详情」的回调**（若需跳转，应加 `onDidReceiveNotificationResponse` / 对应平台 API，并配合全局 `Navigator`）。
5. **国内安卓厂商**：省电/自启动限制可能导致 **延迟或合并告警**；除技术方案外，需要 **引导用户在系统设置中允许自启动、关闭电池优化**（与微信等 IM 类似）。

---

## 4. 主流技术方案（行业常见做法）

以下与「日历 / 待办 / 闹钟」类应用常见做法一致，可按投入分阶段做。

### 4.1 本地提醒（与当前架构一致，优先做扎实）

| 方案 | 说明 |
|------|------|
| **本地通知 + 精确闹钟** | Flutter 侧：`flutter_local_notifications` + `timezone`；Android 侧 `SCHEDULE_EXACT_ALARM` + 用户授权。与 **Google 日历、Outlook 移动端** 的本地提醒同类。 |
| **开机/进程重启后全量重排** | `BOOT_COMPLETED` 或 `Application.onCreate` 中调用 **`schedule_event_store` 加载 + `rescheduleAll`**，保证与持久化数据一致。 |
| **周期性任务补登记** | 使用 **`workmanager`** 或 **`android_alarm_manager_plus`** 每 N 小时执行一次 **reschedule**，防止 OEM 清理 Alarm。 |
| **高优先级渠道 + 全屏意图（可选）** | Android 10+：对「强提醒」可用 **full-screen intent**（需权限与合规），类似来电/闹钟全屏 — **慎用**，需符合商店政策与用户预期。 |

### 4.2 系统闹钟 API（仅当产品要求「与系统时钟一致」）

| 方案 | 说明 |
|------|------|
| **`android_alarm_manager_plus` + `AlarmManager.setAlarmClock`** | 在系统闹钟列表中显示「下次响铃」，**对用户心智最像闹钟**；实现成本高于纯通知，需处理 **权限、Doze、各厂商差异**。 |
| **iOS**：仍主要用 **UNCalendarNotificationTrigger** 等本地通知；**无**与 Android 完全对等的「写入系统时钟」能力。 |

### 4.3 云端兜底（账号已登录、服务端有日程时）

| 方案 | 说明 |
|------|------|
| **FCM / APNs 推送** | 服务端在提醒时刻发 **data message** 或高优先级通知，客户端展示；适用于 **多设备、离线同步** 场景。成本：后端任务调度（Cron / 队列）+ 合规与隐私。 |
| **与现有 `user_data` 同步结合** | 若日程已上云，可由 **服务端定时任务** 在 `trigger_time` 触发推送，作为 **本地通知的补充**（尤其本地被 kill 时）。 |

### 4.4 体验与合规

- **勿扰 / 专注模式**：通知仍可能被系统抑制；可提供 **设置项**：是否使用 **时间敏感通知（iOS）**、Android **绕过 DND**（若系统允许）。
- **权限引导**：首次进入日程或首次设置提醒时，引导打开 **通知、精确闹钟、电池白名单**。

---

## 5. 建议落地顺序（完善「到点提醒」）

1. **（高）** 保证 **`ScheduleEventStore.ensureLoaded()` 之后** 始终 **`rescheduleAll`**（当前 `main.dart` 已在启动顺序中处理，见注释）。
2. **（高）** 验证 **重启手机** 后提醒是否仍在；若否，增加 **开机/冷启动** 路径的 **`rescheduleAll`**（或升级/配置 `flutter_local_notifications` 的 Android 恢复逻辑）。
3. **（中）** 实现 **通知点击** → 打开日程页/详情（`onDidReceiveNotificationResponse` + 全局路由）。
4. **（中）** 增加 **`WorkManager` 定时补登记**（例如每 6～12 小时），作为国产机省电策略的兜底。
5. **（按需）** 产品要求「强提醒」时评估 **全屏意图** 或 **`setAlarmClock`**；**（按需）** 登录用户增加 **服务端推送兜底**。

---

## 6. 代码与配置索引

| 内容 | 位置 |
|------|------|
| 提醒服务（调度、通知、平台细节） | `lib/features/schedule/schedule_reminder_service.dart` |
| 日程变更后触发重排 | `lib/features/schedule/schedule_event_store.dart` |
| 前台弹窗与「稍后提醒」 | `lib/app/main_shell.dart` |
| 启动初始化顺序 | `lib/main.dart` |
| Android 权限 | `android/app/src/main/AndroidManifest.xml` |
| 依赖 | `pubspec.yaml` → `flutter_local_notifications` 等 |

---

## 7. 版本与维护

- 本文描述基于当前仓库实现；**插件大版本升级**（如 `flutter_local_notifications`）时，请对照官方迁移说明核对 **精确闹钟、通知渠道、Windows 初始化** 等是否变更。
- 若后续实现 **FCM / 全屏意图 / WorkManager**，建议在本文追加 **小节** 并指向具体 PR 或提交说明。
