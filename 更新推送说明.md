# 希比 HIBI · 升级推送与版本更新

本文说明：**管理后台如何配置各端安装包与版本号**，以及 **客户端如何对比版本、在「我的」页提示更新、下载并触发安装**；并列出各平台差异与注意事项。

---

## 1. 目标与流程概览

1. 运营在 **ECS 管理后台 → 系统配置** 中填写 **最新版本号**、**更新特征**、**Windows / iOS / Android / Linux** 的安装包或商店链接。
2. 客户端启动后（进入主界面前权限流程之后）**静默请求** `GET /api/app/update_manifest`（无需登录），将服务端版本与当前 `package_info` 版本做 **语义化版本比较**。
3. 若服务端版本 **严格大于** 本机版本，则在 **「我的」** 页顶部功能列表出现 **「版本更新」**（带圆点提示），副标题展示新版本号。
4. 用户进入 **版本更新** 页可阅读 **更新特征**，点击 **立即下载并安装**（或 iOS：**前往 App Store 更新**）：
   - **Android / Windows / Linux**：在应用内下载安装包到临时目录，再调用系统打开安装程序（Android 会请求 **安装未知应用** 权限）。
   - **iOS**：系统 **不允许** 从应用内直接安装 ipa，通常配置 **App Store / TestFlight** 链接，由系统浏览器或 App Store 打开。

---

## 2. 后端与数据存储

| 项目 | 说明 |
|------|------|
| 存储 | SQLite `legal_documents` 表，`doc_key = app_update_manifest`，内容为 **JSON 字符串**。 |
| 公开接口 | `GET /api/app/update_manifest` — 返回 JSON：`latest_version`、`release_notes`、`urls`（四端）、`updated_at`。 |
| 管理接口 | `GET /api/admin/settings/app_update_manifest` — 后台读取回填；`POST /api/admin/settings/app_update_manifest` — 保存（需管理员 Cookie）。 |
| 初始化 | 首次部署时写入空配置（版本号与各 URL 可为空）。 |

**版本号格式建议**：与 `pubspec.yaml` 的 `version:` 主版本段一致，例如 `2.2.0`（可与 `2.2.0+19` 中的 `2.2.0` 比较）。比较规则为按段数值比较（`1.10.0` > `1.9.0`）。

---

## 3. 管理后台操作

1. 登录 `https://你的域名/admin/login`，进入 **系统配置** 选项卡。
2. 找到 **「升级推送地址（App 版本更新）」** 卡片。
3. 填写：
   - **最新版本号**：须 **大于** 已发布客户端的版本，用户端才会出现更新入口。
   - **更新特征**：支持多行，建议 `-` 列表，用户将在「版本更新」页完整看到。
   - **各端 URL**：
     - **Windows / Android / Linux**：可以继续填 **HTTPS 直链**；也可以在后台直接 **上传安装包**，后台会自动生成可下载链接并回填到输入框。
     - **iOS**：仍填写 App Store 或 TestFlight 的链接（系统限制，客户端不会下载 ipa）。
4. 点击 **保存升级配置**。

**iOS 说明**：填写 App Store 或 TestFlight 的 **https 链接** 即可，客户端不会下载 ipa，而是 **用系统打开该链接**。

---

## 4. 客户端行为摘要

| 文件 | 职责 |
|------|------|
| `lib/app/initial_app_loader.dart` | 权限申请后 `unawaited(AppUpdateService.instance.checkSilently())`，不阻塞进入主界面。 |
| `lib/features/profile/services/app_update_service.dart` | 拉取清单、版本比较、`downloadAndOpenInstaller` / `openExternalUrl`。 |
| `lib/features/profile/profile_page.dart` | 有更新时展示「版本更新」入口。 |
| `lib/features/profile/app_update_page.dart` | 更新说明与操作按钮。 |

**基址**：使用 `ApiConfig.authApiBaseUrl` 拼接 `/api/app/update_manifest`（与账户服务同域即可）。

**权限**：

- Android：已在 `AndroidManifest.xml` 声明 `REQUEST_INSTALL_PACKAGES`；实际在安装前通过 `permission_handler` 请求。
- 未在冷启动单独弹窗申请安装权限，避免打扰；仅在用户点击「立即下载并安装」后触发。

---

## 5. 各平台差异与体验建议

| 平台 | 推荐配置 | 客户端行为 |
|------|-----------|--------------|
| **Windows** | `.exe` / 安装包直链 | 下载到临时目录后 `OpenFilex`/`start` 拉起安装程序。 |
| **Android** | `.apk` 直链（或企业分发页，需可直链下载） | 下载后请求安装权限并调起系统安装界面。 |
| **iOS** | App Store / TestFlight URL | **仅打开链接**，不执行应用内安装。 |
| **Linux** | `.AppImage` / `.deb` 等可执行打开方式 | 下载后尝试用桌面环境打开；若失败可提示用户手动安装。 |

**大文件与弱网**：当前实现为单次 `http.get` 下载，超大包建议后续改为分块进度条或引导用户使用浏览器下载（可在更新说明中提示）。

**安全**：请仅使用可信 CDN/对象存储链接；必要时为下载链接配置短期签名 URL（需自行在运营侧轮换后台配置）。

---

## 6. 故障排查

| 现象 | 可能原因 |
|------|-----------|
| 客户端始终无「版本更新」 | 后台版本号未大于客户端；或 `authApiBaseUrl` 未配置/网络失败；或清单为空。 |
| Android 安装无反应 | 未授予「安装未知应用」；或 APK 与签名不一致导致系统拒绝（需用户卸载旧包等，属系统策略）。 |
| iOS 无反应 | 链接非 https 或被系统拦截；需在 Safari 中可打开的商店链接。 |
| Windows 无反应 | 被杀软拦截临时目录 exe；或链接非直链返回 HTML。 |

---

## 7. 与隐私、登录的关系

- 升级检查 **不依赖登录**，便于所有用户及时获知版本。
- 不采集额外个人信息；仅发起对自家 ECS 的公开 GET。

---

## 8. 后续可扩展方向

- 在管理端增加 **最低支持版本**（`min_supported_version`），低于则强制提示升级或限制部分功能。
- 客户端增加 **忽略本版本** 本地记录，减少重复打扰。
- 下载过程显示 **进度条** 与断点续传（大安装包场景）。
