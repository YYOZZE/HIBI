# 安装包打包说明

**用户向更新说明**（每个版本改了什么）见 **`release\版本管理.md`**（唯一权威文件，持续更新；版本子目录不再各存副本）。本文面向维护者：如何打出 **Windows Setup** 与 **签名的 Android APK**，以及交付目录与命名约定。

---

## 一、环境与交付目录（当前惯例）

| 项 | 路径 / 说明 |
|----|-------------|
| 项目根目录 | `I:\TSINGDIGITAL\HIBI-2023` |
| 交付目录 | 项目根目录下 `release\`（与 `windows/Hibi2024_setup.iss` 里 `OutputDir` 一致，按版本分子目录如 `release\V3.0.17\`） |
| Android 签名 | `android/key.properties`，`storeFile` 指向本机 JKS，例如 `Secret\hibi-2023\android_KEY\hibi_2024_key.jks` |
| Inno Setup | 命令行常用：`& "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe" "windows\Hibi2024_setup.iss"`；若安装在其他目录，改为实际 `ISCC.exe` |
| 应用内更新源 | GitHub 固定仓库 Release：`https://github.com/YYOZZE/HIBI`（标签 `HIBI-2023_v{版本号}`，附件 `Hibi2023_{版本号}.apk` / `Hibi2023_Setup_{版本号}.exe`） |

---

## 二、当前基线：V3.3.14（build 66）

打新包前请与仓库对齐下表，再执行下方构建步骤。

| 项 | 内容 |
|----|------|
| `pubspec.yaml` | `version: 3.3.14+66` |
| `lib/app/app_version.dart` | `kAppDisplayVersion = '3.3.14'`（设置「关于」与发版名一致） |
| `windows/Hibi2024_setup.iss` | `#define MyAppVersion "3.3.14"` |
| Windows 安装包文件名 | `Hibi2023_Setup_3.3.14.exe`（由 Inno `OutputBaseFilename` 生成） |
| Android APK 交付名 | `Hibi2023_3.3.14.apk`（由 `app-release.apk` 复制重命名） |
| 交付子目录 | `release\V3.3.14\`（仅安装包与发行说明提取件；版本记录统一维护在 `release\版本管理.md`） |
| Windows 图标 | `python tools/make_windows_app_icon.py` → `windows/runner/resources/app_icon.ico`（轻度圆角方块 squircle；勿用 flutter_launcher_icons 覆盖） |
| Android 图标 | `python tools/process_logo_2026.py` 后 `dart run flutter_launcher_icons`（`windows.generate: false`，勿覆盖 ICO） |

### 构建步骤

1. 项目根目录执行 `flutter pub get`；换 logo 时先跑 `python tools/process_logo_2026.py` 与 `python tools/make_windows_app_icon.py`，再 `dart run flutter_launcher_icons`
2. **Android**：`flutter build apk --release`  
   将 `build\app\outputs\flutter-apk\app-release.apk` 复制到交付子目录并重命名为 **`Hibi2023_3.3.14.apk`**
3. **Windows**：`flutter build windows --release`（产物在 `build\windows\x64\runner\Release\`）
4. **Setup**：执行 `ISCC.exe` 编译 `windows\Hibi2024_setup.iss`，生成 **`Hibi2023_Setup_3.3.14.exe`** 到交付子目录  
5. 更新 **`release\版本管理.md`** 中对应版本章节，并提取该节存为 `release_notes_v{版本}.md` 备用
6. **发布 GitHub Release**：在 `YYOZZE/HIBI` 新建标签 `HIBI-2023_v3.3.14` 的 Release，正文使用当版简述（`release_notes_v{版本}.md`），上传上述 APK 与 Setup.exe（应用内更新检查依赖此 Release）；可用 gh CLI 一键完成，详见 `release\GitHub发行与应用更新原理.md`

一键 Windows 安装包（仅 build + ISCC、不做后处理）：`tools\build_windows_installer.ps1`（需先本地安装 Inno Setup 6）。

更细约定与排障可对照 **`PACKAGING_RELEASE.md`** 第 4.2 节。

---

## 三、Windows Setup（通用说明）

1. **先** `flutter build windows --release`。
2. `windows/Hibi2024_setup.iss` 中 **`SourceDir`** 指向上述 `Release` 目录；**不要**对生成的 `Setup.exe` 再做 Resource Hacker / rcedit 等后处理，否则会损坏安装包（见 `README.md` 打包说明）。
3. 用 Inno Setup 编译 `.iss` 得到单一 `Setup.exe`。

---

## 四、Android APK（通用说明）

```bash
flutter build apk --release
```

签名由 `android/key.properties` 配置；将 `app-release.apk` 按当次版本重命名（如 **`Hibi2023_x.y.z.apk`**）再放入交付目录。

---

## 五、命令行示例（本机路径）

```powershell
# Android
flutter build apk --release
Copy-Item build\app\outputs\flutter-apk\app-release.apk release\V3.3.14\Hibi2023_3.3.14.apk

# Windows + Setup
flutter build windows --release
& "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe" "windows\Hibi2024_setup.iss"
```

*后续若发新号，请同步更新本文「二、当前基线」表格与 `release\版本管理.md` 用户说明。*
