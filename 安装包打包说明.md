# 安装包打包说明

**用户向更新说明**（每个版本改了什么）见 **`release\版本管理.md`**（唯一权威文件，持续更新；版本子目录不再各存副本）。本文面向维护者：如何打出 **Windows Setup** 与 **签名的 Android APK**，以及交付目录与命名约定。

---

## 一、环境与交付目录（当前惯例）

| 项 | 路径 / 说明 |
|----|-------------|
| 项目根目录 | `I:\TSINGDIGITAL\HIBI-2023` |
| 交付目录 | 项目根目录下 `release\`（与 `windows/Hibi2024_setup.iss` 里 `OutputDir` 一致，按版本分子目录如 `release\V3.0.17\`） |
| Android 签名 | `android/key.properties`，`storeFile` 指向本机 JKS，例如 `Secret\hibi-2023\android_KEY\hibi_2024_key.jks` |
| Inno Setup | 命令行常用：`& "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe" "windows\Hibi2024_setup.iss"`；若安装在其他目录，改为实际 `ISCC.exe` |
| 应用内更新源 | GitHub 固定仓库 Release：`https://github.com/YYOZZE/HIBI`（标签 `HIBI-2023_v{版本号}`，附件 `Hibi2023_{版本号}.apk` / `Hibi2023_Setup_{版本号}.exe`） |

---

## 二、当前基线：V3.3.1（build 53）

打新包前请与仓库对齐下表，再执行下方构建步骤。

| 项 | 内容 |
|----|------|
| `pubspec.yaml` | `version: 3.3.1+53` |
| `lib/app/app_version.dart` | `kAppDisplayVersion = '3.3.1'`（设置「关于」与发版名一致） |
| `windows/Hibi2024_setup.iss` | `#define MyAppVersion "3.3.1"` |
| Windows 安装包文件名 | `Hibi2023_Setup_3.3.1.exe`（由 Inno `OutputBaseFilename` 生成） |
| Android APK 交付名 | `Hibi2023_3.3.1.apk`（由 `app-release.apk` 复制重命名） |
| 交付子目录 | `release\V3.3.1\`（仅安装包与发行说明提取件；版本记录统一维护在 `release\版本管理.md`） |

### 构建步骤

1. 项目根目录执行 `flutter pub get`
2. **Android**：`flutter build apk --release`  
   将 `build\app\outputs\flutter-apk\app-release.apk` 复制到交付子目录并重命名为 **`Hibi2023_3.3.1.apk`**
3. **Windows**：`flutter build windows --release`（产物在 `build\windows\x64\runner\Release\`）
4. **Setup**：执行 `ISCC.exe` 编译 `windows\Hibi2024_setup.iss`，生成 **`Hibi2023_Setup_3.3.1.exe`** 到交付子目录  
5. 更新 **`release\版本管理.md`** 中对应版本小节，并提取该节存为 `release_notes_v{版本}.md` 备用
6. **发布 GitHub Release**：在 `YYOZZE/HIBI` 新建标签 `HIBI-2023_v3.3.1` 的 Release，正文使用当版小节（`release_notes_v{版本}.md`），上传上述 APK 与 Setup.exe（应用内更新检查依赖此 Release）；可用 gh CLI 一键完成，详见 `release\GitHub发行与应用更新原理.md`

一键 Windows 安装包（仅 build + ISCC、不做后处理）：`tools\build_windows_installer.ps1`（需先本地安装 Inno Setup 6）。

更细约定与排障可对照 **`PACKAGING_RELEASE.md`** 第 4.2 节。

---

## 三、Windows Setup（通用说明）

1. **先** `flutter build windows --release`。
2. `windows/Hibi2024_setup.iss` 中 **`SourceDir`** 指向上述 `Release` 目录；**不要**对生成的 `Setup.exe` 再做 Resource Hacker / rcedit 等后处理，否则会损坏安装包（见 `README.md` 打包说明）。
3. 用 Inno Setup 编译 `.iss` 得到单一 `Setup.exe`。

---

## 四、Android APK（通用说明）

```bash
flutter build apk --release
```

签名由 `android/key.properties` 配置；将 `app-release.apk` 按当次版本重命名（如 **`Hibi2023_x.y.z.apk`**）再放入交付目录。

---

## 五、历史参考：v1.2.1 当时记录（路径已迁移为 `hibi-2023`，仅供对照）

以下为早期笔记留存，**路径、包名前缀多为旧版 `Hibi2024_*` 与 `hibi-2024`，请勿直接照抄**，以本文「一、二」节为准。

---

### v1.2.1 时曾记录

版本号：**1.2.1**（build 4）  
曾用输出目录：**`C:\Users\a1306\Desktop\hibi-2024\LVvaovaoZ100B`**（已废弃，现用 `hibi-2023`）

#### 曾完成的更新（快照）

- **版本管理.md**：已增加 V1.2.1 版本记录。
- **pubspec.yaml**：`version: 1.2.1+4`。
- **windows/Hibi2024_setup.iss**：`MyAppVersion "1.2.1"`，`OutputDir` 曾为上述目录。
- **Android 签名**：`android/key.properties`／JKS 曾指向  
  `C:\Users\a1306\Desktop\hibi-2024\android_KEY\hibi_2024_key.jks`。

#### Windows

```bat
"C:\Program Files (x86)\Inno Setup 6\ISCC.exe" windows\Hibi2024_setup.iss
```

曾生成：`...\Hibi2024_Setup_1.2.1.exe`

#### Android

曾将 `app-release.apk` 复制为 `...\Hibi2024_1.2.1.apk`。

#### 一键打包（旧示例）

```powershell
cd c:\ALI_Z14\.TSING_important\Tsingcoop_products\pd\jideshi_hibi
flutter build windows --release
& "$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe" "windows\Hibi2024_setup.iss"
```

---

*后续若发新号，请同步更新本文「二、当前基线」表格与 `release\版本管理.md` 用户说明。*
