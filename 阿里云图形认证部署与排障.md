# 阿里云图形认证部署与排障

**说明**：本文只涉及 **号码认证服务（PNVS）下的「图形认证」**（滑块/点选等人机校验，控制台「图形认证方案」、`ct4.js` / `initAlicom4`、二次校验 `captcha.alicaptcha.com/validate`）。当前产品登录/注册不再接入一键登录等其它号码认证子能力。

本文与当前代码、ECS 部署对齐，说明**后端链路**、**环境变量**、**客户端基址**及常见「没跑通」原因。官方图形认证服务端说明：<https://help.aliyun.com/zh/pnvs/developer-reference/graphical-authentication-server-integration>；H5 接入：<https://help.aliyun.com/zh/pnvs/developer-reference/integrate-the-sdk-with-h5-pages>

**控制台方案名称、方案编码（FG）、各端 appId/appKey** 与官方指引的对应关系见：[阿里图片认证.md](./阿里图片认证.md)。

---

## 1. 当前部署基址（示例）

- **服务地址**：`http://121.41.6.21:7861`（与 `lib/config/api_config.dart` 中 `authApiBaseUrl` / `assistantApiBaseUrl` 一致）
- 客户端所有图形认证相关 URL 均由该基址拼接，例如：
  - `GET {base}/api/auth/captcha_config?platform=web`
  - `GET {base}/api/auth/captcha/sdk.js`
  - `GET {base}/api/auth/captcha/embed?app_id=...&platform=web`（桌面端 WebView 加载）
  - `POST {base}/api/auth/captcha/challenge`
  - `POST {base}/api/auth/captcha/verify`

使用 **IP:端口** 或 **域名** 均可，只要客户端与浏览器能访问；若后续上 HTTPS，请同步改 `ApiConfig` 与控制台/证书策略。

---

## 2. 后端代码在做什么（跑通条件）

| 模块 | 作用 |
|------|------|
| `hibi_graph_captcha.py` | 按 `platform`（web/android/ios/harmony）读取 `app_id` / `app_key`（环境变量可覆盖默认），向 **阿里云校验接口** `POST https://captcha.alicaptcha.com/validate?captcha_id={app_id}` 提交二次校验。 |
| `api_only_app.py` | 提供 `captcha_config`、`sdk.js`、`embed`、challenge、verify；登录/注册在**后台开关开启**且**已配置**时，校验 `captcha_challenge_id` 对应的「已验证挑战」或回退为直接传图形 token 调 `verify`。 |

**必须同时满足：**

1. **后台「图形认证」开关为开启**（`legal_documents` 中 `graph_captcha_enabled` 为 `true`；新库默认已写入）。关闭时：`captcha_config` 的 `configured` 会被视为 false，challenge/verify 会拒绝，登录注册不再校验图形。
2. **`hibi_graph_captcha.is_configured(platform)` 为真**：对应端的 `app_id`、`app_key` 非空（代码内有默认，生产建议用环境变量覆盖）。
3. **ECS 能访问公网**：服务端校验时请求 `https://captcha.alicaptcha.com/...`。若安全组/出站策略禁止 HTTPS 出站，用户在前端「滑块成功」后，**登录仍会失败**（`verify` 返回异常）。
4. **用户设备能访问阿里云资源**：H5/JS 需加载 `captcha.alicaptcha.com`、`static.alicaptcha.com` 等。若企业网络或本机防火墙拦截，会出现长时间加载、初始化超时（embed 页内约 35s 超时提示）。

---

## 3. 环境变量（可选覆盖）

在 ECS `.env` 中可按端覆盖（与 `hibi_graph_captcha.py` 一致）：

- `GRAPH_CAPTCHA_WEB_APP_ID` / `GRAPH_CAPTCHA_WEB_APP_KEY`
- `GRAPH_CAPTCHA_ANDROID_*`、`GRAPH_CAPTCHA_IOS_*`、`GRAPH_CAPTCHA_HARMONY_*`
- 通用：`GRAPH_CAPTCHA_APP_ID`、`GRAPH_CAPTCHA_APP_KEY`
- `GRAPH_CAPTCHA_SDK_URL`：若设置，会覆盖 `captcha_config` 返回的 `sdk_url`（否则后端拼成 `{base}/api/auth/captcha/sdk.js`）

**注意**：控制台里申请的 **Web 应用 app_id** 必须与「桌面/WebView 使用的 platform」一致（通常为 `web`），否则阿里云侧校验会失败。

---

## 4. 接口自检（在浏览器或本机终端）

将下面命令中的基址换成你的实际地址（示例：`http://121.41.6.21:7861`）。PowerShell 下请用 `curl.exe`，避免与 `Invoke-WebRequest` 别名冲突。

```text
# 1）配置是否就绪、后台是否要求图形认证
curl.exe "http://121.41.6.21:7861/api/auth/captcha_config?platform=web"

# 2）SDK 是否由服务端提供（应输出 200）
curl.exe -sS -o NUL -w "%{http_code}" "http://121.41.6.21:7861/api/auth/captcha/sdk.js"

# 3）嵌入页是否可打开（应输出 200；用浏览器打开同一 URL 可看滑块）
curl.exe -sS -o NUL -w "%{http_code}" "http://121.41.6.21:7861/api/auth/captcha/embed?app_id=你的Web端AppId&platform=web"

# 4）创建挑战（JSON 在 PowerShell 中需注意引号转义，或改用 Postman）
curl.exe -sS -X POST "http://121.41.6.21:7861/api/auth/captcha/challenge" -H "Content-Type: application/json" -d "{\"platform\":\"web\"}"
```

期望 `captcha_config` 中：`configured: true`，且 `admin_graph_captcha_enabled: true`（若你希望强制图形认证）。

---

## 5. 登录/注册时的两种校验路径

代码逻辑（`api_only_app.py`）：

- 若请求里带 **`captcha_challenge_id`**：先检查该 challenge 是否已通过 `POST /api/auth/captcha/verify` 标记为已验证（一次性消费）。
- 若未带 challenge：则直接用请求体中的 `lot_number`、`captcha_output`、`pass_token`、`gen_time` 等调用 **`verify` → 阿里云**。

桌面端若走 WebView + challenge 流程，须保证：**先 challenge → 用户完成 → verify → 再带 challenge_id 调登录**。

---

## 6. 常见故障与排查

| 现象 | 可能原因 |
|------|----------|
| `captcha_config` 里 `configured: false` | 后台关闭了图形认证，或 `app_id`/`app_key` 为空；或仅关了后台开关导致逻辑上「视为未配置」。 |
| 滑块一直转圈 / 提示初始化超时 | 客户端无法访问 `captcha.alicaptcha.com` / `static.alicaptcha.com`；或 WebView 被策略限制；或网络极差。 |
| 加载阶段报 **`captcha_id paused`** | **`-50105`（[官方错误码表](https://help.aliyun.com/zh/pnvs/developer-reference/graphical-authentication-server-integration)）**：阿里云认定该 **appId 暂时不可用**。图形认证方案管理页**通常没有**明显「暂停/启用」按钮；需核对 **号码认证产品线、欠费/配额、方案是否删除重建、H5 方案是否选 H5 接入端** 等，必要时 **工单** 报 appId + FG 方案编码。详见 [阿里图片认证.md](./阿里图片认证.md) 对应小节。 |
| **Android/iOS 弹窗白屏或收不到结果** | 确认已 `flutter pub get` 引入 `webview_flutter`；embed 须通过 **`HibiCaptcha`** 通道回传（与 Windows WebView2 的 `chrome.webview` 不同）。 |
| **Android 弹窗「网页无法打开」`net::ERR_CLEARTEXT_NOT_PERMITTED`** | 后端为 **HTTP** 时，Android 9+ 默认禁止明文。本仓库已配置 `res/xml/network_security_config.xml` 并在 `AndroidManifest` 引用；仍报错时核对 embed 是否为 `http://` 且已重装应用。 |
| **iOS 图形页空白 / ATS 拦截** | 若基址为 HTTP，须在 `ios/Runner/Info.plist` 配置 `NSAppTransportSecurity`（本仓库已加）；**上架 App Store** 时若被问询，说明为连接用户/企业自建 HTTP 服务，长期建议后端 **HTTPS**。 |
| **鸿蒙（独立 OH Flutter 工程）** | 与 Android 类似：在模块 **网络安全策略** 中允许访问所用 HTTP 源，或为后端配置 **HTTPS**；`platform` 仍报 `harmony` 时见 `auth_captcha_platform.dart` 注释。 |
| 滑块成功但登录提示「图形认证校验异常」或 HTTP 非 200 | ECS **无法访问** `captcha.alicaptcha.com`（出站、DNS、代理）；或 `app_key` 与控制台不一致、平台不匹配。 |
| `challenge` 或 `verify` 返回 403 | 后台 **关闭**了图形认证开关。 |
| `图形认证状态已过期` | challenge 超时（默认约 5 分钟）或已使用过，需重新 challenge。 |

---

## 7. 与后台管理页的关系

- 后台路径：`/admin/customers` → **系统配置** → **图形认证开关**。
- 更完整的后台说明见：[后台管理页说明.md](./后台管理页说明.md) 中「图形认证与 App 行为」及 `graph_captcha_enabled` 接口。

---

## 8. 代码索引

- 服务端校验：`backend_jideshi_hibi_app/hibi_graph_captcha.py`
- 路由与 embed：`backend_jideshi_hibi_app/api_only_app.py`（`/api/auth/captcha_*`）
- 客户端基址：`lib/config/api_config.dart` → `authApiBaseUrl`
- 端类型：`lib/features/auth/services/auth_captcha_platform.dart`；移动 embed：`lib/features/auth/services/graph_captcha_service_mobile.dart`
