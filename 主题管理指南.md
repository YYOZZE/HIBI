# 主题管理指南（后台主题代码 / JSON Token）

本文用于指导在后台「主题管理」中新建主题时，**主题代码（style_code）**应该怎么写，以及各参数含义。

> 说明  
> - 旧版内置主题（`hibi/dark/light/...`）仍由 App 内置 `ThemeData` 渲染；后台的 `style_code` 仅做存档或后续扩展。  
> - **第二阶段 token 主题**：后台新建的自定义主题，可直接使用 JSON token 驱动 App 生成主题（无需绑定内置主题）。  

---

## 1. 最小可用示例

```json
{
  "name": "Light Pro 2026",
  "mode": "light",
  "colors": {
    "primary": "#2563EB",
    "secondary": "#14B8A6",
    "background": "#F6F7FB",
    "surface": "#FFFFFF",
    "surfaceAlt": "#F1F3FA",
    "outline": "#1A111827",
    "onBackground": "#0F172A",
    "onSurface": "#111827",
    "onSurfaceVariant": "#475569",
    "success": "#16A34A",
    "warning": "#F59E0B",
    "error": "#DC2626"
  },
  "shape": { "radiusSm": 12, "radiusMd": 16, "radiusLg": 22 },
  "typography": { "baseSize": 14, "titleScale": 1.06 },
  "effects": { "elevation": 2, "cardOpacity": 0.78, "glassBlur": 12 }
}
```

---

## 2. 顶层字段

- **`name`**：主题名称（可选）。用于调试/存档。
- **`mode`**：`"light"` 或 `"dark"`（可选，默认 `"light"`）。用于决定亮/暗整体基调。
- **`colors`**：颜色系统（必填）。
- **`shape`**：圆角体系（可选）。
- **`typography`**：字号/标题比例（可选）。
- **`effects`**：卡片透明度、阴影高度等（可选）。

---

## 3. `colors`（颜色系统）

支持以下 key（建议都填）：

- **`primary`**：主色（按钮、高亮、选中态）
- **`secondary`**：辅助强调色（标签、次要高亮）
- **`background`**：页面背景色
- **`surface`**：卡片/对话框等表面色
- **`surfaceAlt`**：更高层级或输入框底色等（“次表面”）
- **`outline`**：描边/分隔线色（可用带透明度的色）
- **`onBackground`**：背景上的主要文字颜色
- **`onSurface`**：表面上的主要文字颜色
- **`onSurfaceVariant`**：表面上的次要文字颜色（副标题/提示）
- **`success`**：成功提示色
- **`warning`**：警告提示色
- **`error`**：错误提示色

### 颜色格式

支持三种格式：

- **`"#RRGGBB"`**：自动视为不透明（alpha=FF）
- **`"#AARRGGBB"`**：含透明度（推荐用于 `outline` 等）
- **整数**：例如 `4294967295`（等价于 `0xFFFFFFFF`，不推荐人工填写）

---

## 4. `shape`（圆角体系）

- **`radiusSm`**：小圆角（输入框/小卡片边缘）
- **`radiusMd`**：中圆角（按钮/卡片）
- **`radiusLg`**：大圆角（对话框/大卡片）

单位为逻辑像素（dp）。推荐范围：

- `10 ~ 14`（现代、干净）
- `16 ~ 22`（偏 2026 流行的更“柔和”卡片风）

---

## 5. `typography`（排版）

- **`baseSize`**：正文基准字号（默认 14）
- **`titleScale`**：标题缩放比例（默认 1.05）
- **`fontFamily`**：字体（可选；不填则用系统字体）

---

## 6. `effects`（效果）

- **`elevation`**：卡片阴影高度（默认 2；建议 1~4）
- **`cardOpacity`**：卡片透明度（0~1，默认 0.72）
- **`glassBlur`**：毛玻璃模糊半径（当前版本用于预留/存档，后续可扩展）

---

## 7. 后台新建主题的建议流程

- **步骤 1**：在后台「新增主题（自定义）」中填写名字/说明
- **步骤 2**：把上面的 JSON token 作为「主题代码」
- **步骤 3**：设置 **启用**、**显示（不隐藏）**
- **步骤 4**：App 下次启动/回前台会静默拉取主题策略并更新可选主题列表

> 提醒：若你希望“节日当天自动切换”，请在「日历排期」里设置该自定义主题（即使隐藏也能按排期自动生效）。

