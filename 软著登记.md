# 软件著作权登记 · 环境与说明笔记

依据本项目 **jideshi_hibi**（Flutter 多端客户端，后端 Python 部署于云服务器）整理。**除第 11 项「软件的主要功能」外，每点一段话；每段正文不超过 50 个汉字（含标点），必要处保留英文缩写；第 11 项为登记表用展开说明，约八百字。**

---

## 1. 开发的硬件环境

x86-64 个人电脑，双核以上处理器，建议 16GB 内存与固态硬盘，用于编译与制作安装包。

---

## 2. 运行的硬件环境

用户使用 Windows 电脑或安卓手机、平板；配套业务服务运行于标准 Linux 云主机。

---

## 3. 开发该软件的操作系统

客户端开发与发行打包主要在 **Windows 10 / Windows 11（64 位）** 下完成。

---

## 4. 软件开发环境 / 开发工具

**Flutter**、**Dart**、安卓构建链、**Visual Studio**、**Inno Setup**、**Git**；服务端 **Python 3** 与 IDE。

---

## 5. 该软件的运行平台 / 操作系统

客户端：**Windows（64 位）** 与 **Android（约 API 23 起）**；后端接口：**Linux**（如云 ECS）。

---

## 6. 软件运行支撑环境 / 支持软件

Windows 依赖系统运行库；Android 依赖系统运行时及网络、存储等权限；联网需 HTTPS 与后端服务。

---

## 7. 编程语言

**Dart** 为主；**Kotlin**（安卓配置与嵌入）、**C++**（Windows 原生层）、**Python**（后端）。

---

## 8. 源程序量

Dart 约 1.7 万行、Python 约 3600 行（`lib` 与 `backend_jideshi_hibi_app` 统计，不含第三方依赖与 build 目录）。

---

## 9. 开发目的

在 Windows 与 Android 上统一提供日程、对话助理、思维白板、局域网传输、登录后云同步及增值订阅，提升跨端效率。

---

## 10. 面向领域 / 行业

通用办公与个人信息管理、移动协同；适用日程规划、项目白板、多端数据同步及向量语义检索与语音转写类场景。

---

## 11. 软件的主要功能

希比 HIBI 客户端面向 Windows（64 位）与 Android，以底部导航「思维、日程、助理、传输、我的」五大模块组织功能。思维模块支持多项目列表与无限画布：在画布中可添加与移动方块、连线、缩放平移，便于项目拆解与知识整理；便签与复习等辅助能力帮助巩固要点；业务数据按账号隔离存放于本地目录，登录后通过令牌访问云端同步接口，对思维节点与画布内容进行拉取、推送与分类合并，并在界面中体现同步与订阅状态，可引导至增值页了解权益。日程模块提供事件列表、月历及新建编辑页，事件本地持久化并注册系统或应用内到点提醒，弹出「日程提醒」对话框支持「稍后再次提醒」与「我知道了」；日程可与思维方块上的提醒信息关联，形成从规划到时间的闭环。对话助理模块支持多套会话配置与历史记录管理，消息以气泡形式展示，支持长按复制、删除与批量多选；用户可文字输入，亦可按住说话、松手结束，由后端以 WebSocket 提供流式语音转写为文本。服务端将用户输入、上下文与会话约束做特征抽取并映射为数据向量，在向量空间内结合语义相似度、先验规则与业务边界做检索与应答路径求解，将候选输出以 HTTPS 流式文本分片回传并拼接呈现；在授权范围内可调用封装好的业务工具接口，对日程、思维等结构化数据进行查询与回写。传输模块在局域网内发现周边设备，发起文件或文本传输，对端可接受或拒绝，支持保存路径配置与历史记录，适合在无公网大文件中转场景下就近互传。我的模块涵盖个人资料编辑、多套主题与界面主题扩展、设置项、隐私与用户协议查阅、登录注册及账号本地目录切换、云端同步与登出清理策略，以及订阅与支付相关流程入口。全局采用 Material 风格与毛玻璃背景等多端一致视觉；应用自后台回到前台时自动执行云端拉取，保障多设备数据一致。未登录时数据使用本地默认隔离空间，登录后切换至按账号划分的业务目录与服务端账户绑定。后端以 Python FastAPI 等实现鉴权、分类同步与增值接口；主壳使用 IndexedStack 保留各标签状态，避免反复进入页面时丢失编辑上下文。Windows 可通过安装程序分发，Android 依清单声明网络、存储与通知等权限并完成运行时授权。

---

## 12. 软件的技术特点

Flutter 单仓多端 Material UI；HTTP 先拉后推与分类合并；FastAPI+SQLite；流式 HTTPS 对话与 WebSocket 流式语音转写（ASR）；本地通知、多主题与 Inno 安装包分发。

---

## 鉴别材料 PDF（源代码节选）

项目根目录 **`hibi-2023软著登记鉴别材料.pdf`**：由 `tools/gen_hibi_softcopy_pdf.py` 从 **`lib/`** 下全部 `.dart` 按路径排序拼接生成；**前 30 页 + 后 30 页**，**每页 55 行**；首页为提取说明。重新生成命令：

```powershell
cd c:\ALI_Z14\.TSING_important\Tsingcoop_products\pd\jideshi_hibi
python tools\gen_hibi_softcopy_pdf.py
```

依赖：`fpdf2`（脚本可自动 `pip install`）、Windows 下仿宋等中文字体。

---

## 文档鉴别材料 PDF（功能与界面说明）

项目根目录 **`hibi-2023软著登记文档鉴别材料.pdf`**：由 `tools/gen_hibi_doc_softcopy_pdf.py` 生成，**与 `lib/` 及后端目录对应**，阐述功能、界面结构、运行环境与典型使用流程；**页数上限 60**（脚本内 `MAX_PAGES`）。重新生成命令：

```powershell
cd c:\ALI_Z14\.TSING_important\Tsingcoop_products\pd\jideshi_hibi
python tools\gen_hibi_doc_softcopy_pdf.py
```

依赖与同目录代码鉴别脚本一致：`fpdf2`、系统中文字体。

---

*登记表若只要纯文本，可复制各节第一段正文并去掉 Markdown 格式。*
