# HIBI-2023 v4.0.2

**发布日期**：2026-08-11  
**版本号**：4.0.2（build 68）

## 本版重点

### 助理

- **快捷工具栏**：聊天底栏新增「帮我写作 / 图像生成 / 视频生成」，发送后自动退出工具模式。
- **帮我写作**：按文档模式生成完整 Markdown，落盘到本地，气泡内以文档卡片预览并可打开。
- **生成物目录**：设置中可配置「智能体生成物 · 默认存储路径」（默认 HIBI_Generated）；聊天「更多」可查看当前路径。
- **图像生成**：对接支持 OpenAI 兼容文生图接口的提供商时可直接出图并保存；不支持时回退为「图像提示词」文档。
- **视频生成**：当前交付分镜/脚本文档（完整视频 API 待后续接入）。
- **智能体配置**：选用提供商立即生效；显示/隐藏 API Key 时正确还原明文，避免误显占位符。

### 白板

- 方块仅在**已选中且非编辑**时可拖动，减少选字/复制时的误拖。
- 方块正文改为**右侧滚动条**滚动（文字区禁滚轮误滚）。
- 描边加粗并提高对比；右下角缩放手柄尺寸折中，命中区与可视角标对齐。

## 升级与兼容

- 数据格式与既有版本兼容。
- 生成物默认写入本机「智能体生成物」目录，可在设置中更改或恢复默认。

## 下载

- Windows：Hibi2023_Setup_4.0.2.exe
- Android：Hibi2023_4.0.2.apk
