# 本地账户数据结构说明

> 面向换版本、重装、或「打开 App 后像空账号」时：把本机 `hibi_accounts` 下旧账号目录的数据，安全合并进**当前正在使用的本地账号**（多为 `local/`）。  
> V4.0.1 默认绕过 GitHub 门禁说明见 [`AUTH_GITHUB_SEALED.md`](lib/features/auth/AUTH_GITHUB_SEALED.md)。

---

## 1. 数据根路径（Windows）

业务数据不在安装目录，而在用户文档目录：

```text
%USERPROFILE%\Documents\hibi_accounts\
```

常见绝对路径示例：

```text
C:\Users\<用户名>\Documents\hibi_accounts\
```

### 如何确认路径

1. 资源管理器地址栏粘贴：`%USERPROFILE%\Documents\hibi_accounts` 回车。  
2. 若不确定盘符/用户名：在本机搜索文件名 **`hibi_mind_nodes.json`**，其父目录的上一级即为某个账号目录，再往上一层就是 `hibi_accounts`。  
3. PowerShell 快速定位：

```powershell
Get-ChildItem -Path "$env:USERPROFILE\Documents" -Filter "hibi_mind_nodes.json" -Recurse -ErrorAction SilentlyContinue |
  Select-Object -First 5 FullName
```

> 代码入口：`AccountStoragePaths`（`lib/features/auth/services/account_storage_paths.dart`），根目录 = `path_provider` 的 Application Documents + `/hibi_accounts`。

---

## 2. 目录结构

```text
Documents/
├── hibi_accounts/                          ← 多账号根（活动读写在此）
│   ├── local/                              ← 未登录 / 本地账号（V4 绕过后最常见）
│   ├── gh_<GitHub数字id>/                  ← GitHub 账号，如 gh_154501426
│   ├── <sanitize(旧userId)>/               ← 历史手机号/邮箱等登录留下的目录
│   └── …
├── hibi_accounts_backup_YYYYMMDD_HHMM/     ← 人工备份（SOP 会建）
└── （极旧版本可能在 Documents 根下直接放 JSON，见 §2.3）
```

### 2.1 账号目录名规则

| 类型 | 目录名 | 说明 |
|------|--------|------|
| 本地账号 | `local` | **固定**。个人中心里的 `local:…` id **不会**变成子目录名 |
| GitHub | `gh_<id>` 等 | `sanitize(userId)`：去掉 `/\:*?"<>|`，过长截到 80 字符 |
| 历史账号 | 32 位 hex 等 | 旧登录体系遗留；仍按独立目录保留，切换账号不删盘 |

### 2.2 每个账号目录内关键文件

```text
<账号键>/
├── hibi_mind_nodes.json          ← 思维节点列表（JSON 数组）
├── hibi_schedule_events.json     ← 日程事件列表（JSON 数组）
└── hibi_assistant/
    ├── assistant_agents.json     ← 智能体列表（JSON 数组）
    ├── messages_<agentId>.json   ← 该智能体消息列表（JSON 数组）
    └── chat_files/               ← 对话附件（按 agentId/messageId 分子目录；可能不存在）
        └── <agentId>/<messageId>/…
```

| 文件/目录 | 用途 | 合并时 |
|-----------|------|--------|
| `hibi_mind_nodes.json` | 思维项目与白板 | 按 `id`，`updatedAt` 新者胜（LWW） |
| `hibi_schedule_events.json` | 日程 | 按 `id`，mtime LWW（`updatedAt` → `deletedAt` → `startTime` → `endTime`） |
| `assistant_agents.json` | 智能体配置 | 同 `id` **保留 target**；源独有 id 追加 |
| `messages_*.json` | 聊天记录 | 按 `role|timestamp|content` 去重后追加 |
| `chat_files/` | 附件落盘 | target 缺失则从源拷贝；已存在不覆盖 |

### 2.3 其它可能存在的内容

- **Documents 根下旧文件**（未进 `hibi_accounts`）：`hibi_mind_nodes.json`、`hibi_schedule_events.json`、`hibi_assistant/`。首次走 local 时，`migrateLegacyIntoLocalIfNeeded()` 会在 **local 侧对应文件尚不存在** 时拷入 `local/`。  
- 账号目录内一般只有上述三类；若还有零散 JSON，合并前先备份，再按「缺则补拷、勿整文件覆盖 target」处理。

---

## 3. 当前活跃账号如何对照

### 3.1 V4.0.1 绕过 GitHub 后

默认自动 `loginAsLocal()`，读写目录几乎总是：

```text
Documents\hibi_accounts\local\
```

详见 [`AUTH_GITHUB_SEALED.md`](lib/features/auth/AUTH_GITHUB_SEALED.md)（开关 `bypassGitHubLoginGate`）。

### 3.2 从 App「个人中心」对照磁盘

打开 **个人中心** 看副标题：

| 个人中心显示 | 磁盘目录 |
|--------------|----------|
| `本地账号` + `local:<稳定id>`（如 `local:local_xxxx`） | **`hibi_accounts\local\`**（不是 `local_xxxx` 文件夹） |
| `@githubLogin`（GitHub） | **`hibi_accounts\<sanitize(userId)>\`**，常见 `gh_<数字id>` |
| 未登录（门禁未绕过时） | 同样落在 `local/` |

要点：本地会话的 `userId` / `phoneOrEmail` 用于局域网账号校验与展示；**落盘键由 `AccountStoragePaths.setActiveUser` 决定**——`isLocal == true` 时强制 `local`。

### 3.3 快速判断「现在该写哪个目录」

1. 先看个人中心是不是「本地账号」→ target 优先 `local`。  
2. 若是 GitHub → 在 `hibi_accounts` 下找与 `gh_…` / login 对应的目录。  
3. 启发式（离线排障）：存在 `local\` 且其中有实质 JSON → 当作当前本地 target；多个 `local*` 时选最近有数据、mtime 最新者（现行代码通常只有字面量 `local`）。

---

## 4. SOP：把旧账号数据合并到当前本地账号

适用于：换版本后进了空的 `local`，但同机还留着 `gh_*`、旧 hex 目录里的思维/日程/助理。

### 步骤 0 — 退出 App

完全退出希比（托盘/后台也退出），避免运行中覆盖刚合并的文件。

### 步骤 1 — 整树备份（必须）

PowerShell：

```powershell
$src = Join-Path $env:USERPROFILE "Documents\hibi_accounts"
$ts  = Get-Date -Format "yyyyMMdd_HHmm"
$dst = Join-Path $env:USERPROFILE "Documents\hibi_accounts_backup_$ts"
Copy-Item -Path $src -Destination $dst -Recurse -Force
Write-Host "BACKUP=$dst"
```

确认 `$dst` 存在且文件数量合理后再继续。

### 步骤 2 — 判定 target 与 sources

```powershell
Get-ChildItem (Join-Path $env:USERPROFILE "Documents\hibi_accounts") -Directory |
  Select-Object Name, LastWriteTime
```

- **target**：优先 `local`。  
- **sources**：同级其余目录（`gh_*`、hex 旧账号等）。  
- **不要删除**任何源目录，直到 App 内验证通过。

可用条数粗判「谁有数据」：

```powershell
python -c "import json; from pathlib import Path
root=Path.home()/'Documents'/'hibi_accounts'
for d in sorted(root.iterdir()):
  if not d.is_dir(): continue
  def n(p):
    try: return len(json.loads(p.read_text(encoding='utf-8')))
    except: return '-'
  print(d.name, 'mind=', n(d/'hibi_mind_nodes.json'), 'sched=', n(d/'hibi_schedule_events.json'))"
```

### 步骤 3 — 合并规则（与代码一致）

与仓库内实现对齐（**无独立 `AccountDataMergeService` 类**；逻辑在下列位置）：

| 组件 | 路径 | 作用 |
|------|------|------|
| `SyncMerge` | `lib/features/auth/services/sync_merge.dart` | mind / schedule LWW；assistant 合并 |
| `LocalAccountImportService` | `lib/features/auth/services/local_account_import_service.dart` | App 内「导入到**当前 GitHub**」；合并策略同下表 |
| 回归用例 | `test/account_data_merge_test.dart` | 策略说明与断言 |

| 数据类型 | 规则 |
|----------|------|
| 思维 | 按 `id`；同 id 取 `updatedAt` 较新者 |
| 日程 | 按 `id`；同 id 取 mtime 较新者 |
| Agents | 同 id **保留 target**；源独有 id 并入 |
| Messages | 拼接后按 `role\|timestamp\|content` 去重 |
| chat_files | 仅补拷 target 没有的文件/目录 |

> App 内设置页「从本机其他账号导入」目前要求 **GitHub + Star**（`githubOk`）。V4 绕过后长期停在本地账号时，请用本 SOP 离线合并进 `local/`，不要指望该弹窗指向 local。

### 步骤 4 — 执行合并（推荐 Python）

将下列脚本保存为例如 `%USERPROFILE%\Documents\_merge_hibi_to_local.py`，按需改 `ROOT` / `TARGET_NAME`，退出 App 后执行：`python _merge_hibi_to_local.py`。

```python
# -*- coding: utf-8 -*-
"""将 hibi_accounts 下除 target 外的账号合并进 local（对齐 SyncMerge）。"""
import json
import shutil
from pathlib import Path

ROOT = Path.home() / "Documents" / "hibi_accounts"
TARGET_NAME = "local"
TARGET = ROOT / TARGET_NAME


def load_json(path: Path):
    if not path.exists():
        return None
    with open(path, "r", encoding="utf-8") as f:
        return json.load(f)


def save_json(path: Path, data):
    path.parent.mkdir(parents=True, exist_ok=True)
    with open(path, "w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)
        f.write("\n")


def mtime_key(e: dict) -> str:
    for k in ("updatedAt", "deletedAt", "startTime", "endTime"):
        v = e.get(k)
        if v is not None and str(v).strip():
            return str(v)
    return ""


def merge_by_id_lww(a, b, key_fn):
    """a=target 侧先放入；同 id 用 key_fn 较大者。"""
    m = {}
    for src in (a or [], b or []):
        if not isinstance(src, list):
            continue
        for raw in src:
            if not isinstance(raw, dict):
                continue
            eid = str(raw.get("id") or "").strip()
            if not eid:
                continue
            old = m.get(eid)
            if old is None or key_fn(raw) >= key_fn(old):
                m[eid] = dict(raw)
    return list(m.values())


def dedupe_messages(lst):
    seen, out = set(), []
    for e in lst or []:
        if not isinstance(e, dict):
            continue
        k = f"{e.get('role','')}|{e.get('timestamp','')}|{e.get('content','')}"
        if k not in seen:
            seen.add(k)
            out.append(e)
    return out


def merge_agents(target_agents, source_agents):
    m = {}
    for raw in source_agents or []:
        if isinstance(raw, dict) and str(raw.get("id") or "").strip():
            m[str(raw["id"])] = dict(raw)
    for raw in target_agents or []:  # target 覆盖同 id
        if isinstance(raw, dict) and str(raw.get("id") or "").strip():
            m[str(raw["id"])] = dict(raw)
    return list(m.values())


def merge_account(src: Path, dst: Path):
    # mind / schedule
    mind = merge_by_id_lww(
        load_json(dst / "hibi_mind_nodes.json") or [],
        load_json(src / "hibi_mind_nodes.json") or [],
        lambda e: str(e.get("updatedAt") or ""),
    )
    sched = merge_by_id_lww(
        load_json(dst / "hibi_schedule_events.json") or [],
        load_json(src / "hibi_schedule_events.json") or [],
        mtime_key,
    )
    save_json(dst / "hibi_mind_nodes.json", mind)
    save_json(dst / "hibi_schedule_events.json", sched)

    s_assist, d_assist = src / "hibi_assistant", dst / "hibi_assistant"
    d_assist.mkdir(parents=True, exist_ok=True)
    agents = merge_agents(
        load_json(d_assist / "assistant_agents.json") or [],
        load_json(s_assist / "assistant_agents.json") or [],
    )
    save_json(d_assist / "assistant_agents.json", agents)

    # messages_*.json
    names = set()
    for p in list(s_assist.glob("messages_*.json")) + list(d_assist.glob("messages_*.json")):
        names.add(p.name)
    for name in sorted(names):
        a = load_json(d_assist / name) or []
        b = load_json(s_assist / name) or []
        if not isinstance(a, list):
            a = []
        if not isinstance(b, list):
            b = []
        save_json(d_assist / name, dedupe_messages(a + b))

    # chat_files：仅补缺
    s_cf, d_cf = s_assist / "chat_files", d_assist / "chat_files"
    if s_cf.exists():
        for f in s_cf.rglob("*"):
            if f.is_file():
                rel = f.relative_to(s_cf)
                dest = d_cf / rel
                if not dest.exists():
                    dest.parent.mkdir(parents=True, exist_ok=True)
                    shutil.copy2(f, dest)


def main():
    assert TARGET.exists(), f"缺少 target: {TARGET}"
    sources = sorted(p for p in ROOT.iterdir() if p.is_dir() and p.name != TARGET_NAME)
    print("TARGET:", TARGET)
    print("SOURCES:", [p.name for p in sources])
    for src in sources:
        print("merge", src.name, "...")
        merge_account(src, TARGET)
    print("done")


if __name__ == "__main__":
    main()
```

#### 手工合并（仅小数据）

1. 用能正确处理 UTF-8 的编辑器打开两侧 JSON。  
2. 按上表规则手工并入 target 文件后保存（**UTF-8，勿另存为 UTF-16**）。  
3. `messages_*.json` / `chat_files` 按文件名合并或补拷。  
数据量大时务必用脚本，避免漏项。

### 步骤 5 — 启动 App 验证

1. 打开希比 → 确认个人中心仍是本地账号。  
2. **思维**：旧项目是否出现。  
3. **日程**：事件数量与内容是否合理。  
4. **助理**：智能体列表与历史对话是否在。  
5. 验证通过前：**不要删**源目录与 `hibi_accounts_backup_*`。

---

## 5. 注意事项

1. **先备份再改**；合并失败用备份整树还原即可。  
2. **勿删源目录**直到确认无误；App 切换账号也不会自动清其它目录。  
3. JSON 一律 **UTF-8**；PowerShell `Set-Content` 默认编码可能不是 UTF-8，优先用 Python `encoding="utf-8"`。  
4. **冲突策略**：mind/schedule 是 LWW（新时间胜）；agents 同 id 保 target；消息去重追加——同 id 旧内容可能被较新副本替换，属预期。  
5. **运行中勿改盘**：改文件前退出 App。  
6. **V4 门禁**：绕过 GitHub 后默认写 `local/`；复开门禁后可能再出现 `gh_*` 目录，导入弹窗逻辑见 `LocalAccountImportService` 与 [`AUTH_GITHUB_SEALED.md`](lib/features/auth/AUTH_GITHUB_SEALED.md)。  
7. **高级用户**：可阅读 `SyncMerge`、`LocalAccountImportService`、`test/account_data_merge_test.dart`；仓库注释里偶发的 `AccountDataMergeService` 名称指同一套策略，**并非**单独 Dart 类。

---

## 6. 真实案例摘要（2026-08-09）

供对照「合并前后大概长什么样」。

| 项 | 值 |
|----|-----|
| 机器路径 | `C:\Users\a1306\Documents\hibi_accounts` |
| Target | `local` |
| 源目录 | `1703f2fa…`、`52555b3e…`、`588ef545…`、`gh_154501426` |
| 备份 | `C:\Users\a1306\Documents\hibi_accounts_backup_20260809_0345` |
| 规则 | 对齐 `SyncMerge` / `LocalAccountImportService`；源目录保留 |

**Target（`local`）条数**

| 类型 | 合并前 | 合并后 |
|------|--------|--------|
| 思维节点 | 3 | 29 |
| 日程 | 6 | 122 |
| Agents | 2 | 11 |
| 消息（合计 / 文件数） | 32 / 1 文件 | 157 / 9 文件 |

主要增量来自 `52555b3e…`（思维 +22、日程 +109）；`gh_154501426` 贡献少量日程与 builtin 消息。合并后 mind/schedule/agents 的 `id` 均唯一，四个源目录仍在磁盘上。

---

## 7. 相关文档与代码

| 文档/代码 | 说明 |
|-----------|------|
| [`AUTH_GITHUB.md`](lib/features/auth/AUTH_GITHUB.md) | GitHub 登录与门禁总览 |
| [`AUTH_GITHUB_SEALED.md`](lib/features/auth/AUTH_GITHUB_SEALED.md) | V4.0.1 绕过门禁封存说明 |
| [`AUTH_SPEC.md`](lib/features/auth/AUTH_SPEC.md) | 同步与账号目录约定 |
| `account_storage_paths.dart` | 路径与 sanitize / local 键 |
| `sync_merge.dart` | LWW 与 assistant 合并 |
| `local_account_import_service.dart` | App 内导入到 GitHub |
