# 版本管理

记录每个版本修复的 Bug 与变更内容。

---

## V1.0.1

**发布日期**：2025-03-13（以实际发版为准）  
**版本号**：1.0.1（build 2）

### 本版重点

- 思维白板不丢笔记（手动保存 + 返回前等待落盘）。
- **同步异常覆盖的修复**：云端数据优先，先拉后推，避免本地旧/空数据覆盖云端新笔记。

### 修复的 Bug

#### 1. 电脑端白板内容未实时落盘，返回后笔记消失

**现象**：在思维节点下打开项目白板编辑，未等自动保存就返回列表，再进入该项目时，刚才的方块、连线等不见了。

**原因**：
- 每次编辑会调用「保存」，但保存是**异步写盘**且**未等待完成**；用户快速返回时，写盘可能尚未结束，内存中的最新内容没有写入文件。
- 返回时也没有「等当前保存完成再退出」的逻辑，导致未落盘就离开页面。

**修复**：
- 画布每次保存都会更新一个「待保存」任务，**返回时先等待该任务完成再真正退出**，避免未写盘就离开。
- **左侧工具栏增加「保存」按钮**：用户可随时手动保存；点击后会写盘并（在已登录时）**立即同步到云端**，不再只依赖 3 秒防抖上传。
- 保存或同步**失败时会有明确提示**：
  - 写盘失败 → 提示「保存失败，请重试」；
  - 已保存到本机但同步失败 → 提示「已保存到本机，同步失败请检查网络」。

---

#### 2. 同步异常覆盖：手机端本地空白/旧数据覆盖了电脑端的新笔记

**现象**：在电脑上白板里写了很多内容并已同步到云端；在手机上打开同一账号时，手机先用自己的本地数据（可能是空白或旧版本）去覆盖云端，导致电脑上的新笔记被盖掉（同步异常、旧版本覆盖新版本）。

**原因**：
- 同步策略未严格做到「**先拉后推**」：应用回到前台或切到思维 tab 时会拉取云端并合并，但若之前有一个「待推送」的定时任务，可能**先执行推送再拉取**，从而用本机旧数据覆盖了云端。
- 拉取云端并合并写盘后，**思维列表仍在使用旧的内存数据**，没有从磁盘重新加载，所以界面上看不到刚合并下来的云端内容。

**修复（同步异常覆盖的修复）**：
- **云端优先、先拉后推**：每次执行「拉取云端」前，先**取消所有未执行的推送**，再拉取、合并、写盘；拉取完成后再产生的本地修改会重新触发新的推送。确保不会用本机旧数据抢先覆盖云端，避免同步异常导致笔记被覆盖。
- **思维节点改为单例仓库**：拉取并写盘后，会**统一从磁盘重载思维列表**，列表与画布数据与云端合并结果一致，手机端打开即可看到电脑端已同步的笔记。
- 合并规则：同一项目按 **updatedAt 更新时间为准**，新版本覆盖旧版本，不再出现旧版本覆盖新版本的情况。

### 变更清单（简要）

| 类型 | 说明 |
|------|------|
| 功能 | 思维画布左侧工具栏增加「保存」按钮，支持手动写盘 + 立即同步到云端 |
| 体验 | 保存/同步失败时 SnackBar 提示：「保存失败」「已保存到本机，同步失败请检查网络」等 |
| 体验 | 从画布返回时先等待当前保存完成再退出，减少「返回后笔记没了」 |
| 修复 | 同步前先取消未决推送再拉取，避免手机端用本地旧数据覆盖云端 |
| 修复 | 拉取后思维列表从磁盘重载，手机端能正确显示云端已有笔记 |
| 实现 | MindRepository 单例化；UserSyncScheduler 增加 pushNow()、pull 前 cancelPendingPush |

### 升级与兼容

- 数据格式与 1.0.0 兼容，无需迁移。
- 建议所有端升级到 1.0.1 后使用，多端同时编辑时仍以 **updatedAt 新者为准**。

### 已知限制

- 多端**同时**编辑同一项目时，仍按「后写入者胜」合并，不会做段落级冲突合并。
- 弱网下若同步失败，可多试几次「保存」或切到有网环境再点保存。

---

## V1.0.2

**发布日期**：2025-03-13（以实际发版为准）  
**版本号**：1.0.2（build 3）

### 本版重点

- **主题设置**：设置中新增「主题设置」，支持切换三种应用主题。
- **三种主题**：hibi 主题（原有图三毛玻璃 + 深紫）、暗色主题（纯色深色背景 + 蓝色点缀）、亮色主题（纯色浅色背景 + 蓝色点缀）。
- **字体与标题栏**：全局统一字号层级；修复 Windows 标题栏应用名乱码（希比-2024）。

### 新增功能

| 类型 | 说明 |
|------|------|
| 功能 | 设置页增加「主题设置」入口，可进入主题选择页 |
| 功能 | **hibi 主题**：保留原有视觉，图三背景图 + 毛玻璃 + 深紫色系 |
| 功能 | **暗色主题**：全应用纯色深色背景（#121212），按钮/强调色为 Logo 蓝，无背景图 |
| 功能 | **亮色主题**：全应用纯色浅色背景（#F5F5F5），按钮/强调色为 Logo 蓝，无背景图 |
| 体验 | 主题选择持久化到本地，下次启动保持用户选择 |
| 实现 | ThemeNotifier + SharedPreferences；HibiThemeExtension；FrostedBackground / 思维画布背景随主题切换 |

### 字体与标题栏修复

| 类型 | 说明 |
|------|------|
| 体验 | **字体统一**：全局统一字号层级（AppBar 18px、列表主标题 16px、副标题 14px 等），避免各页「字体有大有小」 |
| 实现 | 在 `AppTheme` 中定义统一字号常量，三个主题的 `textTheme` 均使用该套字号 |
| 修复 | **Windows 标题栏乱码**：窗口标题「希比-2024」在 `windows/runner/main.cpp` 中改为 Unicode 转义（`L"\u5E0C\u6BD4-2024"`），避免源文件编码导致显示为乱码 |

### 技术说明

- 详见 `主题功能.md` 记录主题技术原理与实现方式。

---

## V1.2.1

**发布日期**：2026-03（以实际发版为准）  
**版本号**：1.2.1（build 4）

### 本版重点

- **白板悬浮 UI**：思维白板左右功能栏改为「仅栏内不透视、四角镂空」，视觉上左右栏悬浮于画布。
- **电脑端语音输入**：智能体对话在 Windows/macOS/Linux 支持语音输入（点击开始/结束录音）；移动端保持长按说话。
- **白板返回前保存**：左上角返回与系统返回键均先保存并等待落盘后再退出。

### 变更清单（简要）

| 类型 | 说明 |
|------|------|
| 体验 | 白板背景仅绘制中心区，左右栏与顶/底栏仅覆盖中心 strip，四角透明；macOS 窗口透明以透出桌面 |
| 功能 | 智能体对话桌面端支持「点击开始录音 / 点击结束并发送」；文案去掉「手机端」限制 |
| 修复 | 白板 PopScope 与左上角返回均先 `_saveItems` + `await _pendingSave` 再 pop |
| 实现 | `kFloatingBarWidth`、左右栏 `Container(color)` 包裹；`_isDesktop` + 语音栏分支 |

### 升级与兼容

- 数据格式与 1.0.x / 1.2.x 兼容，无需迁移。

---

## V1.2.2

**发布日期**：2026-03（以实际发版为准）  
**版本号**：1.2.2（build 5）

### 本版重点

- **语音输入交互修复**：按住说话 / 松手发送 / 上划取消流程稳定，修复短录音导致的无效 WAV 报错。
- **语音浮层视觉优化**：半圆面板在不同主题下统一蓝色系与毛玻璃透视感，修复上下风格割裂。
- **助理与个人中心 UI 微调**：新建智能体弹窗改为不透底，取消按钮可见性增强；个人中心头像改为方形圆角。

### 变更清单（简要）

| 类型 | 说明 |
|------|------|
| 修复 | 语音识别录音时序：增加录音开始态判定，避免 `Invalid WAV: no data chunk` |
| 体验 | 录音浮层半圆弧参数与渐变重构；声波条数量增加、线条更细 |
| 体验 | 录音提示文案去掉文字底框，统一字体粗细 |
| 体验 | 新建/编辑智能体弹窗改为实心背景，不再透出底层内容 |
| 体验 | 取消按钮改为描边样式，深浅主题下可读性提升 |
| 体验 | 个人中心头像由圆形改为方形圆角 |

### 升级与兼容

- 数据格式与 1.0.x / 1.2.x 兼容，无需迁移。

---

## V1.2.4

**发布日期**：2026-03（以实际发版为准）  
**版本号**：1.2.4（build 7）

### 本版重点

- **思维白板同步修复（跨端实时与可靠保存）**：修复白板更新后另一端不及时可见、以及保存后退出偶发丢失的问题。
- **白板实时性增强**：白板页同步节奏提升到亚秒级，跨端变更更快显示。
- **语音与助理页面体验优化**：语音浮层视觉统一、新建智能体弹窗可读性提升、个人中心头像样式调整。

### 变更清单（简要）

| 类型 | 说明 |
|------|------|
| 修复 | `MindRepository.update` 改为按 id 覆盖（upsert），避免 pull/reload 后旧对象保存失效 |
| 修复 | 白板页拉取刷新增加并发/保存态保护，避免本地保存时被远端覆盖 |
| 修复 | 白板页远端刷新增加内容比对，仅在确有差异时刷新 |
| 体验 | 同步调度节奏提升：push 防抖约 300ms，白板 pull 间隔约 500ms |
| 体验 | 语音录音时序与浮层视觉继续优化（含主题自适应） |
| 体验 | 助理弹窗不透底、取消按钮更清晰；个人中心头像改方形圆角 |

### 升级与兼容

- 数据格式与 1.0.x / 1.2.x 兼容，无需迁移。

---

## V1.2.4B

**发布日期**：2026-03（以实际发版为准）  
**版本号**：1.2.4B（build 8）

### 本版重点

- **文件传输接收确认**：接收方必须点击“接受”后才开始传输，支持“拒绝”。
- **传输历史增强**：文件记录新增三点菜单，支持删除文件、打开保存目录、另存到本地。
- **交互一致性提升**：文本接收同样支持接受/拒绝，减少误收。

### 变更清单（简要）

| 类型 | 说明 |
|------|------|
| 功能 | 接收流程增加握手确认：发送端等待接收端 `ACCEPT` 后再发送内容 |
| 功能 | 接收端可回传 `REJECT`，发送端收到后提示“对方已拒绝接收” |
| 功能 | 传输记录文件项新增 `...` 菜单：删除文件 / 打开文件保存路径 / 保存到本地 |
| 体验 | 文本接收支持“接受并复制 / 拒绝”，与文件接收交互一致 |
| 实现 | 传输记录补充本地路径字段，便于目录打开与另存处理 |

### 升级与兼容

- 数据格式与 1.2.x 兼容，无需迁移。

---

## V1.2.5

**发布日期**：2026-03（以实际发版为准）  
**版本号**：1.2.5（build 9）

### 本版重点

- **白板交互稳定性优化**：修复跨端实时同步时，正在拖拽方块偶发“来回跳动”的问题。
- **拖拽跟手优化**：方块拖动改为跟随鼠标按下点，不再强制以方块中心吸附到指针。
- **文件传输体验升级**：接收端支持“接受后才开始传输 / 拒绝接收”，并增强传输历史操作能力。

### 变更清单（简要）

| 类型 | 说明 |
|------|------|
| 修复 | 白板交互期间增加远端刷新抑制与短暂缓冲，避免拖拽中被远端状态覆盖导致抖动 |
| 体验 | 白板同步拉取改为交互态自适应频率：活跃期快拉取、空闲期降频，兼顾实时性与稳定性 |
| 体验 | 方块拖拽采用按下点偏移跟随，拖动手感更贴手 |
| 功能 | 文件传输接收增加握手确认（ACCEPT/REJECT），接收方可明确接受或拒绝 |
| 功能 | 传输历史文件项新增三点菜单：删除文件 / 打开保存路径 / 保存到本地 |
| 调整 | 思维白板移除手动保存按钮，保留自动保存与自动同步流程 |

### 升级与兼容

- 数据格式与 1.2.x 兼容，无需迁移。

---

## V2.1.3D

**发布日期**：2026-03（以实际发版为准）  
**版本号**：2.1.3D（build 13）

### 本版重点

- **智能体语音识别流式化**：按住说话时实时显示转写文本，松手后发送最终识别结果。
- **ASR 后端兼容增强**：支持 `ASR_APP_ID/ASR_TOKEN` 与 `ASR_APP_KEY/ASR_ACCESS_KEY` 两套环境变量命名。
- **支付与同步链路稳定性延续修复**：沿用已验证的 ECS 同步、服务重启、健康检查流程，确保线上版本一致。

### 变更清单（简要）

| 类型 | 说明 |
|------|------|
| 功能 | 新增流式语音服务接入（`/api/asr/stream`），录音浮层实时显示 partial/final 文本 |
| 修复 | 补齐 ECS WebSocket 依赖，修复 `not upgraded to websocket` 导致的流式失败 |
| 修复 | ASR 配置兼容双变量命名并增强配置状态返回字段，提升排障效率 |
| 调整 | 应用内“关于”版本号更新为 `2.1.3D`，打包版本切换为 `2.1.3+13` |

### 升级与兼容

- 数据格式与 1.2.x 兼容，无需迁移。
- 线上后端仍按 ECS 部署流程运行，不在本机启动后端。

---

## V2.1.8

**发布日期**：2026-03-28（以实际发版为准）  
**版本号**：2.1.8（build 18）

### 本版重点

- **日程提醒**：本地通知链路补强（Android 闹钟流/振动/精确闹钟降级、iOS/macOS 权限与 Darwin 详情、Windows 告警场景）；回到前台重新登记定时通知；新建日程默认提前 15 分钟提醒。
- **后台管理**：选项卡式 UI（系统配置 / 用户管理）；图形认证总开关；项目助理 System Prompt 在线编辑。
- **智能体与工具**：项目助理工具调用策略与文档（日程/思维节点）；注册/登录文案与 UI 微调。

### 变更清单（简要）

| 类型 | 说明 |
|------|------|
| 功能 | 后台 `graph_captcha_enabled`：可关闭登录/注册图形认证 |
| 功能 | 后台 `abp_system_prompt`：项目助理 System Prompt 库内编辑 |
| 体验 | 管理后台选项卡：默认「系统配置」，「用户管理」再加载客户列表 |
| 体验 | 日程提醒多平台通知与铃声/振动策略优化 |
| 文档 | `工具函数调用.md`、`后台管理页说明.md` 等同步 |

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V2.2.4

**发布日期**：2026-03-30（以实际发版为准）  
**版本号**：2.2.4（build 24）

### 本版重点

- 全端版本号统一为 **2.2.4**，便于与安装包、应用商店等渠道对照。
- **设置 → 关于** 中显示的版本号与当前安装的应用版本一致，安装或更新后可直接在此核对是否为最新包。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V2.2.4B

**发布日期**：2026-03-31（以实际发版为准）  
**版本号**：2.2.4B（build 25）

### 本版重点

- **设置 → 关于** 与安装包命名统一为 **2.2.4B**（`lib/app/app_version.dart` 与 `pubspec` build 对齐）。
- **流式语音识别**：修复第二次按住无法识别、提示「流式识别未返回文本」等问题（ASR WebSocket 会话与语音模式预热线程）。
- **Android 图形认证**：允许 HTTP 明文加载 embed 页（`network_security_config`），避免 `ERR_CLEARTEXT_NOT_PERMITTED`。
- **iOS / macOS**：为可配置的 HTTP 后端放宽 ATS，与图形认证 WebView 一致。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V3.0.0

**发布日期**：2026-04-02（以实际发版为准）  
**版本号**：3.0.0（build 30）

### 本版重点

- 全端版本号升级为 **3.0.0**，并与安装包命名一致（`pubspec.yaml`、`lib/app/app_version.dart`、Windows Setup 输出名对齐）。
- 后台主题管理增强：主题列表、隐藏/显示、日历排期（按北京时间，支持截至日期）与样式代码存档；App 静默拉取主题策略并在北京时间 0 点按排期自动切换。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V3.0.2

**发布日期**：2026-04-02（以实际发版为准）  
**版本号**：3.0.2（build 32）

### 本版重点

- 版本更新增强：下载进度展示，支持暂停/继续/取消，并支持下载完成后直接安装。
- 移动端图形认证弹窗：自适应高度与更协调的标题栏布局，避免弹窗过长不协调。
- 主题系统第二阶段：后台新增自定义主题可通过 JSON token 驱动 App 主题（无需绑定内置主题），并配套 `主题管理指南.md`。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V3.0.3

**发布日期**：2026-04-02（以实际发版为准）  
**版本号**：3.0.3（build 33）

### 本版重点

- **文件传输**：传输页支持设置接收端口与发现端口（可固定或自动分配），修改后自动重启传输服务。
- **主题设置开放**：所有用户均可进入主题设置并切换主题，不再要求登录或主题服务订阅。
- 安装包交付至项目 `release\V3.0.3\` 子目录，并附带本版本说明文档。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V3.0.4

**发布日期**：2026-07-12  
**版本号**：3.0.4（build 34）

### 本版重点

- **传输端口**：局域网发现默认端口统一为 **62637**，并保留 62638、62639 自动回退。
- **电脑端文件夹传输**：支持把文件夹直接拖入传输区，自动递归发送其中的文件；文件选择也支持多选。
- **安卓文件打开**：APK、PDF、Office 文档、图片、音视频、压缩包等常见文件优先调用系统关联应用打开；没有关联应用或打开失败时自动定位到内置文件管理。
- **接收文件管理**：增加搜索、名称/时间/大小排序、新建文件夹、重命名、删除、详情及刷新；同名接收文件自动编号，不再覆盖原文件。
- **Android 兼容**：补充 Android 11+ 文件打开应用可见性声明，改善“点击打开无反应”。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V3.0.5

**发布日期**：2026-07-12  
**版本号**：3.0.5（build 35）

### 本版重点

- **思维节点导出**：长按列表中的思维节点，可将完整项目导出为 HIBI 自定义 `.hbm` 文件。
- **思维节点导入**：点击思维页右下角加号，可选择“从本地导入”，导入 `.hbm` 后自动打开白板。
- **完整数据交换**：`.hbm` 包含节点标题、要义、标星状态以及画布中的方块、笔记、栏目、连线和提醒字段。
- **安全重复导入**：导入时重新生成项目及画布元素 ID，并同步修正栏目/连线引用，避免覆盖本地项目或产生 ID 冲突。
- **Windows 文件关联**：安装 Windows 新版后，双击 `.hbm` 文件可启动希比，自动导入并打开对应思维节点。

### 升级与兼容

- `.hbm` 初始格式版本为 1；后续版本将通过文件内版本号保持兼容。
- 既有本地及云端思维节点数据无需迁移。

---

## V3.0.6

**发布日期**：2026-07-12  
**版本号**：3.0.6（build 36）

### 本版重点

- **独立 HBM 文件图标**：`.hbm` 使用带齿轮角标的专属多尺寸图标，与希比应用 Logo 明确区分；安装 Windows 新版后自动更新文件关联。
- **发送前强制确认设备**：即使只发现一台设备，也必须在确认页选中设备后点击“确定发送”，避免误发。
- **设备确认页实时刷新**：确认页持续接收发现结果，并提供“实时刷新”按钮；可核对设备名称、类型、IP 与端口后再发送。
- **敏捷管理课堂**：“我的”页面在“个人资料”上方新增敏捷管理入口，提供 8 章 24 课的系统课程。
- **以敏捷方式学习**：课堂包含学习冲刺进度、待学习/进行中/已掌握看板、章节知识点、实战任务、产出物与完成检查，进度自动保存在本机。
- **课程覆盖完整实践链路**：从敏捷思维、产品目标、产品待办、Scrum、迭代运行、质量与持续交付，到度量回顾、组织落地和 30 天实践计划。

### 内容依据

- 课程根据《敏捷软件开发宣言》及十二条原则、The Scrum Guide 2020 等公开权威资料原创编排。
- 图片所示商业书籍仅作为主题参考，课程不复制其受版权保护的正文。

### 升级与兼容

- 学习进度仅保存在本机，不影响账户、思维节点或日程数据。
- Windows 更新安装后若资源管理器暂未刷新 `.hbm` 图标，可刷新桌面或重新登录系统。

---

## V3.0.7

**发布日期**：2026-07-12  
**版本号**：3.0.7（build 37）

### 本版重点

- **持久待发送列表**：拖入或选择的文件会固定显示在发送区，首次发送、取消发送、发送完成以及重启 App 后都不会自动清除。
- **重复发送无需重选**：点击“发送列表中的文件”即可再次打开设备确认页，把同一批文件依次发送给不同设备。
- **逐项移除**：每个文件右侧提供删除按钮，只从待发送列表移除，不会删除磁盘中的源文件。
- **源文件状态检查**：若源文件被移动或删除，列表会以红色状态提示；批量再次发送时自动跳过不可用文件。
- **文件信息清晰展示**：列表显示文件类型图标、名称、大小和原始路径，点击有效文件可直接打开检查。

### 升级与兼容

- 待发送文件路径保存在本机，不参与账户云同步。
- 升级不影响已有传输记录、接收文件及其他业务数据。

---

## V3.0.8

**发布日期**：2026-07-12  
**版本号**：3.0.8（build 38）

### 本版重点

- **移除内容区悬浮加号**：思维、日程和助理页面不再使用遮挡列表或月历的右侧悬浮按钮。
- **思维节点**：标题栏新增“新建”入口，继续支持新建思维节点和从本地导入 `.hbm`。
- **日程管理**：标题栏新增紧凑的日程创建入口，并移除没有实际功能的搜索、列表占位按钮。
- **助理管理**：标题栏新增“新建助理”入口，智能体列表内容不再被遮挡。
- **空间优化**：日程月历缩小原先为悬浮按钮保留的底部空白，可展示更多有效内容。
- **空状态引导同步更新**：空列表提示改为引导用户使用标题栏入口，避免继续提示右下角按钮。

### 升级与兼容

- 仅调整操作入口与布局，不改变思维节点、日程、助理或传输数据。
- 原有新建、导入和编辑流程保持兼容。

---

## V3.0.9

**发布日期**：2026-07-12  
**版本号**：3.0.9（build 39）

### 本版重点

- **跨版本文件传输**：设备发现和传输不再依赖 App 显示版本名称，只要基础传输协议一致即可互通。
- **兼容新旧发现端口**：同时支持当前 62637–62639、早期 52637–52639 以及用户自定义发现端口。
- **修复陈旧接收端口**：设备重启或接收端口变化后会实时更新；离线 10 秒自动移除，解决“列表有设备但文件发不过去”。
- **强化 Android 发现**：增加 Wi-Fi 局域网广播接收锁，降低部分安卓机型搜不到 Windows 设备的问题。
- **更快主动检索**：刷新时执行三次短间隔探测，支持对端直接单播回复，同时增强 10.x、172.x 和 192.168.x 私网广播。
- **连接重试与排障提示**：TCP 首次连接失败会自动重试一次，超时后明确显示目标设备 IP 和端口。
- **设备在线状态维护**：持续刷新设备最后在线时间，避免长期展示已退出或网络已切换的设备。

### 升级建议

- 建议 Windows 与 Android 都升级到本版，可获得完整的双端主动发现和单播回复能力。
- 新版仍会主动兼容使用早期发现端口的旧版；旧版自身存在的 Android 广播接收限制无法由另一端完全消除。
- 若同一 Wi-Fi 开启了 AP/客户端隔离，设备间通信会被路由器阻止，需要关闭隔离或改用允许局域网互访的网络。

---

## V3.0.10

**发布日期**：2026-07-12  
**版本号**：3.0.10（build 40）

### 本版重点

- **提升大文件传输速度**：接收端改为边接收边写入磁盘，移除整文件内存缓存和二次复制，降低大文件传输时的内存占用与等待时间。
- **同名文件自动编号**：保存目录已有同名文件时，自动生成“文件名 (1).扩展名”“文件名 (2).扩展名”，不覆盖旧文件，也不再因同名而失败。
- **修复提前显示 100%**：发送进度区分网络发送、等待对方保存和保存完成；新版设备只有在接收端完成磁盘写入并返回确认后才显示 100%。
- **接收进度更准确**：接收数据期间最高显示 99%，磁盘刷新和文件关闭完成后才显示“已保存 100%”。
- **并发保存更安全**：通过原子方式占用目标文件名，多个任务同时接收同名文件也能正确编号。
- **保留跨版本兼容**：新版之间启用保存完成确认；向未声明新协议的旧版设备发送时继续使用基础兼容流程。

### 验证

- 已通过本机 TCP 回环集成测试，覆盖文件内容一致性、同名连续发送自动编号，以及发送进度仅在落盘完成后到 100%。
- 已通过传输模块静态检查和项目全部自动化测试。

---

## V3.0.11

**发布日期**：2026-07-12  
**版本号**：3.0.11（build 41）

### 本版重点

- **敏捷管理入口统一**：入口图标改为课程风格的线性图标，与“个人资料”“服务增值”“主题设置”等功能保持一致。
- **精简入口信息**：移除敏捷管理入口下方的课程解释文案，恢复为与其他功能相同的单行标准按钮。

---

## V3.0.12

**发布日期**：2026-07-12  
**版本号**：3.0.12（build 42）

### 本版重点

- **全局接收确认弹窗**：收到文件或文本时，无论当前位于思维、日程、助理、传输还是“我的”页面，都会弹出传输请求，提供“拒绝”和“确定接收”按钮。
- **解决“看得到但发不过去”**：Windows 安装程序参照 LANDrop 类局域网工具，自动为应用添加程序级防火墙入站、出站规则，放行局域网 TCP 文件通道及 UDP 发现通道。
- **端口冲突自动恢复**：默认 TCP 接收端口被占用时自动选择可用端口，并通过发现协议广播真实端口，不再让整个接收服务启动失败。
- **连接抗抖动增强**：TCP 连接改为三次渐进重试并启用低延迟选项，降低 Wi-Fi 短暂抖动导致的直接发送失败。
- **错误提示更准确**：连接失败时提示确认接收端应用运行状态、同一局域网和防火墙设置，不再要求必须停留在传输页面。
- **并发请求可确认**：确认一个大文件后立即开始接收，其他传输请求仍可继续逐个弹窗确认，不会被上一项传输阻塞。

### 验证

- 已通过端口占用自动回退测试及真实 TCP 回环文件传输测试。
- 已覆盖文件内容一致性、同名编号和接收端落盘后才显示 100%。

---

## V3.0.13

**发布日期**：2026-07-12  
**版本号**：3.0.13（build 43）

### 本版重点

- **附近设备稳定显示**：手动刷新和发送确认页的实时刷新不再先清空列表，刷新过程中已有设备保持可见、可选择。
- **增强广播丢包容错**：设备离线判定由 10 秒延长至 45 秒，避免 Android 省电、Wi-Fi 短暂抖动或漏收几轮 UDP 广播时设备反复出现和消失。
- **接收后隐藏操作按钮**：点击“确定接收”后立即隐藏“接收/拒绝”按钮，只显示实时接收百分比、正在写入或保存完成状态。
- **每个任务独立进度**：不同接收请求使用独立进度，不再共用一个百分比，避免并发传输时进度互相覆盖。
- **断开状态清理**：发送方取消或连接提前断开时自动移除失效请求并给出提示。

### 验证

- 已通过 9 项自动化测试，包括端口冲突回退、真实 TCP 文件传输、落盘确认及同名文件编号。

---

## V3.0.14

**发布日期**：2026-07-13  
**版本号**：3.0.14（build 44）

### 本版重点

- **课堂扩充为 12 章 36 课**：保留原有敏捷思维、产品目标、待办、Scrum、迭代、质量、度量与落地课程，新增 4 章 12 课进阶实务内容。
- **补齐客户发现与产品规划**：新增客户访谈、证据等级、根因分析、成果路线图、端到端发布切片和基于历史数据的概率预测。
- **补齐方法与团队建设**：系统比较 Scrum、Kanban、精益与 XP 的适用边界，新增稳定跨职能团队、技能矩阵、心理安全、建设性冲突和信息辐射源。
- **补齐敏捷治理与经营**：新增产品组合、滚动预算、停止决策、范围交换、敏捷采购合同、时间成本和滚动治理。
- **补齐风险、规模化与变革**：新增风险驱动待办、探针任务、LeSS/Scrum@Scale/SAFe 情境选择，以及 90 天变革实验和回退信号。
- **深度学习结构**：新增课程均包含原创案例推演、常见误区、4—5 步实战任务、可复制模板、填写示例、情境测验与答案解析、延伸依据。
- **贯穿式项目实战**：用户可使用自己的项目，也可跟随“HIBI 跨设备传输稳定性提升”案例，逐步形成客户问题简报、成果路线图、发布预测、团队协议、质量风险板、投资治理和变革计划。
- **学习闭环增强**：带测验的进阶课程需答对情境题后才能标记“已掌握”，避免只浏览标题即完成。
- **兼容已有学习进度**：原 24 课 ID 与本地完成记录保持不变，升级后不会丢失已掌握状态。

### 内容说明

- 课程主题参考用户提供的《敏捷项目管理（第 3 版）》目录，并结合敏捷宣言、Scrum Guide 2020、The Kanban Guide、Evidence-Based Management，以及精益、XP 和持续交付公开实践重新原创编排。
- 课堂不复制书籍正文；案例、讲解、练习、模板和测验均为面向实际工作的原创内容。

### 验证

- 课程静态检查 0 问题，全部 10 项自动化测试通过。
- 自动校验 12 章 36 课的 ID 唯一性，以及所有新增进阶课程的案例、误区、步骤、模板、测验答案和参考资料完整性。


## V3.3.0

**发布日期**：2026-08-08  
**版本号**：3.3.0（build 52）

### 本版重点

- **版本更新下载增强**：发现新版本时弹窗展示远程版本号与更新说明；支持下载进度、断点续传、失败自动重试；Android 下载用 WakeLock + 通知栏进度。
- **智能体对话队列打断**：思考/生成时可继续发送队列；支持打断；离开对话页后任务可在后台完成。
- **局域网数据同步**：同一局域网内发现设备、可选加密、定时增量合并思维导图/日程/设置。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V3.3.2

**发布日期**：2026-08-08  
**版本号**：3.3.2（build 54）

### 更新重点

- **更新通知弹窗可读性**：手机端更新通知改为不透明实底，并加大弹窗区域，避免半透明叠字难读。

### 数据与兼容

- 无数据格式变更；可直接覆盖安装。

---

## V3.3.1

**发布日期**：2026-08-08  
**版本号**：3.3.1（build 53）

### 本版重点

- **思维/助理页顶栏按钮统一**：「新建」与「订阅」尺寸、字号与图标统一，并稍加大。
- **思维白板缩放手柄热区**：可视手柄保持不变，命中热区扩大至约 80px（画布缩放自适应），拖动缩放更易点中。
- **助理列表右侧操作对齐**：希比助手锁图标与其他助理三点菜单统一槽位宽高与居中对齐。
- **传输页发送区加高**：拖放/发送区高度约为原来的3倍，更大气、更易操作。
- **个人中心菜单字重**：菜单项统一为 FontWeight.w500，避免 Windows 中文合成粗体导致同句内粗细不一。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V3.3.4

**发布日期**：2026-08-08  
**版本号**：3.3.4（build 56）

### 本版重点

- **豆包 ASR App 直连**：语音识别改为客户端直连火山 OpenSpeech（SAUC），不再经 HIBI 后端转发；在「智能体配置」填写 APP ID / Access Token / Resource ID 并启用即可。
- **传输页拖放区再加高**：发送文件拖放热区约屏高 32%（320–480px），图标与文案加大，电脑端更易拖入。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。
- ASR 凭据仍保存在本机「智能体配置」；Access Token 须为豆包**语音**控制台 Token，勿与方舟 `ark-` 对话 Key 混用。

---

## V3.3.5

**发布日期**：2026-08-08  
**版本号**：3.3.5（build 57）

### 本版重点

- **传输发送托盘重设计**：拖放区与待发送列表合并为同一卡片；空态紧凑投放条，有文件时列表嵌在框内，拖入/选择先入队再发送。
- **助理消息可选复制**：对话气泡支持划选文字与系统复制菜单。
- **白板缩放手柄可点中**：右下角手柄置顶、热区加大并略外扩；缩放时禁用方块拖动，边手柄避开角区重叠。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。

---

## V3.3.6

**发布日期**：2026-08-08  
**版本号**：3.3.6（build 58）

### 本版重点

- **修复 Android 桌面图标回滚**：3.3.5 构建时 `xhb-image/1_1.png` 被旧版蓝色以太/几何发光图覆盖，导致 APK 内 `ic_launcher` / adaptive foreground 与当前灰色商务品牌不一致；已用品牌源图重生成 mipmap 与 adaptive 图标。
- **Windows 图标保持不变**：继续使用预烘焙透明圆角 `app_icon.ico`（`flutter_launcher_icons` 的 `windows.generate` 仍为关闭）。
- **希比助手改为简单端侧 ABP**：直连模型 API + function calling；日程/思维 tools 在 App 本地执行；去掉预加载进度条与复杂意图路由。需在「智能体配置」填写并选中 API Key。
- **技术框架文档同步**：仓库根目录 `希比助手技术框架.md` 已按端侧主路径重写（模型判断 → tools → 本地写日程/思维），删除「永远走后端 ABP」等过时描述。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。
- **图标刷新**：部分安卓启动器会缓存旧图标；升级后若仍见旧图，请卸载重装，或清除启动器/桌面图标缓存后再查看。
- **希比助手**：依赖本机已配置的火山/OpenAI 兼容 Key；不再依赖后端执行 Skills。

---

## V3.3.7

**发布日期**：2026-08-08
**版本号**：3.3.7（build 59）

### 本版重点

- **思维导图缩放手柄简化**：去掉下方与右侧扩展按钮，仅保留右下角一个；手柄贴在方框角上，附近大热区内按下即为拖拽缩放，避免与方块拖动冲突。
- **Windows 桌面图标圆角加强**：预烘焙更明显的 PS/Win11 式透明圆角与边距，深色桌面上也能看清圆角轮廓。

### 升级与兼容

- 数据格式与既有版本兼容，无需迁移。
- Windows 桌面快捷方式若仍显示旧直角图标，请重新创建快捷方式，或重装后刷新图标缓存。

---

## V3.3.8

**发布日期**：2026-08-09
**版本号**：3.3.8（build 60）

### 本版重点

- **修复希比助手聊天记录消失**：回复后不再用残缺的服务端会话列表覆盖本地历史；端侧对话以本机 `messages_*.json` 为准，落盘可恢复。
- **加速应用内更新下载**：默认优先走 GitHub 加速镜像，缩短直连超时；传输中断后切换线路续传，避免反复卡在慢源。
- **GitHub 账号登录 + Star 门禁**：无自建后端；Device Flow 授权后须 Star `YYOZZE/HIBI` 才能进入应用；设置/个人中心显示 GitHub 用户名与头像。

### 升级与兼容

- 旧版手机号/Mock 登录不再视为有效门禁身份，升级后需重新用 GitHub 登录并 Star 仓库。
- 3.3.8 安装包未内置 Client ID；请升级到 3.3.9+，或自行用 `--dart-define=GITHUB_CLIENT_ID=...` 构建。
- 本地业务数据目录仍按账号隔离，格式兼容。

---

## V3.3.9

**发布日期**：2026-08-09  
**版本号**：3.3.9（build 61）

### 本版重点

- **内置 GitHub OAuth Client ID**：正式包可直接使用 Device Flow 登录，无需再手工 `--dart-define`。
- **文档**：补充 Client ID 获取与「勿建 GitHub App」说明（`AUTH_GITHUB.md` / `GITHUB_OAUTH_CLIENT_ID.md`）。

### 升级与兼容

- 数据格式与既有版本兼容。升级后请用 GitHub 登录，并 Star 仓库 `YYOZZE/HIBI`。
- OAuth App 须已启用 Device Flow。

---


## V3.3.10

**发布日期**：2026-08-09  
**版本号**：3.3.10（build 62）

### 本版重点

- **GitHub 登录 UX 重做**：明确「点登录 → 在 GitHub 网页输入账号密码授权 → 回 App」；App 不收集密码。
- **内嵌 WebView 登录**：Device Flow 验证页优先在 App 内打开（Windows WebView2 / Android·iOS webview_flutter），外跳系统浏览器作兜底；Chrome 系 UA，遇拦截可外开。
- **网络与错误提示**：连接/读超时加长（约 25s / 55s），超时与网络失败显示中文说明并支持重试；大号展示设备验证码，支持复制与分步状态（申请码 / 网页授权 / 检查 Star）。
- **Star 门禁**：授权后引导 Star 流程保持清晰。
- **登录态持久化**：本地保存 access_token（secure storage + prefs 双写）与资料/Star 缓存，**不存 GitHub 密码**；升级后冷启动自动恢复，避免每次更新都重走网页登录。

### 升级与兼容

- 数据格式与既有版本兼容。
- OAuth App 须已启用 Device Flow；用户设备需能访问 github.com。

---
*本文档面向用户说明各版本功能与修复。**安装包如何生成、输出路径与文件命名** 见仓库内 `安装包打包说明.md`。*

*后续版本在此文件下方按「## Vx.y.z」格式追加，每个版本下记录该版本面向用户的变更。*
