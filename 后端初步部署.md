# 希比 HIBI 部署与运行说明

**说明：后端仅在 ECS 上运行，不在本机启动或调试后端。** 本文档说明本机运行前端，以及在 ECS 上部署/运维后端并打通前后端。

---

## 一、项目结构简述

- **前端**：Flutter 应用（`lib/`），支持 Windows / Android 等，本机运行即可。
- **后端**：智能体对话 API，位于 `backend_jideshi_hibi_app/`，需部署到服务器供前端调用。

前端通过 `lib/config/api_config.dart` 中的 `assistantApiBaseUrl` 连接后端；未配置时使用占位提示。

---

## 二、后端部署（阿里云 + Docker）

### 2.1 环境要求

- 阿里云轻量应用服务器（已购）
- 已安装 Docker（如 Docker 26）
- 通义 / 豆包等 OpenAI 兼容 API 的密钥

### 2.2 上传代码到服务器

在服务器上创建目录：

```bash
mkdir -p ~/jideshi_hibi_backend
cd ~/jideshi_hibi_backend
```

将本机 `backend_jideshi_hibi_app/` 下这些文件上传到该目录：

- `api_only_app.py`
- `requirements.txt`
- `Dockerfile`
- `.env.example`（上传后复制为 `.env` 并编辑）

本机使用 SCP 示例（替换为你的公网 IP）：

```powershell
cd 项目根目录
scp backend_jideshi_hibi_app\api_only_app.py backend_jideshi_hibi_app\requirements.txt backend_jideshi_hibi_app\Dockerfile backend_jideshi_hibi_app\.env.example admin@你的公网IP:~/jideshi_hibi_backend/
```

### 2.3 配置 .env

在服务器上：

```bash
cd ~/jideshi_hibi_backend
cp .env.example .env
vi .env
```

填写大模型相关变量，例如通义：

```env
MODEL_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
MODEL_API_KEY=你的通义API密钥
MODEL_ID=qwen-plus
PORT=7861
```

### 2.4 构建并运行容器

```bash
cd ~/jideshi_hibi_backend
sudo docker build -t jideshi_hibi_api .
sudo mkdir -p data
sudo docker run -d \
  --name jideshi_hibi_api \
  --restart unless-stopped \
  -p 7861:7861 \
  --env-file .env \
  -v "$(pwd)/data:/app/data" \
  jideshi_hibi_api
```

验证：

```bash
sudo docker ps
curl -s http://127.0.0.1:7861/
```

应返回包含 `"message":"希比 HIBI 智能体 API"` 的 JSON。

### 2.5 开放端口

在阿里云控制台 → 轻量应用服务器 → 该实例 → **防火墙** 中添加入方向规则：

- 端口：**7861**
- 协议：**TCP**
- 来源：**0.0.0.0/0**（或按需限制）

保存后，在本机浏览器访问 `http://你的公网IP:7861/` 应能打开同一接口。

---

## 三、前端配置与运行

1. 打开 `lib/config/api_config.dart`。
2. 将 `assistantApiBaseUrl` 设为后端地址，例如：

```dart
static const String assistantApiBaseUrl = 'http://你的公网IP:7861';
```

3. 在本机运行应用：

```powershell
flutter run -d windows
```

进入「助理」→ 选择智能体 → 发送消息，能收到大模型回复即表示前后端已打通。

---

## 四、常用运维命令（服务器）

| 操作       | 命令 |
|------------|------|
| 查看日志   | `sudo docker logs -f jideshi_hibi_api` |
| 停止       | `sudo docker stop jideshi_hibi_api` |
| 启动       | `sudo docker start jideshi_hibi_api` |
| 删除容器   | `sudo docker rm -f jideshi_hibi_api` |
| 重新构建并运行 | 在 `~/jideshi_hibi_backend` 下执行：`sudo docker rm -f jideshi_hibi_api`，再执行上文 2.4 的 `docker build` 与 `docker run` |

---

## 五、故障排查

| 现象                     | 可能原因           | 处理建议 |
|--------------------------|--------------------|----------|
| 本机访问 IP:7861 超时    | 防火墙未放行 7861  | 检查阿里云防火墙/安全组 |
| 容器启动后立刻退出       | 环境变量或代码异常 | `sudo docker logs jideshi_hibi_api` 查看报错 |
| 对话返回「未配置大模型」 | .env 未传入容器    | 确认 `docker run` 使用 `--env-file .env` |
| 对话 401/403             | API 密钥错误或过期 | 检查并更新 .env 中的 `MODEL_API_KEY` |

---

更细的步骤与说明见：`backend_jideshi_hibi_app/README.md`、`backend_jideshi_hibi_app/DEPLOY_ALIYUN.md`。
